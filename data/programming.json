[
  {
    "question": "What is React?",
    "answer": "React is a JavaScript library/framework for building user interfaces (UI). It was created by Facebook, in The Year 2013.",
    "type": "react"
  },
  {
    "question": "What are components in React?",
    "answer": "Components are reusable pieces of code that build elements on a page. They allow breaking complex UI into smaller parts, making it easier to maintain and scale.\n\nComponents can be:\n- **Class components** (older style)\n- **Function components** (modern style)\n\nComponents can also accept **props**, which are inputs used to customize them.",
    "type": "react"
  },
  {
    "question": "How does classList.toggle() work and when should you use it?",
    "answer": "**classList.toggle() - Smart Class Switching**\n\n**🎯 What is classList.toggle()?**\nThe `classList.toggle()` method is a convenient way to add or remove a CSS class from an element. It automatically checks if the class exists and toggles it on/off.\n\n**🔄 Basic Syntax:**\n```javascript\nelement.classList.toggle('className');\n```\n\n**📖 How It Works:**\n```javascript\nconst button = document.querySelector('.my-button');\n\n// Toggle the 'active' class\nbutton.classList.toggle('active');\n\n// First click: adds 'active' class\n// <button class=\"my-button active\">Click me</button>\n\n// Second click: removes 'active' class  \n// <button class=\"my-button\">Click me</button>\n\n// Third click: adds 'active' class again\n// And so on...\n```\n\n**🎨 Practical Examples:**\n\n**Toggle Menu Visibility:**\n```html\n<button class=\"menu-toggle\" onclick=\"toggleMenu()\">☰ Menu</button>\n<nav class=\"main-nav\" id=\"mainNav\">\n  <a href=\"#home\">Home</a>\n  <a href=\"#about\">About</a>\n  <a href=\"#contact\">Contact</a>\n</nav>\n\n<script>\nfunction toggleMenu() {\n  const nav = document.getElementById('mainNav');\n  nav.classList.toggle('nav-open');\n}\n</script>\n\n<style>\n.main-nav {\n  display: none;\n}\n\n.main-nav.nav-open {\n  display: block;\n}\n</style>\n```\n\n**Dark/Light Mode Toggle:**\n```javascript\nfunction toggleDarkMode() {\n  document.body.classList.toggle('dark-mode');\n  \n  // Update button text\n  const button = document.querySelector('.theme-toggle');\n  if (document.body.classList.contains('dark-mode')) {\n    button.textContent = '☀️ Light Mode';\n  } else {\n    button.textContent = '🌙 Dark Mode';\n  }\n}\n```\n\n**Accordion/Collapsible Sections:**\n```html\n<div class=\"accordion\">\n  <button class=\"accordion-header\" onclick=\"toggleSection(this)\">\n    Section 1\n    <span class=\"arrow\">▼</span>\n  </button>\n  <div class=\"accordion-content\">\n    <p>This content can be shown/hidden.</p>\n  </div>\n</div>\n\n<script>\nfunction toggleSection(header) {\n  const content = header.nextElementSibling;\n  content.classList.toggle('active');\n  header.classList.toggle('active');\n}\n</script>\n\n<style>\n.accordion-content {\n  display: none;\n}\n\n.accordion-content.active {\n  display: block;\n}\n\n.accordion-header.active .arrow {\n  transform: rotate(180deg);\n}\n</style>\n```\n\n**🛠️ Advanced Toggle Features:**\n\n**Force State (Add/Remove):**\n```javascript\nconst element = document.querySelector('.my-element');\n\n// Force add (like add() but returns true)\nelement.classList.toggle('active', true);  // Always adds\n\n// Force remove (like remove() but returns false)  \nelement.classList.toggle('active', false); // Always removes\n\n// Normal toggle (automatic)\nelement.classList.toggle('active');        // Toggles based on current state\n```\n\n**Check Current State:**\n```javascript\nfunction isElementActive(element) {\n  return element.classList.contains('active');\n}\n\n// Toggle with condition\nif (isElementActive(myButton)) {\n  console.log('Button is currently active');\n}\n```\n\n**Multiple Classes:**\n```javascript\n// Toggle multiple classes at once\nfunction toggleMultipleClasses(element, ...classNames) {\n  classNames.forEach(className => {\n    element.classList.toggle(className);\n  });\n}\n\n// Usage\ntoggleMultipleClasses(menu, 'open', 'animated', 'visible');\n```\n\n**🎯 Real-World Use Cases:**\n\n**Favorite/Bookmark Toggle:**\n```javascript\nfunction toggleFavorite(bookId) {\n  const heartIcon = document.querySelector(`[data-book=\"${bookId}\"]`);\n  heartIcon.classList.toggle('favorited');\n  \n  // Update icon\n  if (heartIcon.classList.contains('favorited')) {\n    heartIcon.textContent = '❤️';\n  } else {\n    heartIcon.textContent = '🤍';\n  }\n}\n```\n\n**Form Validation States:**\n```javascript\nfunction validateEmail(input) {\n  const isValid = input.value.includes('@');\n  input.classList.toggle('valid', isValid);\n  input.classList.toggle('invalid', !isValid);\n}\n```\n\n**Tab System:**\n```javascript\nfunction switchTab(tabId) {\n  // Remove active from all tabs\n  document.querySelectorAll('.tab').forEach(tab => {\n    tab.classList.remove('active');\n  });\n  \n  // Add active to clicked tab\n  document.getElementById(tabId).classList.add('active');\n}\n\n// Using toggle for simpler implementation\nfunction toggleTab(tabId) {\n  const tab = document.getElementById(tabId);\n  tab.classList.toggle('active');\n}\n```\n\n**📊 Toggle vs Add/Remove:**\n\n```javascript\n// Traditional approach\nif (element.classList.contains('active')) {\n  element.classList.remove('active');\n} else {\n  element.classList.add('active');\n}\n\n// Modern approach (same result)\nelement.classList.toggle('active');\n```\n\n**💡 Best Practices:**\n1. **Use descriptive class names** like `is-open`, `is-active`, `is-visible`\n2. **Combine with CSS transitions** for smooth animations\n3. **Consider accessibility** - update ARIA attributes when toggling\n4. **Use data attributes** to store state if needed\n5. **Test both states** - make sure both on/off states work properly\n\n**🔧 CSS Transition Example:**\n```css\n.menu {\n  transform: translateX(-100%);\n  transition: transform 0.3s ease;\n}\n\n.menu.is-open {\n  transform: translateX(0);\n}\n\n.button {\n  background: blue;\n  transition: background 0.2s;\n}\n\n.button.is-active {\n  background: green;\n}\n```\n\n**Summary:** `classList.toggle()` is perfect for any feature that switches between two states - it's cleaner, more readable, and less error-prone than manually checking and adding/removing classes.",
    "type": "javascript",
    "tags": [
      "javascript",
      "dom",
      "classlist",
      "toggle",
      "ui",
      "interaction",
      "css",
      "animations"
    ]
  },
  {
    "question": "How do you generate random numbers and array elements in JavaScript?",
    "answer": "**JavaScript Random Numbers - Complete Guide**\n\n**🎲 Basic Random Generation:**\n\n**Random Decimal (0 to 1):**\n```javascript\nMath.random()  // Returns: 0.0 to 0.9999... (1 is exclusive)\n// Examples: 0.123, 0.789, 0.456\n```\n\n**Random Whole Number (0 to max-1):**\n```javascript\n// Random from 0 to 9\nMath.floor(Math.random() * 10)\n\n// Random from 0 to 99  \nMath.floor(Math.random() * 100)\n```\n\n**🔢 Random Number in Range (min to max):**\n\n**Inclusive Range Function:**\n```javascript\nfunction getRandomInRange(min, max) {\n  return Math.floor(Math.random() * (max - min + 1)) + min;\n}\n\n// Examples:\ngetRandomInRange(1, 10);   // 1, 2, 3, ..., 10\ngetRandomInRange(5, 15);   // 5, 6, 7, ..., 15\ngetRandomInRange(-5, 5);   // -5, -4, ..., 4, 5\n```\n\n**Formula Breakdown:**\n```javascript\n// Step-by-step for range 5-15:\nMath.random()                     // 0.0 to 0.999...\n* (15 - 5 + 1)                    // * 11 → 0.0 to 10.999...\nMath.floor(...)                   // 0 to 10 (integer)\n+ 5                               // 5 to 15 ✓\n```\n\n**🚫 Excluding Zero (Never Generate 0):**\n```javascript\n// Random from 1 to max (never 0)\nfunction getRandomNonZero(max) {\n  return Math.floor(Math.random() * max) + 1;\n}\n\ngetRandomNonZero(10);  // 1, 2, 3, ..., 10 (never 0)\ngetRandomNonZero(6);   // 1, 2, 3, 4, 5, 6 (dice roll)\n```\n\n**📊 Random Array Elements:**\n\n**Random Element from Array:**\n```javascript\nconst fruits = ['🍎', '🍌', '🍒', '🍊', '🍇'];\n\n// Get random fruit\nconst randomFruit = fruits[Math.floor(Math.random() * fruits.length)];\nconsole.log(randomFruit); // 🍌 (random each time)\n```\n\n**Multiple Random Elements:**\n```javascript\nfunction getRandomElements(array, count) {\n  const shuffled = [...array].sort(() => 0.5 - Math.random());\n  return shuffled.slice(0, count);\n}\n\nconst colors = ['red', 'blue', 'green', 'yellow', 'purple'];\ngetRandomElements(colors, 3); // ['blue', 'red', 'yellow'] (random)\n```\n\n**🎯 Practical Examples:**\n\n**Dice Roll Simulator:**\n```javascript\nfunction rollDice() {\n  return Math.floor(Math.random() * 6) + 1;  // 1-6\n}\n\nconsole.log(`You rolled: ${rollDice()}`); // \"You rolled: 4\"\n```\n\n**Random Color Generator:**\n```javascript\nfunction getRandomColor() {\n  const colors = ['red', 'blue', 'green', 'yellow', 'purple', 'orange'];\n  return colors[Math.floor(Math.random() * colors.length)];\n}\n\ndocument.body.style.backgroundColor = getRandomColor();\n```\n\n**Random Password Character:**\n```javascript\nfunction getRandomChar() {\n  const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';\n  return chars[Math.floor(Math.random() * chars.length)];\n}\n\nconsole.log(getRandomChar()); // \"k\" (random character)\n```\n\n**Lottery Number Generator:**\n```javascript\nfunction generateLotteryNumbers(max, count) {\n  const numbers = [];\n  while (numbers.length < count) {\n    const num = Math.floor(Math.random() * max) + 1;\n    if (!numbers.includes(num)) {\n      numbers.push(num);\n    }\n  }\n  return numbers.sort((a, b) => a - b);\n}\n\ngenerateLotteryNumbers(49, 6); // [5, 12, 23, 34, 41, 49] (random)\n```\n\n**🔄 Alternative Methods:**\n\n**Using Crypto (More Secure Random):**\n```javascript\n// For cryptographic purposes\nfunction getSecureRandom(max) {\n  const randomBuffer = new Uint32Array(1);\n  window.crypto.getRandomValues(randomBuffer);\n  return randomBuffer[0] % max;\n}\n```\n\n**Random Boolean:**\n```javascript\nconst randomBool = Math.random() >= 0.5;  // true or false\n```\n\n**Random with Decimals:**\n```javascript\nfunction getRandomFloat(min, max) {\n  return Math.random() * (max - min) + min;\n}\n\ngetRandomFloat(1.5, 5.5); // 3.214 (random decimal)\n```\n\n**📝 Common Patterns Reference:**\n\n```javascript\n// 0 to N-1\nMath.floor(Math.random() * N)\n\n// 1 to N (no zero)\nMath.floor(Math.random() * N) + 1\n\n// Min to Max (inclusive)\nMath.floor(Math.random() * (Max - Min + 1)) + Min\n\n// Random array element\narray[Math.floor(Math.random() * array.length)]\n\n// Random true/false\nMath.random() >= 0.5\n```\n\n**⚠️ Important Notes:**\n- `Math.random()` is **pseudo-random** - not suitable for cryptography\n- The range is **inclusive of min, exclusive of max** in floating point\n- Use `Math.floor()` for integers, not `Math.round()` (biased distribution)\n- For games/casino apps, consider more secure random number generators\n\n**Performance Tip:** If you need many random numbers, generate them in batches rather than one at a time.",
    "type": "javascript",
    "tags": [
      "javascript",
      "math",
      "random",
      "arrays",
      "numbers",
      "range",
      "algorithms",
      "games"
    ]
  },
  {
    "question": "What is Font Awesome and how do you use icons in projects?",
    "answer": "**Font Awesome - Complete Icon Guide**\n\n**🎯 What is Font Awesome?**\nFont Awesome is a popular icon library and toolkit with thousands of vector icons and social logos. Instead of images, it uses scalable vector icons that look crisp at any size.\n\n**🚀 Quick Setup:**\n```html\n<!-- Add this in your HTML <head> -->\n<link rel=\"stylesheet\" \n      href=\"https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css\" \n      crossorigin=\"anonymous\">\n```\n\n**🎨 Basic Icon Usage:**\n```html\n<!-- Simple icon -->\n<i class=\"fas fa-home\"></i>\n\n<!-- Icon with size -->\n<i class=\"fas fa-home fa-2x\"></i>\n\n<!-- Icon with color -->\n<i class=\"fas fa-home\" style=\"color: blue;\"></i>\n\n<!-- Using <span> instead of <i> -->\n<span class=\"fas fa-home\"></span>\n```\n\n**🔧 Icon Styles:**\n```html\n<!-- Solid (filled) icons -->\n<i class=\"fas fa-star\"></i>\n\n<!-- Regular (outline) icons -->\n<i class=\"far fa-star\"></i>\n\n<!-- Brand icons -->\n<i class=\"fab fa-twitter\"></i>\n<i class=\"fab fa-github\"></i>\n<i class=\"fab fa-react\"></i>\n\n<!-- Light icons (Pro) -->\n<i class=\"fal fa-star\"></i>\n\n<!-- Duotone icons (Pro) -->\n<i class=\"fad fa-star\"></i>\n```\n\n**📝 Icons in Buttons (Common Pattern):**\n```html\n<!-- Button with icon + text -->\n<button class=\"btn\">\n  <i class=\"fas fa-plus\"></i>\n  Add Item\n</button>\n\n<!-- Icon-only button -->\n<button class=\"btn-icon\">\n  <i class=\"fas fa-trash\"></i>\n</button>\n\n<!-- Button with icon on right -->\n<button class=\"btn\">\n  Download\n  <i class=\"fas fa-download\"></i>\n</button>\n```\n\n**🎛️ Sizing & Styling:**\n```html\n<!-- Fixed sizes -->\n<i class=\"fas fa-home fa-xs\"></i>    <!-- Extra small -->\n<i class=\"fas fa-home fa-sm\"></i>    <!-- Small -->\n<i class=\"fas fa-home fa-lg\"></i>    <!-- Large -->\n<i class=\"fas fa-home fa-2x\"></i>    <!-- 2x -->\n<i class=\"fas fa-home fa-3x\"></i>    <!-- 3x -->\n<i class=\"fas fa-home fa-5x\"></i>    <!-- 5x -->\n<i class=\"fas fa-home fa-10x\"></i>   <!-- 10x -->\n\n<!-- Custom CSS sizing -->\n<i class=\"fas fa-home\" style=\"font-size: 24px;\"></i>\n\n<!-- Animation -->\n<i class=\"fas fa-spinner fa-spin\"></i>      <!-- Spin -->\n<i class=\"fas fa-pulse fa-spinner\"></i>     <!-- Pulse -->\n```\n\n**🔄 Rotation & Flipping:**\n```html\n<!-- Rotation -->\n<i class=\"fas fa-cog fa-rotate-90\"></i>\n<i class=\"fas fa-cog fa-rotate-180\"></i>\n<i class=\"fas fa-cog fa-rotate-270\"></i>\n\n<!-- Flipping -->\n<i class=\"fas fa-flip-horizontal fa-user\"></i>\n<i class=\"fas fa-flip-vertical fa-user\"></i>\n```\n\n**🎯 Practical Examples:**\n\n**Navigation Menu:**\n```html\n<nav class=\"navbar\">\n  <a href=\"#home\">\n    <i class=\"fas fa-home\"></i>\n    Home\n  </a>\n  <a href=\"#profile\">\n    <i class=\"fas fa-user\"></i>\n    Profile\n  </a>\n  <a href=\"#settings\">\n    <i class=\"fas fa-cog\"></i>\n    Settings\n  </a>\n</nav>\n```\n\n**Social Media Links:**\n```html\n<div class=\"social-links\">\n  <a href=\"#\"><i class=\"fab fa-twitter\"></i></a>\n  <a href=\"#\"><i class=\"fab fa-facebook\"></i></a>\n  <a href=\"#\"><i class=\"fab fa-instagram\"></i></a>\n  <a href=\"#\"><i class=\"fab fa-linkedin\"></i></a>\n  <a href=\"#\"><i class=\"fab fa-github\"></i></a>\n</div>\n```\n\n**Feature List:**\n```html\n<ul class=\"features\">\n  <li><i class=\"fas fa-check text-success\"></i> Feature one</li>\n  <li><i class=\"fas fa-check text-success\"></i> Feature two</li>\n  <li><i class=\"fas fa-times text-danger\"></i> Not available</li>\n</ul>\n```\n\n**⚠️ Common Mistakes & Solutions:**\n\n**Problem: Icons not showing**\n```html\n<!-- ❌ Wrong - missing CDN -->\n<i class=\"fa-home\"></i>\n\n<!-- ✅ Correct -->\n<link rel=\"stylesheet\" href=\"font-awesome-cdn\">\n<i class=\"fas fa-home\"></i>\n```\n\n**Problem: Unclosed tags causing layout issues**\n```html\n<!-- ❌ Wrong - unclosed tag -->\n<button>\n  <i class=\"fas fa-plus\">\n  Add Item\n</button>\n\n<!-- ✅ Correct - properly closed -->\n<button>\n  <i class=\"fas fa-plus\"></i>\n  Add Item\n</button>\n```\n\n**Problem: Wrong style prefix**\n```html\n<!-- ❌ Wrong - using 'fa' instead of 'fas' -->\n<i class=\"fa fa-home\"></i>\n\n<!-- ✅ Correct - proper style prefix -->\n<i class=\"fas fa-home\"></i>      <!-- Solid -->\n<i class=\"far fa-home\"></i>      <!-- Regular -->\n<i class=\"fab fa-twitter\"></i>   <!-- Brands -->\n```\n\n**💡 Pro Tips:**\n1. **Always close `<i>` tags** to prevent layout issues\n2. **Use semantic class names** like `btn--icon` for styling\n3. **Combine with CSS** for better control over appearance\n4. **Check Font Awesome website** for icon names and availability\n5. **Consider loading locally** for production (better performance)\n\n**CSS Styling Examples:**\n```css\n/* Custom icon styling */\n.btn-icon {\n  background: none;\n  border: none;\n  padding: 8px;\n  cursor: pointer;\n}\n\n.btn-icon:hover {\n  color: #007bff;\n  transform: scale(1.1);\n}\n\n/* Social icons */\n.social-links a {\n  color: #666;\n  text-decoration: none;\n  margin: 0 10px;\n  transition: color 0.3s;\n}\n\n.social-links a:hover {\n  color: #007bff;\n}\n```\n\nFont Awesome makes it easy to add professional-looking icons to any project with minimal setup!",
    "type": "html",
    "tags": [
      "html",
      "css",
      "font-awesome",
      "icons",
      "ui",
      "buttons",
      "styling",
      "web-design"
    ]
  },

  {
    "question": "What are hooks in React?",
    "answer": "Hooks allow us to use state and other React features in function components without writing classes.\n\nCommon hooks include:\n- `useState`\n- `useEffect`\n- `useRef`\n- `useReducer`",
    "type": "react"
  },

  {
    "question": "What are SPA, SSR, and SSG in React?",
    "answer": "- **SPA (Single Page App):** Loads a single HTML file and JS bundle; the entire UI, including routes, is handled in the browser.\n- **SSR (Server-Side Rendering):** The server generates HTML for each request.\n- **SSG (Static Site Generation):** Generates static HTML files at build time, which are very fast to load.",
    "type": "react"
  },
  {
    "question": "What is Vite, and what is its default port and how do you change it?",
    "answer": "Vite is a super fast front-end toolkit for building JavaScript projects. It's built on top of ESBuild, a very fast JS bundler.\n\n**Creating a new Vite project:**\n```\nnpm create vite@latest folder-name\n```\n\n**Installing Tailwind CSS v3:**\n```\nnpm install -D tailwindcss@3 postcss autoprefixer\nnpx tailwindcss init -p\n```\n\n**Vite's default port:**\nThe default port is **5173**.\n\n**Changing the port:**\nModify `vite.config.js`:\n```javascript\nimport react from '@vitejs/plugin-react'\n\nexport default {\n  plugins: [react()],\n  server: {\n    port: 3000, // change to your desired port\n  },\n}\n```",
    "type": "react"
  },

  {
    "question": "What is StrictMode in React?",
    "answer": "`<StrictMode>` is a React component (not a string). It watches your components and warns you if you use outdated or unsafe React patterns.",
    "type": "react"
  },
  {
    "question": "What is the JSX arrow function and normal function component skeleton?",
    "answer": "**Arrow Function Component:**\n```javascript\nimport React from 'react'\n\nconst Hero = () => {\n  return (\n    <div>Hero</div>\n  )\n}\n\nexport default Hero\n```\nVS Code shortcut: `rafce` + Tab (React Arrow Function Export Component)\n\n**Normal Function Component:**\n```javascript\nimport React from 'react'\n\nexport default function Hero() {\n  return (\n    <div>Hero</div>\n  )\n}\n```\nVS Code shortcut: `rfc` + Tab (React Function Component)\n\nRecommended extension: **ES7+ React/Redux/React-Native/JS snippets**.",
    "type": "react"
  },

  {
    "question": "How many root elements can a React component return?",
    "answer": "A React component can only return **one root element**. Returning multiple top-level elements causes an error.\n\n❌ This does NOT work:\n```javascript\nconst App = () => {\n  return (\n    <h1>Hello</h1>\n    <p>Welcome!</p>\n  )\n}\n```\nReact says: “I don’t know which one is the main element!”\n\n**How to fix:**\n1. Wrap elements in a parent container:\n```javascript\nconst App = () => {\n  return (\n    <div>\n      <h1>Hello</h1>\n      <p>Welcome!</p>\n    </div>\n  )\n}\n```\n2. Use a React Fragment (no extra HTML added):\n```javascript\nimport React from 'react'\n\nconst App = () => {\n  return (\n    <>\n      <h1>Hello</h1>\n      <p>Welcome!</p>\n    </>\n  )\n}\n```\n`<> ... </>` acts as a “ghost box”: React sees one root, but no extra `<div>` is added to the HTML. Clean and lightweight.",
    "type": "react"
  },
  {
    "question": "Why is 'index' used as a key in React?",
    "answer": "When mapping over an array in React, you need to provide a **key** for each element so React can track changes efficiently.\n\nExample:\n```javascript\nconst names = [\"Brad\", \"Chuks\", \"Iron Nigga\", \"N9ne\", \"wolf\"];\n\nnames.map((name, index) => (\n  <li key={index}>{name}</li>\n))\n```\n\n- `name` → the value in the array\n- `index` → the position of the value\n\nFor `const names = ['Brad', 'Chuks', 'Iron Nigga', 'N9ne']`:\n- Brad → index 0\n- Chuks → index 1\n- Iron Nigga → index 2\n- N9ne → index 3\n\n**Note:** Using `index` as a key is okay for static lists, but if the array can change, a unique ID is preferred.",
    "type": "react"
  },
  {
    "question": "How can you use multiple returns in React?",
    "answer": "You can have multiple `return` statements in a component using conditional logic. Only the first `return` that runs will execute.\n\nExample:\n```javascript\nconst loggedIn = true;\n\nif (loggedIn) {\n  return <h1>Hello Member</h1>; // 1st return\n}\n\nreturn (\n  <>\n    <div>\n      {loggedIn ? <h1>Hello Member</h1> : <h1>Hello Guest</h1>}\n    </div>\n  </>\n); // 2nd return\n```\nIf the first `return` runs, the second won't. It's recommended to structure your logic with conditionals to decide which return to execute.",
    "type": "react"
  },
  {
    "question": "What are some ways to add styles to React components?",
    "answer": "There are multiple ways to style React components.\n\n**Inline styles:**\n```javascript\nconst App = () => {\n  return (\n    <div\n      style={{\n        color: 'white',\n        backgroundColor: 'teal',\n        fontWeight: 'bold',\n        fontSize: '24px',\n        padding: '20px',\n        margin: '20px',\n        borderRadius: '10px',\n        textAlign: 'center'\n      }}\n    >\n      Hello, this is Sample 1\n    </div>\n  )\n}\n\nexport default App;\n```\n\n**Using a style object:**\n```javascript\nconst styles = {\n  color: 'red',\n  fontSize: '55px'\n};\n\nconst App = () => {\n  return <div style={styles}>Hello</div>;\n}\n```\nBoth approaches are valid; using a style object helps keep code cleaner when many styles are applied.",
    "type": "react"
  },
  {
    "question": "How do you import and export components in JSX?",
    "answer": "To use components in React, you need to **export** them from their file and **import** them where you want to use them.\n\n**Steps:**\n1. Create a component in a file inside the `components` folder (e.g., `Navbar.jsx`). You can use the VS Code shortcut `rafce` to generate an arrow function component template.\n```javascript\n// Navbar.jsx\nimport React from 'react';\n\nconst Navbar = () => {\n  return (\n    <nav>\n      <h1>My Navbar</h1>\n    </nav>\n  );\n}\n\nexport default Navbar;\n```\n2. In your main file (e.g., `App.jsx`), import the component:\n```javascript\nimport Navbar from './components/Navbar';\n```\n3. Use the component in JSX:\n```jsx\n<Navbar />\n```\n**Note:** Component names must start with a **capital letter**.",
    "type": "react"
  },
  {
    "question": "What are props in React?",
    "answer": "Props (short for \"properties\") are values you pass from a parent component to a child component. They make components dynamic and reusable.\n\n**Example with `<Hero />` component:**\n```javascript\n// Hero.jsx (child component)\nconst Hero = (props) => {\n  return (\n    <div>\n      <h1>{props.title}</h1>\n      <p>{props.subtitle}</p>\n    </div>\n  );\n}\nexport default Hero;\n\n// App.jsx (parent component)\nimport Hero from './components/Hero';\n\nconst App = () => {\n  return (\n    <div>\n      <Hero title=\"Test Title\" subtitle=\"This is a subtitle\" />\n    </div>\n  );\n}\nexport default App;\n```\n- `props.title` → \"Test Title\"\n- `props.subtitle` → \"This is a subtitle\"\n\n**Destructuring props for cleaner code:**\n```javascript\nconst Hero = ({ title, subtitle }) => {\n  return (\n    <div>\n      <h1>{title}</h1>\n      <p>{subtitle}</p>\n    </div>\n  );\n}\n```\n**Destructuring with default values:**\n```javascript\nconst Hero = ({ title = 'This is the default title', subtitle = 'Default subtitle' }) => {\n  return (\n    <div>\n      <h1>{title}</h1>\n      <p>{subtitle}</p>\n    </div>\n  );\n}\n```\n- If the parent passes props, they override defaults.\n- If the parent does not pass props, default values are used.",
    "type": "react"
  },
  {
    "question": "What is wrapping in React?",
    "answer": "Wrapping means putting one piece of content inside a component so that the component can automatically add styles, structure, or behavior. The content inside becomes `children` of that component.\n\n**Example:**\n```javascript\n// Card.jsx\nconst Card = ({ children }) => {\n  return (\n    <div className=\"bg-blue-200 p-4 rounded-lg shadow-md\">\n      {children}\n    </div>\n  );\n}\nexport default Card;\n\n// App.jsx\nimport Card from './components/Card';\n\nconst App = () => {\n  return (\n    <div>\n      <Card>\n        <h1>Hello!</h1>\n        <p>This is a wrapped paragraph.</p>\n      </Card>\n\n      <Card>\n        <button>Click Me</button>\n      </Card>\n    </div>\n  );\n}\nexport default App;\n```\n**Result:** The `<h1>` and `<p>` inherit the Card styling; the `<button>` inside the second Card also inherits the same styling. ✅",
    "type": "react"
  },
  {
    "question": "What is useState in React?",
    "answer": "Think of `useState` as a special box to store a value in React. You can change the value anytime, and React will automatically update the UI.\n\n**Example: Counter**\n```javascript\nimport { useState } from 'react';\n\nconst Counter = () => {\n  const [count, setCount] = useState(0); // count = current value, setCount = function to update it\n\n  return (\n    <div>\n      <h1>Count: {count}</h1>\n      <button onClick={() => setCount(count + 1)}>Increase</button>\n    </div>\n  );\n};\nexport default Counter;\n```\n- `count` → current value\n- `setCount` → updates the value\n- React automatically re-renders the component when the value changes.",
    "type": "react"
  },
  {
    "question": "What is the substring() method in JavaScript?",
    "answer": "`substring()` extracts part of a string based on start and end positions.\n\n**Syntax:**\n```javascript\nstring.substring(startIndex, endIndex);\n```\n- `startIndex` → starting index (inclusive)\n- `endIndex` → ending index (exclusive, optional)\n\n**Examples:**\n```javascript\nconst text = 'Hello World';\nconsole.log(text.substring(0, 5)); // Output: Hello\n\nconst name = 'Evermind';\nconsole.log(name.substring(4)); // Output: mind\n```",
    "type": "javascript"
  },
  {
    "question": "How to use previous state in React?",
    "answer": "React allows you to access the previous state value when updating state.\n\n**Example: toggling a switch**\n```javascript\nconst [lightIsOn, setLightIsOn] = useState(false);\n\nsetLightIsOn(prev => !prev);\n```\n- `prev` → previous value\n- `!prev` → flips it\n- Clicking repeatedly toggles the value between `true` and `false`.",
    "type": "react"
  },
  {
    "question": "How do you use icons in React?",
    "answer": "You can use icons via the `react-icons` library.\n\n**Install:**\n```\nnpm install react-icons\n```\n\n**Basic Usage:**\n```javascript\nimport { FaLocationDot } from 'react-icons/fa6';\n\nfunction App() {\n  return <FaLocationDot />;\n}\n```\nThis gives you access to all major icon libraries, including Font Awesome.\n\n**Example with buttons in a TodoList:**\n```javascript\nimport { FaCheck, FaTrash } from 'react-icons/fa';\n\nfunction TodoItem({ task, toggleComplete, deleteTask }) {\n  return (\n    <div>\n      <span>{task.text}</span>\n      <button onClick={() => toggleComplete(task.key)}>\n        <FaCheck />\n      </button>\n      <button onClick={() => deleteTask(task.key)}>\n        <FaTrash />\n      </button>\n    </div>\n  );\n}\n```\nThis shows how to render icons inside buttons and interact with them in a React component.",
    "type": "react"
  },
  {
    "question": "What is a layout in React?",
    "answer": "A layout is a component that wraps around pages and keeps certain UI elements persistent across navigation. Think of it as the 'frame' of your app — things that stay the same no matter what page you're on.\n\n**Real-life analogy:**\n- Navbar, sidebar, footer stay the same.\n- Only the main content changes when switching pages.\n\nUsing a layout avoids repeating common elements on every page, especially with React Router.",
    "type": "react"
  },

  {
    "question": "What are Link and NavLink in React Router?",
    "answer": "`<Link>` is a React Router component that navigates without refreshing the whole page.\n\n**Example:**\n```javascript\nimport { Link } from 'react-router-dom';\n\n<Link to='/about'>Go to About</Link>\n```\n\n`<NavLink>` is similar but can detect the active route and style itself differently.\n\n**Example:**\n```javascript\nimport { NavLink } from 'react-router-dom';\n\n<NavLink \n  to='/about'\n  style={({ isActive }) => ({\n    color: isActive ? 'red' : 'blue'\n  })}\n>\n  About\n</NavLink>\n```\n- Active page → red text\n- Other pages → blue text",
    "type": "react"
  },
  {
    "question": "What path do you use as a catch-all route in React?",
    "answer": "A catch-all route matches any path that doesn't exist. Think of it as a safety net for unknown URLs.\n\n**Example (React Router v6):**\n```javascript\nimport { createBrowserRouter } from 'react-router-dom';\nimport Home from './pages/Home';\nimport NotFound from './pages/NotFound';\n\nconst router = createBrowserRouter([\n  { path: '/', element: <Home /> },\n  { path: '*', element: <NotFound /> } // Catch-all route\n]);\n```",
    "type": "react"
  },
  {
    "question": "Why do some JSX tags have slashes and how do you know which to use?",
    "answer": "In JSX:\n- `</Tag>` → closing tag. Ends a tag that has children/text.\n```jsx\n<p>Hello</p> // </p> closes the paragraph\n```\n- `<Tag />` → self-closing tag. No children.\n```jsx\n<Outlet />\n<img src='logo.png' />\n```\n**Rule:**\n- Has children/text → opening + closing tags\n- No children → self-closing\nReact follows HTML rules.\n\nAnalogy:\n- `<p>` → open the box\n- `</p>` → close the box\n- `<br />` → one thing, done, no box needed",
    "type": "react"
  },
  {
    "question": "How do you run a custom JSON server?",
    "answer": "1. Install JSON Server:\n```\nnpm install json-server\n```\n2. Add a script to `package.json`:\n```json\n\"server\": \"json-server --watch src/jobs.json --port <port-num>\"\n```\n3. Run it:\n```\nnpm run server\n```\nThis will start a local server serving your JSON file at the specified port.",
    "type": "javascript"
  },

  {
    "question": "What is the ENOENT error?",
    "answer": "ENOENT stands for “Error NO ENTity” → it means a file was not found.\n\nExample:\n```\nnpm error enoent Could not read package.json\n```\nThis happens when npm is looking for `package.json` in your project folder (e.g., `C:\\Users\\USER\\Desktop\\TO-DO-LIST REACT`) but it doesn’t exist. \n\n`package.json` is mandatory because npm uses it to know your project details and scripts.",
    "type": "javascript"
  },
  {
    "question": "Explain what each file does in the src folder after installing Vite",
    "answer": "**App.css**\n- Think of App.css like the paint and decoration for your main block (App.jsx).\n- It only affects things inside App.jsx unless made global.\n\n```css\n/* App.css */\nh1 {\n  color: darkblue;\n  text-align: center;\n}\n\nbutton {\n  background-color: lightgreen;\n  padding: 10px;\n  border: none;\n  border-radius: 5px;\n}\n```\n```javascript\nimport './App.css';\n```\n\n**App.jsx**\n- Main block—the house of your app.\n- Anything you want displayed goes here: headings, buttons, inputs, etc.\n- Written in JSX (JavaScript + HTML).\n```javascript\nimport React from 'react';\nimport './App.css';\n\nfunction App() {\n  return (\n    <div>\n      <h1>My Todo List</h1>\n      <input placeholder=\"Type a todo\" />\n      <button>Add</button>\n    </div>\n  );\n}\nexport default App;\n```\n\n**index.css**\n- Global styles for the entire page (like painting the playground where your app sits).\n```css\nbody {\n  background-color: #f0f0f0;\n  font-family: Arial, sans-serif;\n  margin: 0;\n}\n\nul {\n  list-style: none;\n  padding: 0;\n}\n```\n\n**main.jsx**\n- Magic truck that takes your App.jsx and mounts it on the page (`<div id=\"root\"></div>`).\n- ReactDOM.createRoot finds the root box and `.render(<App />)` puts the app there and keeps it live.\n```javascript\nimport React from 'react';\nimport ReactDOM from 'react-dom/client';\nimport App from './App';\nimport './index.css';\n\nReactDOM.createRoot(document.getElementById('root')).render(<App />);\n```\nAnalogy:\n- root div = empty stage\n- App.jsx = LEGO house\n- main.jsx = truck that delivers the house onto the stage and makes it live",
    "type": "react"
  },

  {
    "question": "How do we store and manage tasks and input in React using useState?",
    "answer": "In React, state stores data that the app needs to remember and update automatically.\n\n- `todos` stores all tasks as an array.\n- `newTodo` stores the text currently typed by the user.\n\nWe use `useState` to create these “boxes” that React watches.\n\n**Code Example:**\n```javascript\nimport { useState } from 'react';\n\nfunction App() {\n  // Basket of all tasks\n  const [todos, setTodos] = useState([]);\n\n  // The task currently being typed\n  const [newTodo, setNewTodo] = useState('');\n\n  return (\n    <div>\n      <h1>My Todo List</h1>\n    </div>\n  );\n}\n\nexport default App;\n```\n\n**Explanation:**\n- `useState([])` → empty basket, because we start with no tasks\n  - `todos` = the basket itself\n  - `setTodos` = helper to add/remove tasks\n- `useState('')` → empty hand, because input is empty at first\n  - `newTodo` = task in hand\n  - `setNewTodo` = updates input as the user types",
    "type": "react"
  },
  {
    "question": "What is the dependency array in React (useEffect)?",
    "answer": "`useEffect` lets you “do something when needed.”\n\nThe dependency array is a list of things to watch, controlling when the effect runs:\n- `[]` → run once when component mounts\n- No array → run every render\n- `[var1, var2]` → run only when `var1` or `var2` changes\n\n**Why it matters:**\n- Avoids doing the same thing too many times\n- Keeps your app fast and predictable\n\n**Code Example:**\n```javascript\nimport { useState, useEffect } from 'react';\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  // 1. Empty array -> run once\n  useEffect(() => {\n    console.log('Runs only once when component mounts');\n  }, []);\n\n  // 2. No array -> run every render\n  useEffect(() => {\n    console.log('Runs on every render');\n  });\n\n  // 3. Array with dependencies -> run only when count changes\n  useEffect(() => {\n    console.log('Count changed:', count);\n  }, [count]);\n\n  return (\n    <div>\n      <p>{count}</p>\n      <button onClick={() => setCount(count + 1)}>Increment</button>\n    </div>\n  );\n}\n\nexport default Counter;\n```",
    "type": "react"
  },

  {
    "question": "How do we load saved tasks from localStorage into a React state array, and why do we use objects with text, completed, and key instead of just strings?",
    "answer": "We use a `useEffect` hook to load tasks when the component mounts. Each task is stored as an object with three properties:\n\n- `key` → a unique identifier for the task\n- `text` → the actual task description retrieved from localStorage\n- `completed` → a boolean to track whether the task is done\n\nWe don’t just store strings because objects allow us to store multiple pieces of information for each task, like the description, status, and unique key.\n\n**Code Example:**\n```javascript\nimport { useState, useEffect } from 'react';\n\nfunction TodoApp() {\n  const [savedTodos, setSavedTodos] = useState([]);\n\n  useEffect(() => {\n    const todos = [];\n    for (let i = 0; i < localStorage.length; i++) {\n      const key = localStorage.key(i);\n      if (key.startsWith('newTask')) {\n        todos.push({\n          key,                     // unique identifier\n          text: localStorage.getItem(key), // task description\n          completed: false,        // task status\n        });\n      }\n    }\n    setSavedTodos(todos);\n  }, []);\n}\n```",
    "type": "react"
  },
  {
    "question": "How do we make a React input field update state with onChange?",
    "answer": "We make the input controlled by React using the `value` prop and `onChange` event. The `value` shows the current state, and `onChange` updates the state whenever the user types.\n\n**Code Example:**\n```javascript\n<input \n  value={newTodo} \n  onChange={(e) => setNewTodo(e.target.value)} \n  placeholder=\"Enter Task\" \n/>\n```\n\n**Explanation:**\n- `value={newTodo}` → input shows what React remembers\n- `onChange={(e) => setNewTodo(e.target.value)}` → updates React state whenever typing occurs\n\nFlow: Typing → triggers `onChange` → updates `newTodo` → input updates because `value={newTodo}`",
    "type": "react"
  },
  {
    "question": "How do we add a new task in a React TodoList and save it for later?",
    "answer": "We create a function (`addTodo`) that:\n1. Checks the input is not empty\n2. Creates a task object `{key, text, completed}`\n3. Adds it to the `todos` state array\n4. Saves it in `localStorage`\n5. Clears the input\n\n**Code Example:**\n```javascript\nconst addTodo = () => {\n  if (newTodo.trim() === '') return;\n\n  let i = 0;\n  while (localStorage.getItem(`newTask${i}`) !== null) i++;\n  const key = `newTask${i}`;\n\n  localStorage.setItem(key, newTodo);\n  const newTask = { key, text: newTodo, completed: false };\n  setTodos([...todos, newTask]);\n  setNewTodo('');\n};\n```",
    "type": "react"
  },
  {
    "question": "How do we display tasks stored in React state on the page with buttons for completing and deleting?",
    "answer": "We loop through the state array (`todos`) using `map` and return JSX for each task. Each task shows:\n- Text (with style depending on `completed`)\n- Check button → flips completed status\n- Delete button → removes the task\n\n**Code Example:**\n```javascript\n{todos.map((task) => (\n  <div key={task.key} className=\"task\">\n    <li\n      style={{\n        textDecoration: task.completed ? \"line-through\" : \"none\",\n        color: task.completed ? \"red\" : \"black\",\n      }}\n    >\n      {task.text}\n    </li>\n    <button onClick={() => toggleComplete(task.key)}>\n      <i className=\"fa-solid fa-check\"></i>\n    </button>\n    <button onClick={() => deleteTask(task.key)}>\n      <i className=\"fa-solid fa-trash-can\"></i>\n    </button>\n  </div>\n))}\n```",
    "type": "react"
  },
  {
    "question": "How do we mark a task as completed in a React TodoList?",
    "answer": "We create a function `toggleComplete` that:\n1. Loops through the `todos` array\n2. Finds the task that was clicked\n3. Flips its `completed` property (false → true, true → false)\n4. Updates React state so the page redraws\n\n**Code Example:**\n```javascript\nconst toggleComplete = (key) => {\n  setTodos(\n    todos.map((task) =>\n      task.key === key ? { ...task, completed: !task.completed } : task\n    )\n  );\n};\n```",
    "type": "react"
  },
  {
    "question": "How do we delete a task from a React Todo List and localStorage?",
    "answer": "We filter the list to remove the task with the matching key, then remove it from `localStorage`.\n\n**Code Example:**\n```javascript\nconst deleteTask = (key) => {\n  setTodos(todos.filter((task) => task.key !== key));\n  localStorage.removeItem(key);\n};\n```",
    "type": "react"
  },

  {
    "question": "How do you use React Router in React?",
    "answer": "React Router is a library that enables page-to-page navigation without refreshing the browser.\n\n**Step 1: Install React Router**\n```\nnpm install react-router-dom\n```\n\n**Step 2: Wrap your app with <BrowserRouter>**\nIn `main.jsx` / `index.jsx`:\n```javascript\nimport React from 'react';\nimport ReactDOM from 'react-dom/client';\nimport { BrowserRouter } from 'react-router-dom';\nimport App from './App';\n\nReactDOM.createRoot(document.getElementById('root')).render(\n  <BrowserRouter>\n    <App />\n  </BrowserRouter>\n);\n```\nThis enables routing for the entire app. Without it, routes won’t work.\n\n**Step 3: Define your routes inside App.jsx**\n```javascript\nimport { Routes, Route } from 'react-router-dom';\nimport HomePage from './pages/HomePage';\nimport CategoryDetailsPage from './pages/CategoryDetailsPage';\n\nfunction App() {\n  return (\n    <Routes>\n      <Route path='/' element={<HomePage />} />\n      <Route path='/category/:categoryId' element={<CategoryDetailsPage />} />\n    </Routes>\n  );\n}\n\nexport default App;\n```\n- `/` → HomePage\n- `/category/:categoryId` → dynamic category page (ex: `/category/Food`)\n\n**Step 4: Create the dynamic category page**\nFile: `/src/pages/CategoryDetailsPage.jsx`\n```javascript\nimport React from 'react';\nimport { useParams, Link } from 'react-router-dom';\n\nconst CategoryDetailsPage = () => {\n  const { categoryId } = useParams(); // gets value from URL\n\n  return (\n    <section style={{ padding: '20px' }}>\n      <Link to='/'>⬅ Back</Link>\n      <h2>Viewing expenses for: {categoryId}</h2>\n    </section>\n  );\n};\n\nexport default CategoryDetailsPage;\n```\n- `useParams()` extracts the dynamic part of the URL.\n\n**Step 5: Navigate with <Link>**\n```javascript\n<Link to={`/category/${categoryName}`}>\n  Show More\n</Link>\n```\n- Wrap your routes inside `<Routes>`.\n- `<Link>` enables navigation without full page reloads.",
    "type": "react"
  },
  {
    "question": "How can you toggle the visibility of a form in React using useState and pass a function to close it from a child component?",
    "answer": "You can control the form's visibility with `useState` and pass a function as a prop to close it from the child component.\n\n**Parent Component:**\n```javascript\nimport React, { useState } from 'react';\nimport AddNewExpense from './AddNewExpense';\n\nconst App = () => {\n  const [showAddForm, setShowAddForm] = useState(false); // form hidden initially\n\n  return (\n    <section>\n      {/* Show button only if form is hidden */}\n      {!showAddForm && (\n        <button onClick={() => setShowAddForm(true)}>\n          + Add New Expense\n        </button>\n      )}\n\n      {/* Show form if showAddForm is true */}\n      {showAddForm && (\n        <AddNewExpense closeForm={() => setShowAddForm(false)} />\n      )}\n    </section>\n  );\n};\n\nexport default App;\n```\n\n**Child Component (AddNewExpense.jsx):**\n```javascript\nimport React from 'react';\n\nconst AddNewExpense = ({ closeForm }) => {\n  return (\n    <div>\n      <h2>Add Expense Form (UI coming soon)</h2>\n      <button onClick={closeForm}>Cancel</button>\n    </div>\n  );\n};\n\nexport default AddNewExpense;\n```\n\n**Explanation:**\n- `useState(false)` → form starts hidden.\n- Clicking **Add New Expense** → sets `showAddForm` to true → form appears.\n- `closeForm` function passed as prop → lets the child component hide the form by calling `setShowAddForm(false)`.",
    "type": "react"
  },
  {
    "question": "How does the && (AND) operator work in vanilla JavaScript vs React JSX for conditional rendering?",
    "answer": "1️⃣ **Vanilla JavaScript (short-circuit behavior)**\n```javascript\ntrue && 'Hello';    // returns 'Hello'\nfalse && 'Hello';   // returns false\n\n// Example:\nconst isLoggedIn = true;\nconsole.log(isLoggedIn && 'Welcome back!'); // prints 'Welcome back!'\n```\n- `&&` checks the left side first.\n- If left side is true → returns the right side.\n- If left side is false → stops and returns false.\n\n2️⃣ **React JSX (conditional rendering)**\n```javascript\nimport React, { useState } from 'react';\n\nconst App = () => {\n  const [showMessage, setShowMessage] = useState(false);\n\n  return (\n    <div>\n      <button onClick={() => setShowMessage(true)}>Show Message</button>\n\n      {/* Only renders <p> if showMessage is true */}\n      {showMessage && <p>Hello, this is a message!</p>}\n    </div>\n  );\n};\n\nexport default App;\n```\n- `showMessage && <p>...</p>` works the same as JS:\n  - `true && <p>` → renders `<p>`\n  - `false && <p>` → renders nothing (React ignores `false`)\n\n✅ **Key Takeaways:**\n- `&&` always evaluates the left side first.\n- In JS, it returns either the left (falsey) or right value.\n- In JSX, it’s shorthand for \"render this component only if the left side is true.\"\n- React re-evaluates `&&` every time state changes, so components appear/disappear automatically.",
    "type": "react"
  },

  {
    "question": "How do you get and store today’s date in React using useState?",
    "answer": " **Short summary:**\nYou can use JavaScript’s Date object with `useState` to save today’s date in a readable format (YYYY-MM-DD).\n\n🧩 **Step-by-step explanation:**\n```javascript\nimport { useState } from 'react';\n\nconst App = () => {\n  const [date, setDate] = useState(\n    new Date().toISOString().split('T')[0]\n  );\n\n  return (\n    <div>\n      <p>Today's date: {date}</p>\n    </div>\n  );\n};\n\nexport default App;\n```\n**Steps explained:**\n1️⃣ `new Date()` → Creates a Date object with the current date and time.\n2️⃣ `.toISOString()` → Converts the date into a string like \"2025-11-12T14:00:00.000Z\".\n3️⃣ `.split('T')[0]` → Removes the time and keeps only the date part \"2025-11-12\".\n4️⃣ `useState(...)` → Stores this date in React’s state so you can use or update it later with `setDate()`."
  },
  {
    "question": "What is the preventDefault() function in JavaScript, and how do you use it?",
    "answer": "**Short summary:**\n`preventDefault()` is a function you call on an event (`e`) to stop the browser from doing its normal action.\n- For forms → stops page reload on submit\n- For links → stops navigation\n- Useful anywhere you want to handle events yourself instead of the browser's default\n\n🧩 **Examples:**\n\n1️⃣ **On a form submit:**\n```javascript\nconst handleSubmit = (e) => {\n  e.preventDefault(); // stop page reload\n  console.log('Form submitted, but page did not reload!');\n};\n\n<form onSubmit={handleSubmit}>\n  <input type=\"text\" placeholder=\"Type something\" />\n  <button type=\"submit\">Submit</button>\n</form>\n```\nWithout `preventDefault()` → page reloads on submit\nWith `preventDefault()` → handle submission yourself without reload\n\n2️⃣ **On a link:**\n```javascript\n<a\n  href=\"https://google.com\"\n  onClick={(e) => {\n    e.preventDefault(); // stop navigation\n    alert('Link clicked, but browser did not go to Google');\n  }}\n>\n  Click me\n</a>\n```\nNormally, clicking would go to Google. `preventDefault()` stops that, letting you handle the click in React."
  },

  {
    "question": "What are some common 'on' event handlers / event props in React?",
    "answer": "React uses special props that start with 'on' to handle events. Here are some key ones:\n\n1️⃣ onClick\nRuns when you click an element.\n```jsx\n<button onClick={() => console.log('Button clicked!')}>Click me</button>\n```\nEvery click triggers the function.\n\n2️⃣ onChange\nRuns when an input's value changes.\n```jsx\n<input type=\"text\" onChange={(e) => console.log(e.target.value)} />\n```\nEvery keystroke triggers the function.\n\n3️⃣ onSubmit\nRuns when a form is submitted (Enter key or submit button).\n```jsx\n<form onSubmit={handleSubmit}>\n  <input type=\"text\" />\n  <button type=\"submit\">Submit</button>\n</form>\n```\nWithout `e.preventDefault()`, the page reloads. With it, you can handle the data yourself.\n\nOther useful event props:\n- onMouseEnter → triggers when mouse enters an element\n- onMouseLeave → triggers when mouse leaves an element\n- onKeyDown → triggers when a key is pressed\n- onKeyUp → triggers when a key is released\n- onFocus → triggers when input gets focus\n- onBlur → triggers when input loses focus\n- onDoubleClick → triggers on double click\n- onScroll → triggers when a container is scrolled",
    "type": "react"
  },
  {
    "question": "Why is it better to use <label> with inputs in React?",
    "answer": "Using a `<label>` with an input improves accessibility and user experience.\n\n1️⃣ Wrap input inside the label:\n```jsx\n<label>\n  Expense Title:\n  <input\n    type=\"text\"\n    placeholder=\"e.g. Movie ticket\"\n    value={title}\n    onChange={(e) => setTitle(e.target.value)}\n  />\n</label>\n```\nKey points:\n- The text inside the label is automatically linked to the input.\n- Clicking the label focuses the input.\n- Improves accessibility for screen readers.\n- No need for `id` + `htmlFor` if input is nested inside the label.",
    "type": "react"
  },

  {
    "question": "What is rendering in React, and can you give examples?",
    "answer": "Rendering in React means turning your JSX code into actual HTML elements that the browser can display. React updates the DOM efficiently whenever state or props change.\n\n🧩 Examples:\n\n1️⃣ Simple JSX Render\n```jsx\nfunction App() {\n  return <h1>Hello World!</h1>;\n}\n```\nReact renders the <h1> element on the screen.\n\n2️⃣ Conditional Rendering\n```jsx\nfunction Greeting({ isLoggedIn }) {\n  return (\n    <div>\n      {isLoggedIn ? <p>Welcome back!</p> : <p>Please log in.</p>}\n    </div>\n  );\n}\n```\nReact decides what to show based on the `isLoggedIn` prop.\n\n3️⃣ Rendering Components\n```jsx\nfunction Dashboard() {\n  return (\n    <div>\n      <UserProfile />\n      <Notifications />\n    </div>\n  );\n}\n```\nReact renders each component inside `Dashboard`, and each of those components renders their own JSX.\n\n4️⃣ Re-rendering on state change\n```jsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    <div>\n      <p>You clicked {count} times</p>\n      <button onClick={() => setCount(count + 1)}>Click me</button>\n    </div>\n  );\n}\n```\nEach click updates state and triggers a re-render, redrawing the component with the new count.",
    "type": "react"
  },
  {
    "question": "What does 'prev' mean in React state updates?",
    "answer": "In React, `prev` refers to the previous state value when using the functional form of `setState`:\n```jsx\nsetState(prev => newState)\n```\nReact automatically passes the latest state as `prev`, which ensures your update is based on the most current state, even if multiple updates happen quickly.\n\n🧩 Example with an expense list:\n```jsx\nconst [expenses, setExpenses] = useState([]);\n\nconst handleAddExpense = (expense) => {\n  setExpenses((prev) => [expense, ...prev]);\n};\n```\nExplanation:\n- `prev` = old array of expenses\n- `[expense, ...prev]` = new array with the new expense added at the front\n- React replaces the old state with the new array\n\nYou can rename `prev` to anything; React just passes the latest state automatically:\n```jsx\nsetExpenses((currentState) => {\n  console.log(currentState);\n  return [expense, ...currentState];\n});\n```",
    "type": "react"
  },

  {
    "question": "How do you toggle a value with a button in React?",
    "answer": "You can toggle a boolean state when clicking a button using useState.\n\nExample:\n```jsx\nimport React, { useState } from 'react';\n\nfunction App() {\n  const [dropdown, setDropdown] = useState(false);\n\n  return (\n    <button onClick={() => setDropdown(!dropdown)}>\n      {dropdown ? 'Hide' : 'Show'} Dropdown\n    </button>\n  );\n}\n```\nExplanation:\n- `!dropdown` flips the current value (true → false, false → true)\n- Clicking the button updates the state, causing a re-render with the new value.",
    "type": "react"
  },
  {
    "question": "Why doesn’t style[1] work when styles are defined as an object in React, and how can you fix it?",
    "answer": "If you define styles as an object, numeric indices do not exist — you must access properties by their key names.\n\nExample of incorrect usage:\n```jsx\nconst style = {\n  color: 'red',\n  backgroundColor: 'orange',\n  newest: { color: 'blue', backgroundColor: 'red' }\n};\n\n// style[1] is undefined\n```\n✅ Correct way (access by key):\n```jsx\n<button style={style.newest} onClick={() => setDropdown(!dropdown)}>\n  Show Stats\n</button>\n```\n✅ Alternative: use an array of style objects:\n```jsx\nconst styles = [\n  { color: 'red', backgroundColor: 'orange' },\n  { color: 'blue', backgroundColor: 'red' }\n];\n<button style={styles[1]} onClick={() => setDropdown(!dropdown)}>\n  Show Stats\n</button>\n```\nArrays allow numeric indexing.",
    "type": "react"
  },
  {
    "question": "What is useMemo in React and how do you use it?",
    "answer": "useMemo is a React Hook that memoizes (remembers) the result of a calculation so React can skip recomputing it if dependencies haven't changed.\n\nExample:\n```jsx\nimport React, { useState, useMemo } from 'react';\n\nfunction App() {\n  const [count, setCount] = useState(0);\n  const [text, setText] = useState('');\n\n  const expensiveCalculation = useMemo(() => {\n    console.log('Calculating...');\n    return count * 2;\n  }, [count]);\n\n  return (\n    <div>\n      <p>Expensive calculation: {expensiveCalculation}</p>\n      <button onClick={() => setCount(count + 1)}>Increase Count</button>\n      <input value={text} onChange={e => setText(e.target.value)} />\n    </div>\n  );\n}\n```\n- First argument: function that returns a value\n- Second argument: array of dependencies\n- React recomputes only when dependencies change, optimizing performance.",
    "type": "react"
  },
  {
    "question": "What is the .toFixed() method in JavaScript?",
    "answer": ".toFixed() is a Number method that rounds a number to a specified number of decimal places and returns a string.\n\nSyntax:\n```js\nnum.toFixed(n) // n = digits after decimal\n```\nExamples:\n```js\nconst num = 12.3456;\nnum.toFixed(2); // '12.35'\nnum.toFixed(0); // '12'\nnum.toFixed(4); // '12.3456'\n```\nNote: Result is always a string. To convert back to a number:\n```js\nconst rounded = parseFloat(num.toFixed(2)); // 12.35 as number\n```",
    "type": "javascript"
  },
  {
    "question": "How do you calculate today's, this month's, and this year's totals from a list of expenses in React?",
    "answer": "You can loop through all expenses, compare each expense's date to today's date using getFullYear(), getMonth(), and getDate(), then update running totals stored in React state.\n\n**Example:**\n```javascript\nimport React, { useState, useEffect } from 'react';\n\nfunction ExpenseSummary({ expenses }) {\n  const [totals, setTotals] = useState({\n    totalToday: 0,\n    totalThisMonth: 0,\n    totalThisYear: 0,\n  });\n\n  useEffect(() => {\n    const today = new Date();\n    const newTotals = { totalToday: 0, totalThisMonth: 0, totalThisYear: 0 };\n\n    expenses.forEach(({ amount, date }) => {\n      const expenseDate = new Date(date);\n\n      const isSameYear = expenseDate.getFullYear() === today.getFullYear();\n      if (!isSameYear) return;\n\n      newTotals.totalThisYear += amount;\n\n      const isSameMonth = expenseDate.getMonth() === today.getMonth();\n      if (isSameMonth) {\n        newTotals.totalThisMonth += amount;\n\n        const isSameDay = expenseDate.getDate() === today.getDate();\n        if (isSameDay) newTotals.totalToday += amount;\n      }\n    });\n\n    setTotals(newTotals);\n  }, [expenses]);\n\n  return (\n    <section>\n      <h3>Expense Summary</h3>\n      <p>Today: ${totals.totalToday}</p>\n      <p>This Month: ${totals.totalThisMonth}</p>\n      <p>This Year: ${totals.totalThisYear}</p>\n    </section>\n  );\n}\n\nexport default ExpenseSummary;\n```\n\n**Explanation:**\n- **useEffect:** Recalculates totals whenever the expenses array changes.\n- **getFullYear, getMonth, getDate:** Used to compare each expense’s date with today.\n- **isSameYear → isSameMonth → isSameDay:** Nested checks ensure proper totals without unnecessary calculations.\n- **setTotals:** Updates the component’s state, triggering a re-render to show fresh totals.\n\nThis pattern is great for dashboards or expense trackers that need daily, monthly, and yearly summaries automatically recalculated when new data arrives.",
    "type": "react"
  },
  {
    "question": "How do you check if a date is in the same year, month, or day as today in JavaScript?",
    "answer": "You can compare different parts of a Date object using getFullYear(), getMonth(), and getDate(). This helps categorize data by year, month, or day.\n\n**Example:**\n```javascript\nconst isSameYear = expenseDate.getFullYear() === today.getFullYear();\nif (!isSameYear) return;\n\ntotals.totalThisYear += amount;\n\nconst isSameMonth = expenseDate.getMonth() === today.getMonth() && isSameYear;\nif (isSameMonth) {\n  totals.totalThisMonth += amount;\n\n  const isSameDay = expenseDate.getDate() === today.getDate();\n  if (isSameDay) {\n    totals.totalToday += amount;\n  }\n}\n```\n\n**Explanation:**\n- **getFullYear():** Checks if both dates are in the same year.\n- **getMonth():** Checks if they are in the same month (0–11).\n- **getDate():** Checks if they fall on the same day of the month.\n\n**Logic:**\n- If the expense is in the same year → add to yearly total.\n- If it’s also in the same month → add to monthly total.\n- If it’s also the same day → add to today’s total.\n\nThis layered check ensures each level (year → month → day) only runs when the previous one matches, keeping the code efficient and readable.",
    "type": "javascript"
  },
  {
    "question": "What are the Rules of Hooks in React?",
    "answer": "React Hooks must follow strict rules so React can track them correctly.\n\n1️⃣ **Hooks must run in the same order every render**\nReact tracks hooks by their position, not their name. If you change the order, the component breaks.\nExample order React expects:\n- Hook #1 → useState\n- Hook #2 → useEffect\n- Hook #3 → useMemo\n\n2️⃣ **Never call hooks inside conditions**\n❌ Bad:\n```javascript\nif (isLoggedIn) {\n  useState(0); // skipped sometimes → breaks\n}\n```\nHooks get skipped when the condition is false → React loses track.\n\n3️⃣ **Never call hooks inside loops or nested functions**\n❌ Bad:\n```javascript\nfor (...) {\n  useEffect(() => {});\n}\n```\n❌ Bad:\n```javascript\nfunction doSomething() {\n  useState(5);\n}\n```\nHooks ONLY work inside:\n- React component functions\n- Custom hooks\n\n4️⃣ **Hooks must run before any early return**\n✔ Safe:\n```javascript\nconst [a, setA] = useState(0);\nconst [b, setB] = useState(1);\nif (!ready) return null;\n```\n❌ Not safe:\n```javascript\nif (!ready) return null;\nuseState(0); // sometimes skipped\n```\n\n5️⃣ **You can write hooks in any order — just keep them consistent**\nThis is valid:\n```javascript\nuseEffect(...);\nuseState(...);\nuseMemo(...);\nuseCallback(...);\n```\nReact doesn't care about your writing order — it ONLY cares that the order never changes between renders.\n\n✔ Good Example:\n```javascript\nfunction Profile({ user }) {\n  const [count, setCount] = useState(0);  // hook #1\n  const [theme, setTheme] = useState('light'); // hook #2\n\n  useEffect(() => {\n    console.log('User updated');\n  }, [user]); // hook #3\n\n  return <div>{user.name}</div>;\n}\n```\n\n❌ Bad Example:\n```javascript\nfunction Profile({ user }) {\n  if (!user) return null; // ❌ early return before hooks\n\n  const [count, setCount] = useState(0);\n\n  if (count > 5) {\n    useEffect(() => {}, []); // ❌ hook inside condition\n  }\n\n  return <div>Hi</div>;\n}\n```\n\n⭐ Summary:\n- Hooks must run in the SAME order on every render\n- Put hooks at the top of your component\n- No hooks inside conditions, loops, or nested functions\n- No hooks after early returns",
    "type": "react"
  },
  {
    "question": "What are custom hooks in React?",
    "answer": "A custom hook is a reusable function that uses React's built-in hooks to share logic between components.\n\n🔹 **Starts with `use`** (example: useCounter, useFetch)\n🔹 **Can use any hook** (useState, useEffect, useMemo, etc.)\n🔹 **Does NOT return JSX** — it only returns data or functions\n🔹 **Called inside components just like built-in hooks**\n\nExample — simple custom hook:\n```javascript\nimport { useState } from 'react';\n\nexport function useCounter(initial = 0) {\n  const [count, setCount] = useState(initial);\n  return [count, setCount];\n}\n```\n\nUsing it in a component:\n```javascript\nimport React from 'react';\nimport { useCounter } from './useCounter';\n\nfunction Counter() {\n  const [count, setCount] = useCounter(0);\n\n  return (\n    <button onClick={() => setCount(count + 1)}>\n      Count: {count}\n    </button>\n  );\n}\n```\n\n⭐ Why use custom hooks?\n- Reuse logic across multiple components\n- Keep components clean\n- Extract messy useEffect/useState code into its own file",
    "type": "react"
  },

  {
    "question": "What does it mean for a React component to mount and unmount?",
    "answer": "In React, **mounting** is when a component appears on the screen for the first time. React creates it, renders it, and inserts it into the DOM. For class components, this is when `componentDidMount` runs; for function components, this is when a `useEffect(() => {}, [])` runs.\n\n**Updating** happens whenever state or props change.\n\n**Unmounting** is when React removes the component from the DOM. Cleanup functions from `useEffect` run during unmount.\n\nSimple analogy:\n- **Mount:** Component enters the room.\n- **Update:** Component changes something while staying in the room.\n- **Unmount:** Component leaves the room.",
    "type": "react"
  },
  {
    "question": "What is lazy initialization in React's useState?",
    "answer": "Lazy initialization means you give `useState` a function, and React calls that function **only once** during the initial render.\n\nExample:\n```js\nconst [value] = useState(() => expensiveWork());\n```\nReact runs `expensiveWork()` only the first time the component mounts — it does **not** run on every render. This is perfect for expensive calculations, loading from `localStorage`, generating large default values, or anything costly.\n\nIf you don't wrap the value in a function, the work runs every render, which is wasteful.",
    "type": "react"
  },
  {
    "question": "Why must a React component return exactly one root element, and how does this rule apply when using array methods like map()?",
    "answer": "Every React component must return **one single parent element**. The same rule applies to any function that returns JSX — especially `.map()`.\n\n`.map()` must return one JSX root per iteration. Multiple siblings must be wrapped in a `<div>`, `<section>`, or `<>...</>` fragment.\n\nExample (wrong):\n```jsx\nitems.map(i => (\n  <p>{i.name}</p>\n  <span>{i.age}</span>\n));\n```\nExample (correct):\n```jsx\nitems.map(i => (\n  <>\n    <p>{i.name}</p>\n    <span>{i.age}</span>\n  </>\n));\n```\nOther array methods:\n- `.filter()` doesn't return JSX → React doesn't care.\n- `.reduce()` is fine as long as each JSX return has one root.\n- `.forEach()` returns nothing → no rules.\n\n**Unbreakable rule:** Any function that returns JSX must return one parent element.",
    "type": "react"
  },

  {
    "question": "What does Object.entries() do in JavaScript, and how does it work with destructuring?",
    "answer": "`Object.entries()` converts an object into an array of `[key, value]` pairs so you can loop through it. This is useful because objects can't be mapped directly.\n\nExample:\n```js\nconst obj = { a: 1, b: 2 };\nObject.entries(obj);\n// => [['a', 1], ['b', 2]]\n```\nYou can destructure each pair:\n```js\nObject.entries(expenses).map(([category, data]) => {\n  // category = 'Shopping'\n  // data = { expenses: [...], total: 100 }\n});\n```\nYou can also destructure *inside* the value and even rename:\n```js\nObject.entries(expenses).map(([category, { expenses: categoryExpenses, total }]) => {})\n```\nThis lets you extract both the key and the nested properties cleanly.",
    "type": "javascript"
  },
  {
    "question": "What is the full pattern of using Object.entries() with nested destructuring in React?",
    "answer": "The pattern combines three things at once:\n1. `Object.entries()` → convert object to `[key, value]` pairs.\n2. Array destructuring → `[key, value]`.\n3. Object destructuring inside the value → with renaming.\n\nExample:\n```js\nObject.entries(expensesByCategory).map(([category, { expenses: categoryExpenses, total }]) => {\n  return <div>{category}: ${total}</div>;\n});\n```\nThis gives you:\n- `category` → the object's key\n- `categoryExpenses` → renamed from `expenses`\n- `total` → extracted from the value\n\nIt’s clean, readable, and perfect for rendering category-based UI.",
    "type": "javascript"
  },
  {
    "question": "What are cascading renders in React, and why do they happen?",
    "answer": "Cascading renders happen when your component keeps re-rendering because of a loop created by `setState`. It usually happens when state updates are triggered inside `useEffect` in a way that causes the effect to re-run.\n\nExample of infinite loop:\n```\nrender → useEffect runs → setState → render → useEffect runs → setState → ...\n```\nThat’s cascading renders.\n\nCorrect way:\nDefine your async function *inside* the effect:\n```js\nuseEffect(() => {\n  const fetchData = async () => {\n    const res = await fetch(url);\n    setData(await res.json());\n  };\n\n  fetchData();\n}, []);\n```\nThis prevents React from thinking the function is a dependency and avoids the loop.",
    "type": "react"
  },
  {
    "question": "How do you use async/await in React to fetch data safely?",
    "answer": "The basic pattern is:\n\n**1. Setup state:**\n```js\nconst [data, setData] = useState(null);\nconst [loading, setLoading] = useState(true);\n```\n\n**2. Create async function:**\n```js\nconst fetchData = async () => {\n  try {\n    setLoading(true);\n    const res = await fetch('API_URL');\n    const json = await res.json();\n    setData(json);\n  } catch (err) {\n    console.error(err);\n  } finally {\n    setLoading(false);\n  }\n};\n```\n\n**3. Call it in useEffect (runs on mount):**\n```js\nuseEffect(() => {\n  fetchData();\n}, []);\n```\n\n**4. Optional: reuse on button click:**\n```jsx\n<button onClick={fetchData}>Reload</button>\n```\n\n**5. Render safely:**\n```jsx\n{loading ? <p>Loading...</p> : <p>{data?.name}</p>}\n```\n\n**Tips:**\n- Always use try/catch.\n- Use `finally` to stop loading.\n- Use `useCallback` if passing fetch functions around.",
    "type": "react"
  },
  {
    "question": "How do you work with dates in JavaScript using new Date() and Date.now()?",
    "answer": "JavaScript provides two main ways to work with dates and timestamps:\n\n1️⃣ **new Date()**\n- Creates a Date object representing a specific point in time.\n- Can be called with no arguments (current date/time), a date string, or year/month/day parameters.\n\n**Examples:**\n```javascript\n// Current date and time\nconst now = new Date();\nconsole.log(now); // Thu Nov 13 2025 14:41:21 GMT+0100 (WAT)\n\n// Specific date (ISO format YYYY-MM-DD)\nconst birthday = new Date('2025-11-03');\nconsole.log(birthday); // Mon Nov 03 2025 00:00:00 GMT+0100 (WAT)\n\n// Including time\nconst meeting = new Date('2025-11-03T14:30:00');\nconsole.log(meeting); // Mon Nov 03 2025 14:30:00 GMT+0100 (WAT)\n\n// Extract month, day, year\nconst month = now.getMonth() + 1; // Months are 0-indexed\nconst day = now.getDate();\nconst year = now.getFullYear();\nconsole.log(`${month}/${day}/${year}`); // e.g. 11/13/2025\n```\n💡 Tip: Use ISO strings for cross-browser consistency. `getUTC*` methods give UTC times.\n\n2️⃣ **Date.now()**\n- Returns the number of milliseconds since January 1, 1970 (Unix epoch).\n- Useful for timestamps, measuring durations, or generating unique IDs.\n\n**Examples:**\n```javascript\nconst timestamp = Date.now();\nconsole.log(timestamp); // e.g. 1731474081650\n\n// Measure elapsed time\nconst start = Date.now();\ndoHeavyWork();\nconst end = Date.now();\nconsole.log(`Elapsed: ${end - start} ms`);\n```\n💡 Note: Date.now() only gives a numeric value, not a readable date. Wrap it with `new Date(Date.now())` if you need a readable format.\n\n**Summary:**\n- `new Date()` → readable date object, can extract day/month/year.\n- `Date.now()` → timestamp in milliseconds, fast for calculations.\n- Combine both for practical use: `new Date(Date.now())` gives the current date/time as a Date object.",
    "type": "javascript"
  },
  {
    "question": "How do you assign IDs and classes to HTML elements and style them in CSS?",
    "answer": "**1️⃣ Using IDs**\n- IDs are unique identifiers for HTML elements.\n- Use `id` in HTML and `#id` in CSS.\n\n**HTML Example:**\n```html\n<p id=\"p1\">This paragraph has ID p1</p>\n<h1 id=\"main-title\">Main Title</h1>\n<h2 id=\"subtitle\">Subtitle</h2>\n```\n\n**CSS Example:**\n```css\n#p1 {\n    color: red;\n    font-size: 18px;\n}\n\n#main-title {\n    color: blue;\n    text-align: center;\n}\n\n#subtitle {\n    color: green;\n    font-style: italic;\n}\n```\n💡 Tip: Each ID should be unique on the page.\n\n---\n\n**2️⃣ Using Classes**\n- Classes can be shared by multiple elements.\n- Use `class` in HTML and `.className` in CSS.\n\n**HTML Example:**\n```html\n<p class=\"highlight\">Highlighted paragraph</p>\n<div class=\"box\">Box content</div>\n<h1 class=\"title\">Title with class</h1>\n```\n\n**CSS Example:**\n```css\n.highlight {\n    background-color: yellow;\n    font-weight: bold;\n}\n\n.box {\n    border: 2px solid black;\n    padding: 20px;\n    margin: 10px;\n}\n\n.title {\n    color: red;\n    text-align: center;\n}\n```\n💡 Tip: Use classes for reusable styles and IDs for unique elements.\n\n**✅ Summary:**\n- **IDs (`id`)** → unique element, styled with `#id`.\n- **Classes (`class`)** → multiple elements, styled with `.class`.\n- Always combine semantic HTML with clear IDs/classes for maintainable CSS.",
    "type": "css",
    "tags": ["css", "id", "class", "selectors", "styling"]
  },
  {
    "question": "What is a JavaScript runtime?",
    "answer": "A JavaScript runtime is the environment where JavaScript code actually runs.\nJavaScript by itself is just text — the runtime gives it the power to do things.\n\nThere are two main runtimes:\n\n**Browser Runtime:**\nLets JavaScript interact with web pages (DOM, buttons, input, fetch, etc.).\n\n**Node.js Runtime:**\nLets JavaScript interact with files, servers, databases, and the computer itself.\n\nIn short:\nJavaScript runtime = the place that gives JavaScript the tools and power it needs to work.",
    "type": "javascript"
  },
  {
    "question": "What are event propagation and event delegation in JavaScript DOM?",
    "answer": "**Event Propagation & Delegation - Complete Guide**\n\n**🔄 Event Propagation (Event Flow):**\nEvent propagation describes how events travel through the DOM tree. It has three phases:\n\n**1. Capturing Phase (Trickle Down):**\n- Event travels from the **window → document → body → parent → target**\n- Moves from outermost ancestor down to the target element\n- Rarely used in practice\n\n**2. Target Phase:**\n- Event reaches the **exact element** that was clicked/triggered\n- This is where the main event handling occurs\n\n**3. Bubbling Phase (Bubble Up):**\n- Event travels back up from **target → parent → body → document → window**\n- Moves from target element up to outermost ancestor\n- **This is the default behavior**\n\n**Visual Example:**\n```html\n<div class=\"grandparent\">\n  <div class=\"parent\">\n    <button class=\"child\">Click Me</button>\n  </div>\n</div>\n\n<script>\n// Event propagation demonstration\ndocument.querySelector('.grandparent').addEventListener('click', () => {\n  console.log('Grandparent clicked (bubbling)');\n});\n\ndocument.querySelector('.parent').addEventListener('click', () => {\n  console.log('Parent clicked (bubbling)');\n});\n\ndocument.querySelector('.child').addEventListener('click', () => {\n  console.log('Child clicked (target)');\n});\n\n// When button is clicked, console shows:\n// \"Child clicked (target)\"\n// \"Parent clicked (bubbling)\"  \n// \"Grandparent clicked (bubbling)\"\n</script>\n```\n\n**🎯 Event Delegation (Smart Event Handling):**\nEvent delegation uses event bubbling to handle events efficiently by attaching a single listener to a parent element.\n\n**Traditional Approach (Inefficient):**\n```javascript\n// ❌ Multiple listeners for similar elements\ndocument.querySelector('.item1').addEventListener('click', handleClick);\ndocument.querySelector('.item2').addEventListener('click', handleClick);\ndocument.querySelector('.item3').addEventListener('click', handleClick);\n// ... and so on for each item\n```\n\n**Delegation Approach (Efficient):**\n```javascript\n// ✅ Single listener on parent\ndocument.querySelector('.item-list').addEventListener('click', function(e) {\n  // Check if the clicked element is an item\n  if (e.target.classList.contains('item')) {\n    console.log(`Item ${e.target.dataset.id} clicked!`);\n    e.target.style.backgroundColor = 'yellow';\n  }\n});\n```\n\n**Practical Example - Dynamic List:**\n```html\n<ul class=\"shopping-list\">\n  <li class=\"item\" data-id=\"1\">Milk</li>\n  <li class=\"item\" data-id=\"2\">Bread</li>\n  <li class=\"item\" data-id=\"3\">Eggs</li>\n</ul>\n\n<button onclick=\"addNewItem()\">Add Butter</button>\n\n<script>\n// Single event listener for all items (current and future)\ndocument.querySelector('.shopping-list').addEventListener('click', function(e) {\n  if (e.target.classList.contains('item')) {\n    console.log(`You clicked: ${e.target.textContent} (ID: ${e.target.dataset.id})`);\n    e.target.classList.toggle('completed');\n  }\n});\n\n// New items automatically work with existing listener\nfunction addNewItem() {\n  const list = document.querySelector('.shopping-list');\n  const newItem = document.createElement('li');\n  newItem.className = 'item';\n  newItem.dataset.id = Date.now();\n  newItem.textContent = 'Butter';\n  list.appendChild(newItem);\n}\n</script>\n```\n\n**🎯 Key Benefits of Event Delegation:**\n- **Performance**: Single listener instead of many\n- **Dynamic Elements**: Works with elements added later\n- **Memory Efficient**: Less memory usage\n- **Cleaner Code**: Centralized event handling\n\n**🛠️ Event Object Properties for Delegation:**\n```javascript\nelement.addEventListener('click', function(e) {\n  console.log(e.target);        // The actual clicked element\n  console.log(e.currentTarget); // The element with the listener\n  console.log(e.eventPhase);    // 1=Capturing, 2=Target, 3=Bubbling\n  e.stopPropagation();         // Stop event from propagating further\n  e.preventDefault();          // Prevent default browser behavior\n});\n```\n\n**💡 When to Use Event Delegation:**\n- Lists with many similar items\n- Dynamic content that changes frequently\n- Tables with interactive cells\n- Navigation menus\n- Any group of related elements\n\n**Summary:** Event propagation explains **how** events travel, while event delegation is a **technique** that leverages bubbling for efficient event handling.",
    "type": "javascript",
    "tags": [
      "javascript",
      "dom",
      "events",
      "propagation",
      "delegation",
      "bubbling",
      "capturing",
      "performance"
    ]
  },
  {
    "question": "What are the different ways to add event listeners in JavaScript?",
    "answer": "**Event Listeners - Complete Implementation Guide**\n\n**🎯 Three Methods to Add Event Listeners:**\n\n**1. Inline HTML Event Handlers (Simple but Limited):**\n```html\n<!-- Directly in HTML -->\n<button onclick=\"alert('Button clicked!')\">Click Me</button>\n\n<!-- With function call -->\n<button onclick=\"handleClick()\">Call Function</button>\n\n<!-- With parameters -->\n<button onclick=\"greetUser('John')\">Greet John</button>\n\n<!-- Access event object -->\n<button onclick=\"console.log(event)\">Log Event</button>\n```\n\n**Pros & Cons:**\n- ✅ **Simple**: Easy for quick prototypes\n- ✅ **Direct**: No JavaScript setup required\n- ❌ **Mixes concerns**: HTML + JavaScript together\n- ❌ **Hard to maintain**: Difficult to update multiple elements\n- ❌ **Limited**: Can only attach one listener per event\n- ❌ **Global scope**: Functions must be globally available\n\n**2. DOM Element Properties (Traditional):**\n```javascript\nconst button = document.querySelector('.my-button');\n\n// Assign function directly to element property\nbutton.onclick = function() {\n  alert('Button clicked via property!');\n};\n\n// Or use named function\nfunction handleButtonClick() {\n  console.log('Button was clicked');\n}\nbutton.onclick = handleButtonClick;\n```\n\n**Pros & Cons:**\n- ✅ **Clean separation**: HTML and JavaScript separate\n- ✅ **Simple syntax**: Easy to understand\n- ❌ **Single handler**: Overwrites previous onclick handlers\n- ❌ **Limited control**: No options for capture phase, once, etc.\n\n**3. addEventListener() Method (Modern & Recommended):**\n```javascript\nconst button = document.querySelector('.my-button');\n\n// Basic usage\nbutton.addEventListener('click', function() {\n  alert('Button clicked!');\n});\n\n// With named function (RECOMMENDED)\nfunction handleButtonClick() {\n  console.log('Button clicked at:', new Date());\n}\nbutton.addEventListener('click', handleButtonClick); // Note: No parentheses!\n\n// With arrow function\nbutton.addEventListener('click', () => {\n  console.log('Arrow function handler');\n});\n\n// Multiple events on same element\nbutton.addEventListener('mouseover', handleHover);\nbutton.addEventListener('mouseout', handleMouseOut);\nbutton.addEventListener('click', handleClick);\n```\n\n**🚨 CRITICAL: Don't Call the Function!**\n```javascript\n// ❌ WRONG - executes immediately\nbutton.addEventListener('click', handleClick());\n\n// ✅ CORRECT - passes function reference\nbutton.addEventListener('click', handleClick);\n```\n\n**🛠️ Advanced addEventListener Features:**\n\n**Event Object Access:**\n```javascript\nbutton.addEventListener('click', function(event) {\n  console.log('Event type:', event.type);        // 'click'\n  console.log('Target element:', event.target);  // The button\n  console.log('Current target:', event.currentTarget); // The button\n  console.log('Coordinates:', event.clientX, event.clientY);\n});\n```\n\n**Event Options:**\n```javascript\n// Capture phase (instead of bubbling)\nbutton.addEventListener('click', handleClick, { capture: true });\n\n// Run once then automatically remove\nbutton.addEventListener('click', handleClick, { once: true });\n\n// Passive listener (better scrolling performance)\nbutton.addEventListener('touchstart', handleTouch, { passive: true });\n\n// Multiple options\nbutton.addEventListener('click', handleClick, { \n  capture: false, \n  once: true, \n  passive: true \n});\n```\n\n**Removing Event Listeners:**\n```javascript\n// Must use named function to remove\nfunction handleSpecificClick() {\n  console.log('This will only work once');\n}\n\nbutton.addEventListener('click', handleSpecificClick);\n\n// Later, remove the listener\nbutton.removeEventListener('click', handleSpecificClick);\n```\n\n**🎯 Practical Comparison:**\n\n**Scenario: Multiple buttons with different approaches**\n```html\n<button class=\"inline-btn\" onclick=\"alert('Inline handler')\">Inline</button>\n<button class=\"property-btn\">Property</button>\n<button class=\"listener-btn\">addEventListener</button>\n\n<script>\n// DOM Property approach\nconst propertyBtn = document.querySelector('.property-btn');\npropertyBtn.onclick = function() {\n  alert('DOM property handler');\n};\n\n// addEventListener approach (BEST PRACTICE)\nconst listenerBtn = document.querySelector('.listener-btn');\nlistenerBtn.addEventListener('click', function() {\n  alert('addEventListener - most flexible');\n});\n\n// Can add multiple listeners\nlistenerBtn.addEventListener('click', function() {\n  console.log('Second listener also fires');\n});\n</script>\n```\n\n**📋 Recommendation Guide:**\n- **Use addEventListener()** for production code\n- **Use DOM properties** for simple scripts\n- **Avoid inline handlers** except for quick prototypes\n- **Always use named functions** when you need to remove listeners\n\n**Key Benefits of addEventListener():**\n- ✅ Multiple listeners per event\n- ✅ Fine control over event phase (capture/bubble)\n- ✅ Advanced options (once, passive)\n- ✅ Easy to remove specific listeners\n- ✅ Better performance with passive events\n- ✅ Industry standard practice",
    "type": "javascript",
    "tags": [
      "javascript",
      "dom",
      "events",
      "event-listeners",
      "addeventlistener",
      "onclick",
      "inline-events",
      "best-practices"
    ]
  },
  {
    "question": "What is the window object in JavaScript and what can you do with it?",
    "answer": "**The Window Object - Browser Global Environment**\n\n**🌐 What is the Window Object?**\nThe `window` object is the **global object** in browser-based JavaScript. It represents the entire browser window/tab and serves as the root of the DOM hierarchy.\n\n**🔍 Accessing the Window Object:**\n```javascript\n// Explicit access\nconsole.log(window.innerWidth); // Viewport width\n\n// Implicit access (window is global)\nconsole.log(innerWidth); // Same as window.innerWidth\n\n// Global variables become window properties\nvar globalVar = 'Hello';\nconsole.log(window.globalVar); // 'Hello'\n\n// But let/const don't become window properties\nlet localVar = 'Hi';\nconsole.log(window.localVar); // undefined\n```\n\n**🛠️ Key Window Properties & Methods:**\n\n**Browser Information:**\n```javascript\n// Viewport dimensions\nconsole.log('Viewport width:', window.innerWidth);\nconsole.log('Viewport height:', window.innerHeight);\nconsole.log('Screen width:', window.screen.width);\nconsole.log('Screen height:', window.screen.height);\n\n// Browser details\nconsole.log('User Agent:', window.navigator.userAgent);\nconsole.log('Platform:', window.navigator.platform);\nconsole.log('Language:', window.navigator.language);\n\n// URL information\nconsole.log('Current URL:', window.location.href);\nconsole.log('Hostname:', window.location.hostname);\nconsole.log('Path:', window.location.pathname);\n```\n\n**Navigation & History:**\n```javascript\n// Page navigation\nwindow.location.href = 'https://google.com'; // Navigate to URL\nwindow.location.reload(); // Reload current page\nwindow.location.assign('/new-page.html'); // Navigate to new page\n\n// Browser history\nwindow.history.back(); // Go back one page\nwindow.history.forward(); // Go forward one page\nwindow.history.go(-2); // Go back two pages\n\n// Opening new windows\nconst newWindow = window.open('https://example.com', '_blank');\nnewWindow.close(); // Close the new window\n```\n\n**Timers & Scheduling:**\n```javascript\n// Execute once after delay\nconst timeoutId = window.setTimeout(() => {\n  console.log('This runs after 2 seconds');\n}, 2000);\n\n// Execute repeatedly\nconst intervalId = window.setInterval(() => {\n  console.log('This runs every second');\n}, 1000);\n\n// Clear timers\nclearTimeout(timeoutId);\nclearInterval(intervalId);\n```\n\n**Storage & Data:**\n```javascript\n// Local Storage (persistent)\nwindow.localStorage.setItem('username', 'john_doe');\nconst user = window.localStorage.getItem('username');\nwindow.localStorage.removeItem('username');\n\n// Session Storage (tab-specific)\nwindow.sessionStorage.setItem('tempData', 'some value');\n\n// Cookies\ndocument.cookie = 'user=john; expires=Fri, 31 Dec 2023 23:59:59 GMT; path=/';\n```\n\n**Dialog Boxes:**\n```javascript\n// Simple dialogs (blocking)\nwindow.alert('This is an alert!');\n\nconst shouldDelete = window.confirm('Are you sure?');\nif (shouldDelete) {\n  console.log('User confirmed');\n}\n\nconst userName = window.prompt('Enter your name:', 'John Doe');\nconsole.log('User entered:', userName);\n```\n\n**🎯 Practical Examples:**\n\n**Responsive Layout Detection:**\n```javascript\n// Check if mobile device\nfunction isMobile() {\n  return window.innerWidth <= 768;\n}\n\n// Update layout on resize\nwindow.addEventListener('resize', function() {\n  if (isMobile()) {\n    document.body.classList.add('mobile-layout');\n  } else {\n    document.body.classList.remove('mobile-layout');\n  }\n});\n```\n\n**Page Visibility Detection:**\n```javascript\n// Detect when user switches tabs\nwindow.addEventListener('visibilitychange', function() {\n  if (document.hidden) {\n    console.log('User switched to another tab');\n    // Pause videos, stop animations, etc.\n  } else {\n    console.log('User returned to this tab');\n    // Resume activities\n  }\n});\n```\n\n**Scroll Position Tracking:**\n```javascript\nwindow.addEventListener('scroll', function() {\n  const scrollY = window.scrollY;\n  const pageHeight = document.documentElement.scrollHeight;\n  const viewportHeight = window.innerHeight;\n  \n  const scrollPercentage = (scrollY / (pageHeight - viewportHeight)) * 100;\n  console.log(`Scrolled ${scrollPercentage.toFixed(1)}% down the page`);\n});\n```\n\n**🔧 Global Function Context:**\n```javascript\n// Global functions are window methods\nfunction sayHello() {\n  console.log('Hello!');\n}\n\n// These are equivalent\nsayHello();\nwindow.sayHello();\n\n// Global scope\nconsole.log(this === window); // true in global context\n```\n\n**⚠️ Important Notes:**\n- `window` is only available in **browser environments**\n- In Node.js, the global object is `global`, not `window`\n- `var` declarations become window properties, but `let`/`const` do not\n- Be careful with global pollution - avoid adding too many properties to window\n\n**Summary:** The window object is your gateway to browser capabilities - from managing windows and navigation to handling storage, timers, and user interactions.",
    "type": "javascript",
    "tags": [
      "javascript",
      "browser",
      "window-object",
      "global-object",
      "dom",
      "bom",
      "web-api",
      "browser-api"
    ]
  },
  {
    "question": "What Is the Event Loop in Node.js?",
    "answer": "The event loop is the system inside Node.js that handles tasks one at a time while keeping everything running smoothly.\nNode.js does slow tasks (like reading files or fetching data) in the background, and the event loop checks when those tasks are done and runs their results.\n\nIn short:\nThe event loop keeps Node.js from getting stuck by managing tasks and running them when they're ready.",
    "type": "nodejs"
  },
  {
    "question": "What Is a REPL?",
    "answer": "REPL is a tool that lets you interact with JavaScript (or other languages) one command at a time.\n\nREPL stands for:\n- **Read** – reads what you type\n- **Evaluate** – figures out the result\n- **Print** – shows the result\n- **Loop** – repeats for the next command\n\nExample:\nGo to cmd and type `node` then enter, and you can run code, console.log, and use functions.\n\n**Use:**\nIt's like a code playground where you can test small pieces of code instantly without making a full file.",
    "type": "nodejs"
  },
  {
    "question": "How do you create a new folder from cmd?",
    "answer": "```bash\nmkdir filename\ncd filename\n```",
    "type": "command-line"
  },
  {
    "question": "How do you setup a new Node.js project?",
    "answer": "First run:\n```bash\nnpm init\n```\n\nThere will be questions asked:\n- package name\n- version\n- description\n- entry point\n- test command\n- git repository\n- keywords\n- author\n- license\n\nUse `npm install <pkg>` afterwards to install a package and save it as a dependency in the package.json file.\n\nExample package.json output:\n```json\n{\n  \"name\": \"nodejs\",\n  \"version\": \"1.0.0\",\n  \"description\": \"nodejs\",\n  \"main\": \"index.js\",\n  \"scripts\": {\n    \"test\": \"echo \\\"Error: no test specified\\\" && exit 1\"\n  },\n  \"author\": \"userN9ne\",\n  \"license\": \"MIT\"\n}\n```\n\nIf you want to skip all questions, run:\n```bash\nnpm init -y\n```",
    "type": "nodejs"
  },
  {
    "question": "How do you store and retrieve data from localStorage?",
    "answer": "**localStorage - Browser Data Storage Guide**\n\n**🎯 What is localStorage?**\n`localStorage` is a web browser API that allows you to store data persistently in the user's browser. Data survives page refreshes, browser restarts, and even system reboots.\n\n**💾 Basic Storage Operations:**\n\n**Storing Data:**\n```javascript\n// Store simple string\nlocalStorage.setItem('username', 'Chuks');\n\n// Store number (converted to string)\nlocalStorage.setItem('userAge', '25');\n\n// Store boolean\nlocalStorage.setItem('isLoggedIn', 'true');\n```\n\n**Retrieving Data:**\n```javascript\n// Get string value\nconst username = localStorage.getItem('username');\nconsole.log(username); // \"Chuks\"\n\n// Get number (returns string, need conversion)\nconst age = localStorage.getItem('userAge');\nconsole.log(age); // \"25\" (string)\nconsole.log(Number(age)); // 25 (number)\n\n// Get boolean\nconst isLoggedIn = localStorage.getItem('isLoggedIn') === 'true';\nconsole.log(isLoggedIn); // true (boolean)\n```\n\n**📦 Storing Objects and Arrays:**\n\n**Storing Complex Data:**\n```javascript\nconst user = {\n  name: \"Chuks\",\n  age: 21,\n  preferences: {\n    theme: \"dark\",\n    language: \"en\"\n  }\n};\n\nconst shoppingCart = [\"apple\", \"banana\", \"milk\"];\n\n// Convert to JSON string before storing\nlocalStorage.setItem('user', JSON.stringify(user));\nlocalStorage.setItem('cart', JSON.stringify(shoppingCart));\n```\n\n**Retrieving Complex Data:**\n```javascript\n// Get and parse objects\nconst storedUser = JSON.parse(localStorage.getItem('user'));\nconsole.log(storedUser.name); // \"Chuks\"\nconsole.log(storedUser.preferences.theme); // \"dark\"\n\n// Get and parse arrays\nconst storedCart = JSON.parse(localStorage.getItem('cart'));\nconsole.log(storedCart[0]); // \"apple\"\n```\n\n**🔍 Inspecting localStorage:**\n\n**View All Stored Data:**\n```javascript\n// Get all keys\nconst keys = Object.keys(localStorage);\nconsole.log(keys); // [\"username\", \"userAge\", \"user\", \"cart\"]\n\n// Get all values\nconst values = Object.values(localStorage);\nconsole.log(values); // [\"Chuks\", \"25\", \"{\\\"name\\\":\\\"Chuks\\\"...}\", \"[\\\"apple\\\"...]\"]\n\n// Get all key-value pairs\nconst entries = Object.entries(localStorage);\nconsole.log(entries);\n// [\n//   [\"username\", \"Chuks\"],\n//   [\"userAge\", \"25\"],\n//   [\"user\", \"{\\\"name\\\":\\\"Chuks\\\"...}\"],\n//   [\"cart\", \"[\\\"apple\\\",\\\"banana\\\",\\\"milk\\\"]\"]\n// ]\n```\n\n**🎯 Practical Examples:**\n\n**User Preferences Manager:**\n```javascript\nclass UserPreferences {\n  static saveTheme(theme) {\n    localStorage.setItem('theme', theme);\n  }\n\n  static getTheme() {\n    return localStorage.getItem('theme') || 'light'; // Default to light\n  }\n\n  static saveSettings(settings) {\n    localStorage.setItem('userSettings', JSON.stringify(settings));\n  }\n\n  static getSettings() {\n    const stored = localStorage.getItem('userSettings');\n    return stored ? JSON.parse(stored) : {};\n  }\n}\n\n// Usage\nUserPreferences.saveTheme('dark');\nUserPreferences.saveSettings({\n  fontSize: 'large',\n  notifications: true,\n  language: 'en'\n});\n\n// On page load\nconst theme = UserPreferences.getTheme();\ndocument.body.classList.add(`${theme}-theme`);\n```\n\n**Shopping Cart Persistence:**\n```javascript\nclass ShoppingCart {\n  constructor() {\n    this.items = this.loadCart();\n  }\n\n  loadCart() {\n    const stored = localStorage.getItem('shoppingCart');\n    return stored ? JSON.parse(stored) : [];\n  }\n\n  addItem(product) {\n    this.items.push(product);\n    this.saveCart();\n  }\n\n  removeItem(productId) {\n    this.items = this.items.filter(item => item.id !== productId);\n    this.saveCart();\n  }\n\n  saveCart() {\n    localStorage.setItem('shoppingCart', JSON.stringify(this.items));\n  }\n\n  clearCart() {\n    this.items = [];\n    localStorage.removeItem('shoppingCart');\n  }\n\n  getItemCount() {\n    return this.items.length;\n  }\n}\n\n// Usage\nconst cart = new ShoppingCart();\ncart.addItem({ id: 1, name: 'Laptop', price: 999 });\nconsole.log(cart.getItemCount()); // 1\n\n// Cart persists even after page refresh\n```\n\n**🛠️ Advanced Operations:**\n\n**Checking if Item Exists:**\n```javascript\nfunction hasLocalStorage(key) {\n  return localStorage.getItem(key) !== null;\n}\n\nconsole.log(hasLocalStorage('username')); // true\nconsole.log(hasLocalStorage('nonexistent')); // false\n```\n\n**Removing Data:**\n```javascript\n// Remove specific item\nlocalStorage.removeItem('username');\n\n// Clear everything\nlocalStorage.clear();\n\n// Remove multiple items\n['tempData1', 'tempData2', 'cache'].forEach(key => {\n  localStorage.removeItem(key);\n});\n```\n\n**Storage Event Listening:**\n```javascript\n// Listen for storage changes (from other tabs)\nwindow.addEventListener('storage', (event) => {\n  console.log(`Key changed: ${event.key}`);\n  console.log(`Old value: ${event.oldValue}`);\n  console.log(`New value: ${event.newValue}`);\n  \n  if (event.key === 'user') {\n    // Update UI when user data changes\n    updateUserDisplay();\n  }\n});\n```\n\n**⚠️ Limitations & Best Practices:**\n\n**Storage Limits:**\n```javascript\n// Typically 5-10MB per domain\nconsole.log('Storage limit reached');\n\ntry {\n  localStorage.setItem('largeData', hugeString);\n} catch (e) {\n  if (e.name === 'QuotaExceededError') {\n    console.log('Storage is full!');\n    // Clear some space or use sessionStorage\n  }\n}\n```\n\n**Data Types & Serialization:**\n```javascript\n// ❌ Don't store these directly\nlocalStorage.setItem('date', new Date()); // Stores \"Thu Dec 07 2023...\"\nlocalStorage.setItem('function', function() {}); // Stores \"function() {}\"\n\n// ✅ Convert properly\nlocalStorage.setItem('birthday', new Date().toISOString());\nconst storedDate = new Date(localStorage.getItem('birthday'));\n```\n\n**🔒 Security Considerations:**\n\n**Sensitive Data:**\n```javascript\n// ❌ Never store sensitive information\nlocalStorage.setItem('password', 'user123'); // UNSAFE!\nlocalStorage.setItem('token', 'jwt-token-here'); // RISKY!\n\n// ✅ Use HTTP-only cookies for sensitive data\n// localStorage is accessible by any JavaScript on your domain\n```\n\n**💡 Pro Tips:**\n1. **Always use try-catch** for storage operations\n2. **Set reasonable expiration** for stored data\n3. **Compress large data** before storing\n4. **Use prefixes** for better organization (`user_theme`, `app_settings`)\n5. **Consider sessionStorage** for temporary data\n6. **Validate retrieved data** before using it\n\n**🆚 localStorage vs sessionStorage:**\n```javascript\n// localStorage - persists forever\nlocalStorage.setItem('permanent', 'I stay forever');\n\n// sessionStorage - clears when tab closes\nsessionStorage.setItem('temporary', 'I disappear when tab closes');\n```\n\n**Summary:** localStorage is perfect for user preferences, cached data, and offline functionality, but avoid storing sensitive information and be mindful of storage limits.",
    "type": "javascript",
    "tags": [
      "javascript",
      "localstorage",
      "web-storage",
      "browser-api",
      "persistence",
      "data-storage",
      "json"
    ]
  },
  {
    "question": "What are Object.values(), Object.keys(), and Object.entries() methods?",
    "answer": "**Object Iteration Methods - Complete Guide**\n\n**🎯 Object Iteration Methods:**\nThese methods provide different ways to extract data from JavaScript objects as arrays, making it easier to work with object data.\n\n**👤 Sample Object:**\n```javascript\nconst user = {\n  name: \"Chuks\",\n  age: 21,\n  country: \"Nigeria\",\n  email: \"chuks@example.com\",\n  isActive: true\n};\n```\n\n**1. Object.values() - Get All Values:**\n```javascript\n// Returns array of property values\nconst values = Object.values(user);\nconsole.log(values);\n// [\"Chuks\", 21, \"Nigeria\", \"chuks@example.com\", true]\n```\n\n**2. Object.keys() - Get All Keys:**\n```javascript\n// Returns array of property names\nconst keys = Object.keys(user);\nconsole.log(keys);\n// [\"name\", \"age\", \"country\", \"email\", \"isActive\"]\n```\n\n**3. Object.entries() - Get Key-Value Pairs:**\n```javascript\n// Returns array of [key, value] pairs\nconst entries = Object.entries(user);\nconsole.log(entries);\n// [\n//   [\"name\", \"Chuks\"],\n//   [\"age\", 21],\n//   [\"country\", \"Nigeria\"],\n//   [\"email\", \"chuks@example.com\"],\n//   [\"isActive\", true]\n// ]\n```\n\n**🎯 Practical Examples:**\n\n**Looping Through Objects:**\n```javascript\n// Traditional for...in loop\nfor (let key in user) {\n  console.log(`${key}: ${user[key]}`);\n}\n\n// Modern Object.entries() with for...of\nfor (let [key, value] of Object.entries(user)) {\n  console.log(`${key}: ${value}`);\n}\n\n// Using forEach with Object.entries\nObject.entries(user).forEach(([key, value]) => {\n  console.log(`${key}: ${value}`);\n});\n```\n\n**Data Transformation:**\n```javascript\n// Convert object to array of formatted strings\nconst userInfo = Object.entries(user).map(([key, value]) => {\n  return `${key}: ${value}`;\n});\nconsole.log(userInfo);\n// [\"name: Chuks\", \"age: 21\", \"country: Nigeria\", ...]\n\n// Get all string values\nconst stringValues = Object.values(user).filter(value => \n  typeof value === 'string'\n);\nconsole.log(stringValues); // [\"Chuks\", \"Nigeria\", \"chuks@example.com\"]\n```\n\n**Object Validation:**\n```javascript\nfunction validateUser(user) {\n  const required = ['name', 'email', 'age'];\n  const missing = required.filter(field => !Object.keys(user).includes(field));\n  \n  if (missing.length > 0) {\n    return `Missing fields: ${missing.join(', ')}`;\n  }\n  \n  return 'User is valid';\n}\n\nconsole.log(validateUser(user)); // \"User is valid\"\n```\n\n**🔄 Converting Back to Objects:**\n```javascript\n// From entries back to object\nconst entries = Object.entries(user);\nconst reconstructed = Object.fromEntries(entries);\nconsole.log(reconstructed); // Same as original user object\n\n// Transform and convert back\nconst updatedEntries = Object.entries(user).map(([key, value]) => {\n  if (key === 'age') return [key, value + 1]; // Increment age\n  if (key === 'name') return [key, value.toUpperCase()]; // Uppercase name\n  return [key, value];\n});\n\nconst updatedUser = Object.fromEntries(updatedEntries);\nconsole.log(updatedUser);\n// { name: \"CHUKS\", age: 22, country: \"Nigeria\", ... }\n```\n\n**📊 Working with Complex Data:**\n\n**Nested Objects:**\n```javascript\nconst company = {\n  name: \"Tech Corp\",\n  employees: 50,\n  departments: {\n    engineering: 20,\n    sales: 15,\n    marketing: 10,\n    hr: 5\n  },\n  locations: [\"Lagos\", \"Abuja\", \"Port Harcourt\"]\n};\n\n// Get top-level values\nconsole.log(Object.values(company));\n// [\"Tech Corp\", 50, {engineering: 20, ...}, [\"Lagos\", ...]]\n\n// Get department counts\nconst deptCounts = Object.values(company.departments);\nconsole.log(deptCounts); // [20, 15, 10, 5]\n\n// Sum all department employees\nconst totalDeptEmployees = Object.values(company.departments)\n  .reduce((sum, count) => sum + count, 0);\nconsole.log(totalDeptEmployees); // 50\n```\n\n**🎯 Real-World Use Cases:**\n\n**Form Data Processing:**\n```javascript\nfunction processFormData(formData) {\n  // Convert form data to key-value pairs\n  const data = Object.fromEntries(\n    Object.entries(formData)\n      .filter(([key, value]) => value !== '') // Remove empty values\n      .map(([key, value]) => [key, value.trim()]) // Trim whitespace\n  );\n  \n  return data;\n}\n\nconst formData = {\n  name: \"  Chuks  \",\n  email: \"chuks@example.com\",\n  age: \"21\",\n  notes: \"\" // Empty field\n};\n\nconst cleaned = processFormData(formData);\nconsole.log(cleaned);\n// { name: \"Chuks\", email: \"chuks@example.com\", age: \"21\" }\n```\n\n**API Response Filtering:**\n```javascript\nasync function getActiveUsers() {\n  const response = await fetch('/api/users');\n  const users = await response.json();\n  \n  // Filter active users using Object.values\n  return Object.values(users).filter(user => user.isActive);\n}\n```\n\n**Data Visualization Prep:**\n```javascript\nfunction prepareChartData(salesData) {\n  const months = Object.keys(salesData);\n  const revenue = Object.values(salesData);\n  \n  return {\n    labels: months,\n    datasets: [{\n      label: 'Monthly Revenue',\n      data: revenue\n    }]\n  };\n}\n\nconst sales = {\n  jan: 15000,\n  feb: 18000,\n  mar: 22000,\n  apr: 19000\n};\n\nconst chartData = prepareChartData(sales);\n```\n\n**⚡ Performance Considerations:**\n\n```javascript\n// Object.keys() is fastest for simple iteration\nObject.keys(user).forEach(key => {\n  console.log(user[key]);\n});\n\n// Object.entries() is great when you need both key and value\nObject.entries(user).forEach(([key, value]) => {\n  console.log(key, value);\n});\n\n// Object.values() when you only care about values\nObject.values(user).forEach(value => {\n  console.log(value);\n});\n```\n\n**💡 Best Practices:**\n1. Use **Object.keys()** when you need property names\n2. Use **Object.values()** when you only care about values  \n3. Use **Object.entries()** when you need both key and value\n4. Combine with **array methods** (map, filter, reduce) for data transformation\n5. Use **Object.fromEntries()** to convert back to objects\n\n**Summary:** These methods bridge the gap between objects and arrays, making object data much easier to work with in functional programming patterns.",
    "type": "javascript",
    "tags": [
      "javascript",
      "objects",
      "object-methods",
      "iteration",
      "object-keys",
      "object-values",
      "object-entries",
      "data-transformation"
    ]
  },
  {
    "question": "What is encodeURIComponent and when should you use it?",
    "answer": "**encodeURIComponent() - URL Encoding Guide**\n\n**🎯 What is encodeURIComponent?**\n`encodeURIComponent()` is a JavaScript function that converts text into a URL-safe format by replacing special characters with percent-encoded equivalents (`%XX`). This prevents URLs from breaking when they contain problematic characters.\n\n**🔧 Basic Usage:**\n```javascript\nconst original = \"Spider Man: No Way Home\";\nconst encoded = encodeURIComponent(original);\nconsole.log(encoded); // \"Spider%20Man%3A%20No%20Way%20Home\"\n```\n\n**📝 Character Encoding Examples:**\n```javascript\n// Spaces become %20\nencodeURIComponent(\"hello world\");    // \"hello%20world\"\n\n// Special symbols get encoded\nencodeURIComponent(\"price=100&discount=20\"); // \"price%3D100%26discount%3D20\"\n\n// Unicode characters\nencodeURIComponent(\"café & résumé\");  // \"caf%C3%A9%20%26%20r%C3%A9sum%C3%A9\"\n\n// Problematic URL characters\nencodeURIComponent(\"path/to/file?name=test\"); // \"path%2Fto%2Ffile%3Fname%3Dtest\"\n```\n\n**🎯 Practical API Example:**\n\n**Building API URLs Safely:**\n```javascript\nasync function searchMovies(query, year) {\n  // Encode user input to prevent URL issues\n  const encodedQuery = encodeURIComponent(query);\n  const encodedYear = encodeURIComponent(year);\n  \n  // Safe URL construction\n  const url = `https://api.movies.com/search?q=${encodedQuery}&year=${encodedYear}`;\n  \n  try {\n    const response = await fetch(url);\n    const data = await response.json();\n    return data.results;\n  } catch (error) {\n    console.error('Search failed:', error);\n    return [];\n  }\n}\n\n// Usage with special characters\nsearchMovies(\"Star Wars: The Force Awakens\", 2015)\n  .then(movies => console.log(movies));\n\n// Without encoding, this would break:\n// \"Star Wars: The Force Awakens\" → colon and spaces break the URL\n// With encoding, it becomes safe:\n// \"Star%20Wars%3A%20The%20Force%20Awakens\"\n```\n\n**Form Data Submission:**\n```javascript\nasync function submitUserProfile(userData) {\n  const encodedName = encodeURIComponent(userData.name);\n  const encodedBio = encodeURIComponent(userData.bio);\n  \n  const response = await fetch('/api/profile', {\n    method: 'POST',\n    headers: {\n      'Content-Type': 'application/x-www-form-urlencoded',\n    },\n    body: `name=${encodedName}&bio=${encodedBio}`\n  });\n  \n  return response.json();\n}\n\n// User input with problematic characters\nsubmitUserProfile({\n  name: \"John O'Reilly\",\n  bio: \"Loves coding & coffee! #developer\"\n});\n```\n\n**🆚 encodeURIComponent vs encodeURI:**\n\n**encodeURIComponent (More Aggressive):**\n```javascript\n// Encodes most special characters including /, ?, =, &\nencodeURIComponent(\"api/search?q=test\"); \n// \"api%2Fsearch%3Fq%3Dtest\"\n```\n\n**encodeURI (Less Aggressive):**\n```javascript\n// Preserves URL structure characters like /, ?, =, &\nencodeURI(\"api/search?q=test\");\n// \"api/search?q=test\" (only encodes spaces if present)\n```\n\n**When to Use Each:**\n- **Use encodeURIComponent** for URL parameters, query values, form data\n- **Use encodeURI** for complete URLs that you want to keep intact\n\n**🔍 Decoding URLs:**\n```javascript\nconst encoded = \"Spider%20Man%3A%20No%20Home\";\nconst decoded = decodeURIComponent(encoded);\nconsole.log(decoded); // \"Spider Man: No Home\"\n```\n\n**🎯 Real-World Scenarios:**\n\n**Search Functionality:**\n```javascript\nfunction buildSearchURL(baseURL, filters) {\n  const params = new URLSearchParams();\n  \n  Object.entries(filters).forEach(([key, value]) => {\n    if (value !== null && value !== undefined) {\n      params.append(key, encodeURIComponent(value.toString()));\n    }\n  });\n  \n  return `${baseURL}?${params.toString()}`;\n}\n\n// Usage\nconst searchURL = buildSearchURL('https://api.example.com/search', {\n  query: \"coffee & tea\",\n  category: \"food/drink\",\n  price: \"$10-$20\",\n  location: \"New York, NY\"\n});\n\nconsole.log(searchURL);\n// \"https://api.example.com/search?query=coffee%20%26%20tea&category=food%2Fdrink&price=%2410-%2420&location=New%20York%2C%20NY\"\n```\n\n**Social Media Share Links:**\n```javascript\nfunction createShareLink(title, url) {\n  const encodedTitle = encodeURIComponent(title);\n  const encodedURL = encodeURIComponent(url);\n  \n  return `https://twitter.com/intent/tweet?text=${encodedTitle}&url=${encodedURL}`;\n}\n\nconst shareLink = createShareLink(\n  \"Check out this amazing article!\",\n  \"https://example.com/article?ref=social\"\n);\n```\n\n**⚠️ Common Mistakes:**\n\n**Double Encoding:**\n```javascript\n// ❌ Wrong - encodes twice\nconst bad = encodeURIComponent(encodeURIComponent(\"hello & world\"));\n// \"hello%2520%2526%2520world\" (broken)\n\n// ✅ Correct - encode once\nconst good = encodeURIComponent(\"hello & world\");\n// \"hello%20%26%20world\" (correct)\n```\n\n**Encoding Complete URLs:**\n```javascript\n// ❌ Wrong - breaks the URL structure\nencodeURIComponent(\"https://example.com/path?query=test\");\n\n// ✅ Correct - encode only the dynamic parts\nconst base = \"https://example.com/search\";\nconst query = encodeURIComponent(\"coffee & tea\");\nconst url = `${base}?q=${query}`;\n```\n\n**💡 Best Practices:**\n1. **Always encode** user input before putting it in URLs\n2. **Use URLSearchParams** for modern URL construction\n3. **Encode individual values**, not entire URLs\n4. **Decode on the server** if needed\n5. **Consider validation** in addition to encoding\n\n**Modern Alternative - URLSearchParams:**\n```javascript\n// Cleaner approach for URL parameters\nconst params = new URLSearchParams({\n  query: \"coffee & tea\",\n  category: \"food/drink\",\n  limit: \"10\"\n});\n\nconst url = `https://api.example.com/search?${params}`;\n// Automatically handles encoding\n```\n\n**Summary:** Use `encodeURIComponent()` whenever you're inserting dynamic data into URLs to prevent broken links and security issues.",
    "type": "javascript",
    "tags": [
      "javascript",
      "url",
      "encoding",
      "encodeuricomponent",
      "api",
      "web-development",
      "security"
    ]
  },

  {
    "question": "How do you export in CommonJS and ES Modules?",
    "answer": "**CommonJS (Node.js default):**\n\nExport a function:\n```javascript\nmodule.exports = functionName;\n```\n\nImport:\n```javascript\nconst functionName = require(\"./filepath\");\n```\n\nExport multiple functions:\n```javascript\nmodule.exports = {\n  function1,\n  function2\n};\n```\n\nImport multiple:\n```javascript\nconst { function1, function2 } = require(\"./utils\");\n```\n\n**ES Modules:**\n\nFirst add to package.json:\n```json\n{\n  \"type\": \"module\"\n}\n```\n\nExport:\n```javascript\nexport const getPosts = () => posts;\n```\n\nImport:\n```javascript\nimport { getPosts } from './postController.js';\n```\n\nAlternative export:\n```javascript\nexport { functionName };\n```\n\nExport default:\n```javascript\nexport default functionName;\n```\n\nImport default:\n```javascript\nimport functionName from './postController.js';\n```\n\nExport both default and named:\n```javascript\nexport default function1;\nexport { function2 };\n```\n\nImport both:\n```javascript\nimport function1, { function2 } from \"./postController.js\";\n```",
    "type": "nodejs"
  },
  {
    "question": "How to create a server in Node.js?",
    "answer": "```javascript\nimport http from \"http\";\n\nconst PORT = 9000;\n\nconst server = http.createServer((req, res) => {\n  res.write(\"Hello World\");\n  res.end();\n});\n\nserver.listen(PORT, () => {\n  console.log(`Server Running On Port ${PORT}`);\n});\n```\n\n**What this Node.js code does (Simple Version):**\n\n- `import http from \"http\";` - Brings in Node.js's HTTP toolkit\n- `const PORT = 9000;` - Chooses the port number your server will use\n- `http.createServer((req, res) => { ... })` - Creates a server that listens for requests\n  - `req` = the request (what the visitor asks)\n  - `res` = the response (what you answer)\n- `res.write(\"Hello World\");` - What your server sends back\n- `res.end();` - Closes the conversation\n- `server.listen(PORT, ...)` - Starts listening on the specified port\n\n**Alternative approaches:**\n\n1. Without res.write:\n```javascript\nres.end(\"Hello World\");\n```\n\n2. Set headers and HTML:\n```javascript\nconst server = http.createServer((req, res) => {\n  res.setHeader('Content-Type', 'text/html');\n  res.end(\"<h1>Hello World!</h1>\");\n});\n```\n\n3. Set status code:\n```javascript\nconst server = http.createServer((req, res) => {\n  res.setHeader('Content-Type', 'text/plain');\n  res.statusCode = 404;\n  res.end(\"<h1>Hello World!</h1>\");\n});\n```\n\n4. Set both header and status together:\n```javascript\nres.writeHead(200, { 'Content-Type': 'text/html' });\n```",
    "type": "nodejs"
  },
  {
    "question": "How do you set a shortcut to run JS files on Node.js?",
    "answer": "Add to your scripts in package.json:\n\n```json\n{\n  \"scripts\": {\n    \"start\": \"node filename.js\"\n  }\n}\n```\n\nNow you can run:\n```bash\nnpm start\n```\n\nIf you use any other words apart from 'start', you have to add 'run':\n\n```json\n{\n  \"scripts\": {\n    \"dev\": \"node server.js\"\n  }\n}\n```\n\nRun with:\n```bash\nnpm run dev\n```",
    "type": "nodejs"
  },
  {
    "question": "How do you install nodemon so you don't always have to restart your server when you make changes?",
    "answer": "**Method 1: Using Nodemon (Recommended for all Node.js versions)**\n\n**Step 1:** Install nodemon\n```bash\nnpm i -D nodemon\n```\n\nThe `-D` flag makes it a development dependency (you'll see it in devDependencies in package.json).\n\n**Note:** You can delete node_modules folder and only run `npm install` to get it back.\n\n**Step 2:** Add to package.json scripts\n```json\n{\n  \"scripts\": {\n    \"start\": \"nodemon server.js\",\n    \"dev\": \"nodemon server.js\"\n  }\n}\n```\n\nNow run:\n```bash\nnpm start\n```\nor\n```bash\nnpm run dev\n```\n\n**Method 2: Using Node.js Built-in Watch Mode (Node 18.11.0+)**\n\n**For any Node.js application:**\n```json\n{\n  \"scripts\": {\n    \"dev\": \"node --watch server.js\",           // Basic server\n    \"api\": \"node --watch api/index.js\",         // API server\n    \"script\": \"node --watch scripts/process.js\", // Any script\n    \"app\": \"node --watch app.js\"                // General app\n  }\n}\n```\n\nThen run:\n```bash\nnpm run dev\n```\n\n**What --watch works with:**\n- ✅ Express.js servers\n- ✅ HTTP servers\n- ✅ Any Node.js script\n- ✅ API applications\n- ✅ Command-line tools\n- ✅ Any .js file run with Node.js\n\n**Comparison:**\n- **Nodemon**: More features, stable, handles more file types, works with all Node versions\n- **Node --watch**: Built-in (Node 18.11.0+), no extra dependencies, good for simple projects\n\n**Key Idea:** Both methods automatically restart your Node.js application when you make code changes, saving you from manual restarts during development. The `--watch` flag works for ANY Node.js application, not just Express.",
    "type": "nodejs"
  },
  {
    "question": "How do you prevent files from being pushed to GitHub?",
    "answer": "**Step 1:** Create a .gitignore file\n\n**Step 2:** Add filenames/folders you don't want to push:\n```\nnode_modules/\n.env\n.DS_Store\n*.log\n```\n\nExample .gitignore content:\n```\n# Dependencies\nnode_modules/\n\n# Environment variables\n.env\n.env.local\n\n# OS files\n.DS_Store\nThumbs.db\n\n# Logs\n*.log\nnpm-debug.log*\n```",
    "type": "git"
  },
  {
    "question": "What are env files and how to use them?",
    "answer": "Env stands for Environment Variables.\n\n**Why use them:**\n- Recommended to add .env files to .gitignore so API keys don't leak\n- Store configuration separately from code\n\n**Sample .env file:**\n```env\nAPI_KEY=91301394134\nPORT=3432\nDATABASE_URL=mongodb://localhost:27017/mydb\nNODE_ENV=development\n```\n\n**How to use:**\n\n1. In package.json scripts, run with env file flag:\n```json\n{\n  \"scripts\": {\n    \"dev\": \"nodemon --env-file=.env server.js\"\n  }\n}\n```\n\n2. In your code, access environment variables:\n```javascript\n// server.js\nconst PORT = process.env.PORT;\nconst API_KEY = process.env.API_KEY;\n\nconsole.log(`Server running on port ${PORT}`);\n```\n\n**Note:** You may need to install and use the `dotenv` package for some setups:\n```bash\nnpm install dotenv\n```\n\nThen in your code:\n```javascript\nimport 'dotenv/config';\n// Now process.env has your .env variables\n```",
    "type": "nodejs"
  },
  {
    "question": "How do you test API with Postman?",
    "answer": "**Steps to test API with Postman:**\n\n1. Install Postman (desktop app or browser extension)\n2. Open Postman\n3. Enter your API URL, e.g.:\n   ```\n   http://localhost:8000\n   ```\n4. Select the HTTP method:\n   - GET (retrieve data)\n   - POST (send/create data)\n   - PUT (update data)\n   - DELETE (remove data)\n\n5. Click \"Send\" to make the request\n\n**Additional features:**\n- Add headers (Content-Type, Authorization)\n- Add request body (for POST/PUT requests)\n- Save requests for later use\n- Create collections of related API calls\n- Test different endpoints like:\n  - `http://localhost:8000/api/users`\n  - `http://localhost:8000/api/products`\n  - `http://localhost:8000/about`",
    "type": "tools"
  },
  {
    "question": "How to get current file path in Node.js?",
    "answer": "**In CommonJS (default Node.js):**\n```javascript\nconsole.log(__filename); // Full path to current file\nconsole.log(__dirname);  // Directory of current file\n```\n\n**In ES Modules:**\nES modules don't have __dirname by default, so you create it:\n\n```javascript\nimport { fileURLToPath } from 'url';\nimport { dirname } from 'path';\n\nconst __filename = fileURLToPath(import.meta.url);\nconst __dirname = dirname(__filename);\n\nconsole.log(__filename); // Full path to current file\nconsole.log(__dirname);  // Directory of current file\n```\n\n**Usage example:**\n```javascript\nimport path from 'path';\n\nconst filePath = path.join(__dirname, 'public', 'index.html');\n// Creates: /your/project/path/public/index.html\n```",
    "type": "nodejs"
  },
  {
    "question": "How does a Node.js server that serves HTML files work?",
    "answer": "This Node.js server reads HTML files and sends them to the browser based on the URL the user visits.\n\n**1. Importing modules:**\n```javascript\nimport http from \"http\";      // To create the server\nimport fs from \"fs/promises\"; // To read files\nimport url from \"url\";        // To get file paths\nimport path from \"path\";      // To build correct paths\n```\n\n**2. Getting current file and folder (ES modules):**\n```javascript\nconst __filename = url.fileURLToPath(import.meta.url);\nconst __dirname = path.dirname(__filename);\n```\n\n**3. Creating the server:**\n```javascript\nconst server = http.createServer(async (req, res) => {\n  if (req.method === \"GET\") {\n    let filePath;\n\n    if (req.url === \"/\") {\n      filePath = path.join(__dirname, \"public\", \"index.html\");\n    } else if (req.url === \"/about\") {\n      filePath = path.join(__dirname, \"public\", \"about.html\");\n    } else {\n      throw new Error(\"Not Found\");\n    }\n\n    const data = await fs.readFile(filePath);\n    res.setHeader(\"Content-Type\", \"text/html\");\n    res.write(data);\n    res.end();\n  } else {\n    throw new Error(\"Method Not Allowed\");\n  }\n});\n```\n\n**4. What the server does:**\n- Checks if the user is asking for `/` or `/about`\n- Reads the matching HTML file using `fs.readFile`\n- Sends the file back as a response\n- Rejects any URL that doesn't match\n- Rejects any request that isn't GET\n\n**Summary:**\nThis code creates a simple file-serving server that:\n- Uses GET requests\n- Matches URLs to specific HTML files\n- Sends those files to the browser\n- Uses __dirname to locate files safely\n\nThis is the basic idea behind routing and serving pages in Node.js.",
    "type": "nodejs"
  },
  {
    "question": "What is regex in Node.js?",
    "answer": "A regex (regular expression) is a pattern used to test if a string follows a certain format.\n\n**Basic example:**\n```javascript\n\"hello\".match(/^hello$/); // matches exactly \"hello\"\n```\n\nRegex uses special symbols to describe patterns.\n\n**1. Character Ranges:**\n```javascript\n[0-9] // Matches any single digit from 0 to 9\n\n\"5\".match(/[0-9]/);   // ✔ matches\n\"a\".match(/[0-9]/);   // ✖ doesn't match\n```\n\n**2. Quantifiers:**\nThese tell regex how many times something should appear:\n- `+` → 1 or more\n- `*` → 0 or more\n- `?` → 0 or 1\n\n**Examples:**\n```javascript\n\"123\".match(/[0-9]+/); // ✔ matches (3 digits)\n\"9\".match(/[0-9]+/);   // ✔ matches (1 digit)\n\"\".match(/[0-9]+/);    // ✖ needs at least one digit\n```\n\n**3. Anchors:**\n- `^` → start of string\n- `$` → end of string\n\n**Examples:**\n```javascript\n\"hello\".match(/^he/);   // ✔ starts with \"he\"\n\"hello\".match(/lo$/);   // ✔ ends with \"lo\"\n```\n\n**4. Route Regex Example:**\n```javascript\n/^\\/api\\/users\\/([0-9]+)$/\n```\n\n**Breakdown:**\n- `^` → start\n- `\\/api\\/users\\/` → matches the exact path\n- `([0-9]+)` → one or more digits (captured in group)\n- `$` → end\n\n**This matches URLs like:**\n- `/api/users/1`\n- `/api/users/25`\n- `/api/users/90001`\n\n**Does NOT match:**\n- `/api/users/abc`\n- `/api/users/1/edit`\n- `/users/1`\n\n**5. Full Example in Code:**\n```javascript\nif (req.url.match(/^\\/api\\/users\\/([0-9]+)$/) && req.method === 'GET') {\n  // This block runs ONLY when URL is /api/users/number\n  // Example: /api/users/42\n  const userId = req.url.match(/^\\/api\\/users\\/([0-9]+)$/)[1];\n  console.log(`User ID: ${userId}`);\n}\n```",
    "type": "nodejs"
  },
  {
    "question": "What is the .find method in JavaScript?",
    "answer": "The `.find()` method searches through an array and returns the first element that satisfies a condition you define.\n\n**If nothing matches?**\nIt returns `undefined`, not an error.\n\n**Basic example with numbers:**\n```javascript\nconst nums = [10, 20, 30, 40];\nconst result = nums.find(n => n > 25);\nconsole.log(result); // 30\n```\n\nIt stops as soon as it finds a match — efficient and neat.\n\n**Example with objects (where .find() really shines):**\n```javascript\nconst users = [\n  { id: 1, name: \"Anna\" },\n  { id: 2, name: \"Ben\" },\n  { id: 3, name: \"Cara\" }\n];\n\nconst user = users.find(u => u.id === 2);\nconsole.log(user);\n// { id: 2, name: \"Ben\" }\n```\n\n**Key points:**\n- Works only on arrays\n- Returns the actual item, not an index\n- Stops at the first match\n- Doesn't modify the original array",
    "type": "javascript"
  },
  {
    "question": "How to write a basic HTTP route handler in Node.js?",
    "answer": "**Basic HTTP route handler for getting a user by ID:**\n\n```javascript\nif (req.url.match(/^\\/api\\/users\\/([0-9]+)$/) && req.method === \"GET\") {\n  const id = req.url.split(\"/\")[3];\n  const user = users.find((u) => u.id === id);\n\n  if (user) {\n    res.setHeader(\"Content-Type\", \"application/json\");\n    res.write(JSON.stringify(user));\n    res.end();\n    return;\n  }\n\n  res.statusCode = 404;\n  res.setHeader(\"Content-Type\", \"application/json\");\n  res.write(JSON.stringify({ message: \"User Not Found\" }));\n  res.end();\n  return;\n}\n```\n\n**What this code does:**\n1. Checks if the URL matches `/api/users/{number}` AND method is GET\n2. Extracts the ID from the URL using `split(\"/\")[3]`\n3. Uses `.find()` to search for the user with that ID\n4. If user is found:\n   - Sets content type to JSON\n   - Sends the user data as JSON response\n5. If user is not found:\n   - Sets status code to 404\n   - Sends a \"User Not Found\" message as JSON\n\n**Alternative using regex capture group:**\n```javascript\nif (req.url.match(/^\\/api\\/users\\/([0-9]+)$/) && req.method === \"GET\") {\n  const match = req.url.match(/^\\/api\\/users\\/([0-9]+)$/);\n  const id = match[1]; // Get the captured group\n  const user = users.find((u) => u.id === parseInt(id));\n  // ... rest of the code\n}\n```",
    "type": "nodejs"
  },
  {
    "question": "What is middleware in Node.js?",
    "answer": "Middleware is just a function that runs BEFORE your main route handler.\n\n**What middleware can do:**\n- Check the request\n- Modify the request or response\n- Log things\n- Set headers\n- Block the request\n- Allow it to continue\n\n**Simple definition:**\n👉 Middleware = a helper that runs in the middle before your actual route code.\n\n**⭐ Basic Middleware Syntax:**\nMiddleware always looks like this:\n```javascript\n(req, res, next) => {\n  // do something\n  next(); // lets the request continue\n}\n```\n\n- `req` = request\n- `res` = response\n- `next()` → calls the next middleware/route\n\n**⚠️ Important:** If you forget `next()`, your server freezes.\n\n**⭐ Middleware Examples:**\n\n**1. Logger Middleware:**\n```javascript\nconst logger = (req, res, next) => {\n  console.log(`${req.method} ${req.url}`);\n  next();\n};\n```\n*What this middleware does:*\nEvery request that comes in gets logged:\n```\nGET /api/users\n```\nThen it calls `next()` so the request can move forward.\n\n**2. JSON Middleware:**\n```javascript\nconst jsonMiddleware = (req, res, next) => {\n  res.setHeader(\"Content-Type\", \"application/json\");\n  next();\n};\n```\n*What it does:*\nSets the response type to JSON before the route handler runs.\n\n**3. How Middlewares Are Used Together:**\nInside your server:\n```javascript\nlogger(req, res, () => {\n  jsonMiddleware(req, res, () => {\n    // then the routes run here\n  });\n});\n```\n\n**Flow:**\n```\nRequest comes in\n   ↓\nlogger middleware runs\n   ↓\njson middleware runs\n   ↓\nthen the correct route runs\n```\n\n**4. A Route that Runs After Middleware:**\nExample:\n```javascript\nif (req.url === \"/api/users\" && req.method === \"GET\") {\n  getUsersHandler(req, res);\n}\n```\nBecause the middlewares already ran:\n- The request is already logged\n- The response is already set to JSON\n- So the handler can focus only on sending data.\n\n**⭐ Quick Summary:**\n- Middleware = helper functions that run before your main route\n- They use `(req, res, next)`\n- `next()` moves to the next step\n- Your server chains them together before checking routes",
    "type": "nodejs"
  },
  {
    "question": "How do you add data to an API using POST in Node.js?",
    "answer": "**1. What a POST Route Does:**\nA POST route is used when the client wants to send new data to the server.\nExample: adding a new user to your API.\n\n**2. Correct Route Matching:**\nPOST requests are sent to:\n```\n/api/users\n```\nSo the server must check for:\n```javascript\nreq.url === \"/api/users\" && req.method === \"POST\"\n```\n*Missing the `/` makes the route fail.*\n\n**3. Reading the Request Body:**\nPOST data arrives in small chunks. You collect it like this:\n```javascript\nlet body = \"\";\n\nreq.on(\"data\", (chunk) => {\n  body += chunk.toString();\n});\n```\nYou're basically collecting pieces until the full body arrives.\n\n**4. When All Data Arrives:**\n`end` means the request body is complete:\n```javascript\nreq.on(\"end\", () => {\n  const newUser = JSON.parse(body);\n});\n```\nNow you convert the string into a usable object.\n\n**5. Saving the New User:**\nYou can store the new user in your array:\n```javascript\nusers.push(newUser);\n```\n\n**6. Sending a Proper Response:**\nFor successful creation, use status 201:\n```javascript\nres.statusCode = 201;\nres.write(JSON.stringify(newUser));\nres.end();\n```\nThis returns the newly added user back to the client.\n\n**⭐ Complete POST Route Example:**\n```javascript\nif (req.url === \"/api/users\" && req.method === \"POST\") {\n  let body = \"\";\n  \n  req.on(\"data\", (chunk) => {\n    body += chunk.toString();\n  });\n  \n  req.on(\"end\", () => {\n    const newUser = JSON.parse(body);\n    users.push(newUser);\n    \n    res.statusCode = 201;\n    res.setHeader(\"Content-Type\", \"application/json\");\n    res.write(JSON.stringify(newUser));\n    res.end();\n  });\n}\n```\n\n**⭐ Routing Logic Explained:**\nThe server works like a traffic officer:\n- If the request says \"GET /api/users\" → send to getUsersHandler\n- If the request says \"POST /api/users\" → send to createUserHandler\n- If it doesn't match anything → send to notFoundHandler\n\n**Simple Routing Example:**\n```javascript\nconst server = createServer((req, res) => {\n  if (req.url === \"/api/users\" && req.method === \"GET\") {\n    getUsersHandler(req, res);\n  } else if (req.url === \"/api/users\" && req.method === \"POST\") {\n    createUserHandler(req, res);\n  } else {\n    notFoundHandler(req, res);\n  }\n});\n```\nThis simple check is called **routing**.",
    "type": "nodejs"
  },

  {
    "question": "What is the Node.js path module?",
    "answer": "**Simple Definition:**\nThe path module is a tool kit for working with file and folder locations. It helps you take apart, examine, and build file paths.\n\n**Breaking Down a Path:**\n```javascript\nconst filePath = \"./dir1/dir2/text.txt\";\n\n// basename() - Gets the filename with extension\npath.basename(filePath) // \"text.txt\"\n\n// dirname() - Gets the folder path\npath.dirname(filePath)  // \"./dir1/dir2\"\n\n// extname() - Gets the file extension\npath.extname(filePath)  // \".txt\"\n\n// parse() - Shows all path pieces at once\npath.parse(filePath)    // {root, dir, base, ext, name}\n```\n\n**Finding Your Current Location:**\n```javascript\n// Gets the full path of current file\nconst __filename = url.fileURLToPath(import.meta.url);\n\n// Gets the folder containing current file  \nconst __dirname = path.dirname(__filename);\n```\n\n**Building New Paths:**\n```javascript\n// join() - Combines folder names into a path\npath.join(__dirname, \"dir1\", \"dir2\", 'text.txt')\n// Result: /current/folder/dir1/dir2/text.txt\n\n// resolve() - Creates absolute full computer path\npath.resolve(__dirname, \"dir1\", \"dir2\", \"text.txt\")\n// Similar to join() but gives complete path\n```\n\n**Key Idea:**\nUse the path module to work with file locations safely across different operating systems.",
    "type": "nodejs"
  },
  {
    "question": "What is the Node.js OS module?",
    "answer": "**Simple Definition:**\nThe os module lets you get information about your computer system - like checking your computer's memory, processor, and user details.\n\n**User Information:**\n```javascript\n// userInfo() - Gets current user details\nos.userInfo()\n// Shows: username, home directory, shell\n```\n\n**Memory Information:**\n```javascript\n// totalmem() - Total RAM in computer\nos.totalmem()\n// Returns number in bytes\n\n// freemem() - Available RAM right now  \nos.freemem()\n// Returns number in bytes\n\n// Convert bytes to GB\nconsole.log(`Total RAM: ${os.totalmem() / 1024 / 1024 / 1024} GB`);\n```\n\n**CPU Information:**\n```javascript\n// cpus() - Gets processor information\nos.cpus()\n// Shows all CPU cores with speed and model\n\n// Count CPU cores\nconsole.log(`CPU Cores: ${os.cpus().length}`);\n```\n\n**Other Useful Methods:**\n```javascript\nos.platform()    // Operating system\nos.arch()        // Architecture (x64, arm)\nos.uptime()      // System uptime in seconds\nos.hostname()    // Computer name\nos.networkInterfaces() // Network information\n```\n\n**Key Idea:**\nThe os module helps you check your computer's system information and resources directly from Node.js.",
    "type": "nodejs"
  },
  {
    "question": "What is the Node.js URL module?",
    "answer": "**Simple Definition:**\nThe url module helps you work with web addresses - breaking them into pieces, reading search parameters, and converting file URLs.\n\n**Breaking Down a URL:**\n```javascript\nconst urlString = \"https://www.google.com/search?q=hello+world\";\n\n// URL() - Breaks URL into readable pieces\nconst urlObj = new URL(urlString);\n// Shows: protocol, hostname, pathname, search\n\nconsole.log(urlObj.protocol); // \"https:\"\nconsole.log(urlObj.hostname); // \"www.google.com\"\nconsole.log(urlObj.pathname); // \"/search\"\nconsole.log(urlObj.search);   // \"?q=hello+world\"\n```\n\n**Working with File URLs:**\n```javascript\n// import.meta.url - Gets current file location as URL\nimport.meta.url // \"file:///current/file/path\"\n\n// fileURLToPath() - Converts file URL to normal path\nurl.fileURLToPath(import.meta.url) // \"/current/file/path\"\n```\n\n**Working with Search Parameters:**\n```javascript\n// URLSearchParams - Manages the ?query=string part\nconst params = new URLSearchParams(urlObj.search);\n\n// get() - Reads a specific parameter\nparams.get(\"q\") // \"hello world\"\n\n// append() - Adds new parameter\nparams.append(\"limit\", \"5\") // Adds &limit=5\n\n// delete() - Removes parameter  \nparams.delete(\"limit\") // Removes &limit=5\n\n// has() - Check if parameter exists\nparams.has(\"q\") // true\n\n// getAll() - Get all values for parameter\nparams.getAll(\"tag\") // Useful for multiple values\n```\n\n**Key Idea:**\nThe url module helps you understand and manipulate web addresses and file paths in your code.",
    "type": "nodejs"
  },
  {
    "question": "What is the Node.js Crypto module?",
    "answer": "**Simple Definition:**\nThe crypto module provides tools for encryption, hashing, and security - like creating secret codes and fingerprints for data.\n\n**1. Creating Hashes (One-Way Fingerprint)**\n```javascript\nimport crypto from 'crypto';\n\nconst hash = crypto.createHash(\"sha256\");\nhash.update(\"password1234\");\nconst hashedPassword = hash.digest(\"hex\");\n// Creates: Unique fingerprint that cannot be reversed\n```\n*Use:* Storing passwords securely\n*Like:* Turning words into permanent secret codes\n\n**2. Generating Random Bytes (Creating Secrets)**\n```javascript\n// Synchronous\nconst randomBytes = crypto.randomBytes(16);\nconsole.log(randomBytes.toString(\"hex\"));\n\n// Asynchronous\ncrypto.randomBytes(16, (err, buf) => {\n  if (err) throw err;\n  console.log(buf.toString(\"hex\"));\n});\n```\n*Creates:* Random secret codes\n*Use:* Generating secure keys and passwords\n*Like:* Creating random locker combinations\n\n**3. Encryption & Decryption (Two-Way Lock)**\n```javascript\n// Setup\nconst algorithm = \"aes-256-cbc\";\nconst key = crypto.randomBytes(32);  // Main secret key\nconst iv = crypto.randomBytes(16);   // Extra random secret\n\n// Encrypt (Lock message)\nconst cipher = crypto.createCipheriv(algorithm, key, iv);\nlet encrypted = cipher.update(\"hello\", \"utf8\", \"hex\");\nencrypted += cipher.final(\"hex\");\n\n// Decrypt (Unlock message)  \nconst decipher = crypto.createDecipheriv(algorithm, key, iv);\nlet decrypted = decipher.update(encrypted, 'hex', 'utf8');\ndecrypted += decipher.final(\"utf8\");\n\nconsole.log(decrypted); // \"hello\"\n```\n\n**Key Differences:**\n- **Hashing:** One-way (create fingerprint, cannot reverse)\n- **Encryption:** Two-way (lock and unlock with same key)\n\n**Common Use Cases:**\n```javascript\n// Password hashing\nfunction hashPassword(password) {\n  return crypto.createHash('sha256').update(password).digest('hex');\n}\n\n// Generate secure tokens\nfunction generateToken() {\n  return crypto.randomBytes(32).toString('hex');\n}\n\n// Verify data integrity\nfunction createHMAC(data, secret) {\n  return crypto.createHmac('sha256', secret).update(data).digest('hex');\n}\n```\n\n**Key Idea:** Use crypto to secure passwords, create secret messages, and generate secure random data.",
    "type": "nodejs"
  },

  {
    "question": "What is the Node.js Process Module?",
    "answer": "**Simple Definition:**\nThe process module gives you information and control about your currently running Node.js program.\n\n**Command Line Arguments:**\n```javascript\n// process.argv - Gets command line arguments\nprocess.argv[2] // First argument after filename\nprocess.argv[3] // Second argument\n\n// If you ran: node app.js hello world\n// Then:\n// process.argv[0] = path to node\n// process.argv[1] = path to app.js\n// process.argv[2] = \"hello\"\n// process.argv[3] = \"world\"\n```\n\n**Environment Information:**\n```javascript\n// process.env - Shows all environment variables\nprocess.env.USERNAME // Current user\nprocess.env.NODE_ENV // Development/Production\nprocess.env.PATH     // System paths\n\n// process.pid - Shows program's process ID\nprocess.pid // \"This program is process #1234\"\n\n// process.cwd() - Shows current working directory\nprocess.cwd() // \"You're currently in this folder\"\n\n// process.title - Shows program name\nprocess.title // \"This program is called: node\"\n\n// process.version - Node.js version\nprocess.version // \"v18.15.0\"\n```\n\n**Program Performance:**\n```javascript\n// process.memoryUsage() - Checks memory usage\nconst memory = process.memoryUsage();\nconsole.log(`Heap used: ${memory.heapUsed} bytes`);\n// Shows how much memory your program is using\n\n// process.uptime() - Shows how long program has been running\nprocess.uptime() // \"Running for X seconds\"\n\n// process.cpuUsage() - CPU usage statistics\nconst startUsage = process.cpuUsage();\n// ... do some work\nconst endUsage = process.cpuUsage(startUsage);\n```\n\n**Program Lifecycle Control:**\n```javascript\n// process.on('exit') - Runs code when program exits\nprocess.on('exit', (code) => {\n    console.log(`Exiting with code: ${code}`);\n});\n\n// process.on('uncaughtException') - Handle uncaught errors\nprocess.on('uncaughtException', (err) => {\n    console.error('Uncaught Exception:', err);\n    process.exit(1);\n});\n\n// process.exit() - Stops program immediately\nprocess.exit(0) // Successful exit\nprocess.exit(1) // Error exit\n// Code after this won't run\n```\n\n**Key Idea:** Use the process module to get information about your running program and control its behavior.",
    "type": "nodejs"
  },
  {
    "question": "What is Node.js EventEmitter?",
    "answer": "**Simple Definition:**\nEventEmitter is a messaging system that lets different parts of your code communicate by sending and receiving events.\n\n**Setting Up EventEmitter:**\n```javascript\nimport { EventEmitter } from \"events\";\n\n// Create an event emitter instance\nconst myEmitter = new EventEmitter();\n```\n\n**Creating Event Handlers:**\n```javascript\n// Functions that run when events occur\nfunction greetHandler(name) {\n  console.log(\"Hello \" + name);\n}\n\nfunction goodbyeHandler(name) {\n  console.log(\"Goodbye \" + name);\n}\n\nfunction dataHandler(data) {\n  console.log(\"Received data:\", data);\n}\n```\n\n**Registering Event Listeners:**\n```javascript\n// Listen for specific events\nmyEmitter.on(\"greet\", greetHandler);\nmyEmitter.on(\"goodbye\", goodbyeHandler);\nmyEmitter.on(\"data\", dataHandler);\n\n// once() - Listen for event only once\nmyEmitter.once(\"welcome\", () => {\n  console.log(\"Welcome! (This only runs once)\");\n});\n```\n\n**Emitting Events:**\n```javascript\n// Trigger events with data\nmyEmitter.emit(\"greet\", 'Mary');     // Output: \"Hello Mary\"\nmyEmitter.emit(\"goodbye\", 'John');   // Output: \"Goodbye John\"\nmyEmitter.emit(\"data\", { id: 1, name: 'Test' });\nmyEmitter.emit(\"welcome\");           // Runs once\nmyEmitter.emit(\"welcome\");           // Doesn't run again\n```\n\n**Error Handling:**\n```javascript\n// Listen for error events\nmyEmitter.on(\"error\", (err) => {\n  console.log('An Error Occurred:', err);\n});\n\n// Emit error events\nmyEmitter.emit(\"error\", new Error('Something went wrong'));\n```\n\n**Removing Listeners:**\n```javascript\n// Remove specific listener\nmyEmitter.off(\"greet\", greetHandler);\n\n// Remove all listeners for an event\nmyEmitter.removeAllListeners(\"greet\");\n\n// Remove all listeners\nmyEmitter.removeAllListeners();\n```\n\n**Key Methods:**\n- `on(event, handler)` - Listen for an event\n- `once(event, handler)` - Listen for event only once\n- `emit(event, data)` - Trigger an event with data\n- `off(event, handler)` - Remove specific listener\n- `removeAllListeners(event)` - Remove all listeners for event\n\n**Key Idea:** EventEmitter allows different parts of your code to communicate through events - one part sends messages (emit) and other parts listen and respond (on).",
    "type": "nodejs"
  },
  {
    "question": "What is React Router Outlet?",
    "answer": "**Simple Definition:**\nAn Outlet is a placeholder component that renders the current child route's content within a parent layout.\n\n**Think of it like this:**\nAn Outlet is an empty picture frame in your layout - you keep the same frame but change the picture inside when navigating.\n\n**Basic Usage:**\n```jsx\nimport { Outlet } from 'react-router-dom';\n\n// Parent layout component\nfunction Layout() {\n  return (\n    <div>\n      <header>My Website Header</header>\n      <nav>Navigation Menu</nav>\n      \n      {/* Child routes appear here */}\n      <Outlet />\n      \n      <footer>My Website Footer</footer>\n    </div>\n  );\n}\n```\n\n**Router Setup:**\n```jsx\nimport { BrowserRouter, Routes, Route } from 'react-router-dom';\n\nfunction App() {\n  return (\n    <BrowserRouter>\n      <Routes>\n        <Route path=\"/\" element={<Layout />}>\n          <Route index element={<HomePage />} />\n          <Route path=\"home\" element={<HomePage />} />\n          <Route path=\"about\" element={<AboutPage />} />\n          <Route path=\"contact\" element={<ContactPage />} />\n          <Route path=\"products\" element={<ProductsPage />} />\n        </Route>\n      </Routes>\n    </BrowserRouter>\n  );\n}\n```\n\n**What Happens:**\n- `/` or `/home` → HomePage appears in the Outlet\n- `/about` → AboutPage appears in the Outlet\n- `/contact` → ContactPage appears in the Outlet\n- `/products` → ProductsPage appears in the Outlet\n\n**Nested Routes with Multiple Outlets:**\n```jsx\n// Dashboard layout with sidebar and main content\nfunction Dashboard() {\n  return (\n    <div className=\"dashboard\">\n      <Sidebar />\n      <div className=\"main-content\">\n        <Outlet />  {/* Nested routes appear here */}\n      </div>\n    </div>\n  );\n}\n\n// Router setup with nested routes\n<Routes>\n  <Route path=\"dashboard\" element={<Dashboard />}>\n    <Route index element={<DashboardHome />} />\n    <Route path=\"settings\" element={<Settings />} />\n    <Route path=\"profile\" element={<Profile />} />\n  </Route>\n</Routes>\n```\n\n**Benefits of Using Outlet:**\n- **Reusable Layouts**: Share headers, footers, navigation across multiple pages\n- **Clean Organization**: Separate layout concerns from page content\n- **Nested Routing**: Support for multi-level navigation structures\n- **Performance**: Only the Outlet content re-renders on navigation\n\n**Key Idea:**\nOutlet allows you to create shared layouts (headers, footers, sidebars) while only changing the main content area when navigating between routes. It's essential for building structured, maintainable React applications with React Router.",
    "type": "react"
  },

  {
    "question": "What is Express.js?",
    "answer": "**Simple Definition:**\nExpress.js is a web framework for Node.js that makes building websites and APIs much easier.\n\n**What It Does:**\n- Handles HTTP requests and responses\n- Creates routes for different URLs\n- Adds middleware for extra functionality\n- Serves HTML, JSON, or files\n\n**Basic Example:**\n```javascript\nconst express = require('express');\nconst app = express();\n\napp.get('/', (req, res) => {\n  res.send('Home Page');\n});\n\napp.listen(3000);\n```\n\n**Installation:**\n```bash\nnpm i express\n```\n\n**Key Idea:**\nExpress simplifies web server development by handling the complex web communication stuff so you can focus on your application logic.",
    "type": "nodejs"
  },
  {
    "question": "What are Opinionated vs. Unopinionated Frameworks?",
    "answer": "**Opinionated** (pronounced uh-PIN-yuh-nay-ted):\n- Has strong rules and specific ways of doing things\n- Provides structure and conventions\n\n**Unopinionated:**\n- Provides tools but lets you decide how to use them\n- Offers freedom and flexibility\n\n**IKEA Analogy:**\n- **Opinionated = IKEA Furniture**\n  - Specific instructions\n  - Pre-designed parts\n  - Must follow their way\n  - *Examples: Next.js, Ruby on Rails, Angular*\n\n- **Unopinionated = Wood & Tools**\n  - Basic materials only\n  - No instructions\n  - Build anything any way\n  - *Examples: Express.js, Vanilla JS, Vue.js*\n\n**Key Idea:**\nOpinionated frameworks provide structure and rules; unopinionated frameworks provide freedom and flexibility.",
    "type": "programming"
  },
  {
    "question": "How do you load files with Node.js and Express.js (normal and static)?",
    "answer": "**Method 1: Manual File Serving (Normal)**\n```javascript\nconst express = require(\"express\");\nconst path = require(\"path\");\nconst app = express();\n\napp.get(\"/\", (req, res) => {\n  res.sendFile(path.join(__dirname, 'public', 'index.html'));\n});\n\napp.get(\"/about\", (req, res) => {\n  res.sendFile(path.join(__dirname, \"public\", \"about.html\"));\n});\n\napp.listen(9000, () => {\n  console.log(\"Server is running on port 9000\");\n});\n```\n\n**Method 2: Static File Serving (Recommended)**\n```javascript\n// Setup static file serving\napp.use(express.static(path.join(__dirname, \"public\")));\n\n// Now Express automatically serves files from the 'public' folder:\n// - / → serves public/index.html\n// - /about.html → serves public/about.html\n// - /css/style.css → serves public/css/style.css\n// - /images/logo.png → serves public/images/logo.png\n```\n\n**Folder Structure:**\n```\nproject/\n├── server.js\n└── public/\n    ├── index.html\n    ├── about.html\n    ├── css/\n    │   └── style.css\n    └── images/\n        └── logo.png\n```\n\n**Key Differences:**\n- **Manual**: You define each route individually\n- **Static**: Express automatically serves all files in the folder\n- **Static is better** for serving CSS, JS, images, and multiple HTML files",
    "type": "nodejs"
  },
  {
    "question": "How do you create Dynamic Routes in Express.js?",
    "answer": "**Simple Definition:**\nDynamic routes allow you to create URLs with changing parts, like /posts/1, /posts/2, etc., where the number can be any post ID.\n\n**Basic Syntax:**\n```javascript\napp.get(\"/posts/:id\", (req, res) => {\n  const postId = req.params.id;  // Gets the ID from URL\n  // Your code to find and return the post\n});\n```\n\n**Complete Example:**\n```javascript\n// Sample posts data\nconst posts = [\n  { id: 1, title: \"First Post\", content: \"Hello world!\" },\n  { id: 2, title: \"Second Post\", content: \"Learning Express\" },\n  { id: 3, title: \"Third Post\", content: \"Dynamic routes are cool\" }\n];\n\n// Dynamic route for individual posts\napp.get(\"/posts/:id\", (req, res) => {\n  const postId = parseInt(req.params.id);  // Convert string to number\n  const post = posts.find(p => p.id === postId);  // Find post by ID\n  \n  if (post) {\n    res.json(post);  // Return the post if found\n  } else {\n    res.status(404).json({ error: \"Post not found\" });\n  }\n});\n```\n\n**How URLs Work:**\n- `/posts/1` → Returns post with ID 1\n- `/posts/2` → Returns post with ID 2\n- `/posts/3` → Returns post with ID 3\n- `/posts/999` → Returns \"Post not found\" error\n\n**Multiple Parameters:**\n```javascript\napp.get(\"/users/:userId/posts/:postId\", (req, res) => {\n  const userId = req.params.userId;\n  const postId = req.params.postId;\n  // Handle the request\n});\n```\n\n**Key Points:**\n- `:id` is a URL parameter that can be any value\n- Access it with `req.params.id`\n- Always convert to number if needed (`parseInt()`)\n- Add error handling for invalid IDs\n\n**Key Idea:** Dynamic routes let you create flexible URLs that can handle different IDs or values in the URL path.",
    "type": "nodejs"
  },
  {
    "question": "Why should you use return after sending a response in Express?",
    "answer": "**Simple Definition:**\nWhen you send a response (like `res.json()` or `res.send()`), the function doesn't automatically stop. You need to use `return` to prevent sending multiple responses.\n\n**Without Return (Problem):**\n```javascript\nif (post) {\n  res.json(post);\n} \n// Code continues running here!\nres.status(404).json({ error: \"Not found\" });\n// This will ALWAYS run → ERROR! (Can't send multiple responses)\n```\n\n**With Return (Solution):**\n```javascript\nif (post) {\n  return res.json(post);  // Stops here if post exists\n} \nres.status(404).json({ error: \"Not found\" });  // Only runs if no post\n```\n\n**Or Use Else:**\n```javascript\nif (post) {\n  res.json(post);\n} else {\n  res.status(404).json({ error: \"Not found\" });\n}\n// Both paths are separate - no multiple responses\n```\n\n**Why This Matters:**\n- Express will throw an error if you try to send multiple responses\n- Without `return`, both response statements might execute\n- This is a common source of bugs in Express applications\n\n**Key Idea:**\nUse `return` after sending a response to stop the function execution, otherwise Express will try to send multiple responses and crash.",
    "type": "nodejs"
  },
  {
    "question": "How do you split Express routes into separate files?",
    "answer": "**Simple Definition:**\nSplitting routes means organizing your API endpoints into different files instead of having all routes in one main file.\n\n**Step 1: Create the Main Server File (server.js)**\n```javascript\nconst express = require(\"express\");\nconst postsRouter = require(\"./routes/posts\"); // Import router\nconst app = express();\n\n// Use the router with base path\napp.use('/api/posts', postsRouter);\n\napp.listen(3000, () => {\n  console.log(\"Server running\");\n});\n```\n\n**Step 2: Create Routes Folder & File (routes/posts.js)**\n```javascript\nconst express = require(\"express\");\nconst router = express.Router(); // Create router instance\n\n// Sample data\nconst posts = [\n  { id: 1, title: \"First Post\" },\n  { id: 2, title: \"Second Post\" }\n];\n\n// Define routes WITHOUT the base path\nrouter.get(\"/\", (req, res) => {\n  // Handles: GET /api/posts\n  res.json(posts);\n});\n\nrouter.get(\"/:id\", (req, res) => {\n  // Handles: GET /api/posts/5\n  const post = posts.find(p => p.id === parseInt(req.params.id));\n  if (post) {\n    res.json(post);\n  } else {\n    res.status(404).json({ error: \"Post not found\" });\n  }\n});\n\nmodule.exports = router; // Export the router\n```\n\n**Key Rules for Splitting:**\n\n**Main File (server.js):**\n- Import router: `require(\"./routes/posts\")`\n- Use base path: `app.use('/api/posts', postsRouter)`\n\n**Route File (routes/posts.js):**\n- Create router: `express.Router()`\n- Define relative paths: `\"/\"` instead of `\"/api/posts\"`\n- Export router: `module.exports = router`\n\n**Path Matching Explained:**\n- `server.js`: `app.use('/api/posts', postsRouter)`\n- `posts.js`: `router.get(\"/\")`\n- **Result URL**: `/api/posts/` + `/` = `/api/posts`\n\n**Benefits:**\n- Better organization\n- Easier maintenance\n- Team collaboration\n- Scalable codebase",
    "type": "nodejs"
  },

  {
    "question": "How does the filter() method work for deleting items?",
    "answer": "**How filter() works:**\n- Goes through each item in the array\n- Keeps the item if your condition returns `true`\n- Removes the item if your condition returns `false`\n- Always returns a new array (doesn't modify the original)\n\n**Delete logic with filter():**\n```javascript\n// To delete an item, keep everything that is NOT the item\nposts = posts.filter(post => post.id !== idToDelete);\n```\n\n**Key comparison:**\n- `!==` → keep all other items → removes the match (correct for deletion)\n- `===` → keeps only the match → wrong for deletion\n\n**Example:**\n```javascript\nconst posts = [\n  { id: 1, title: \"First Post\" },\n  { id: 2, title: \"Second Post\" },\n  { id: 3, title: \"Third Post\" }\n];\n\n// Delete post with id 2\nconst idToDelete = 2;\nconst updatedPosts = posts.filter(post => post.id !== idToDelete);\n\nconsole.log(updatedPosts);\n// Output: [{ id: 1, title: \"First Post\" }, { id: 3, title: \"Third Post\" }]\n```\n\n**Remember:**\n- `===` keeps the item you're trying to delete\n- `!==` removes the item you're trying to delete\n- Filter creates a new array, so assign it back to your variable",
    "type": "javascript"
  },
  {
    "question": "What is the white-space: pre-line CSS property?",
    "answer": "**Simple Definition:**\nThe `white-space: pre-line` CSS property controls how whitespace and line breaks are handled in HTML.\n\n**Normal HTML Behavior:**\nHTML collapses spaces and ignores line breaks in text.\n\n**Example without pre-line:**\n```html\n<div>\nHello\nWorld\n</div>\n```\nWould display as: `Hello World` — no line break, even though there's one in the HTML source.\n\n**Using white-space: pre-line:**\n```css\ndiv {\n  white-space: pre-line;\n}\n```\nNow the same HTML displays as:\n```\nHello\nWorld\n```\n\n**Other white-space values:**\n- `normal`: Collapses whitespace (default)\n- `nowrap`: Collapses whitespace, no line breaks\n- `pre`: Preserves all whitespace and line breaks\n- `pre-line`: Collapses spaces but preserves line breaks\n- `pre-wrap`: Preserves both spaces and line breaks\n\n**Key Idea:** `white-space: pre-line` makes HTML respect the line breaks (`\\n`) in your text content while still collapsing multiple spaces into one.",
    "type": "css"
  },
  {
    "question": "What is the .replace() method in JavaScript?",
    "answer": "**Simple Definition:**\nThe `.replace()` method is a string method that lets you replace part of a string with something else.\n\n**Syntax:**\n```javascript\nstring.replace(searchValue, newValue)\n```\n- `searchValue` → what you want to find (can be a string or regular expression)\n- `newValue` → what you want to replace it with\n\n**Example with string replacement:**\n```javascript\nlet text = \"I have a dog. My neighbor has a dog too.\";\nlet newText = text.replace(/dog/g, \"cat\");\nconsole.log(newText);\n// Output: \"I have a cat. My neighbor has a cat too.\"\n```\n- `/dog/g` → find all \"dog\" words (g = global)\n- `\"cat\"` → replace with \"cat\"\n\n**Example with line breaks for HTML:**\n```javascript\nlet message = \"Hello\\nWorld\\nHow are you?\";\nlet htmlText = message.replace(/\\n/g, \"<br>\");\nconsole.log(htmlText);\n// Output: \"Hello<br>World<br>How are you?\"\n```\n\n**Working with numbers:**\n`.replace()` only works on strings. If you have a number:\n```javascript\nlet num = 12345;\nnum.replace(\"2\", \"9\"); // ❌ ERROR - numbers aren't strings\n```\n\n**Solution:** Convert to string first:\n```javascript\nlet num = 12345;\nlet newNum = num.toString().replace(\"2\", \"9\"); \nconsole.log(newNum); // \"19345\"\n\n// Using regex with numbers\nlet zeroedNum = num.toString().replace(/\\d/g, \"0\"); \nconsole.log(zeroedNum); // \"00000\"\n```\n\n**Key Points:**\n- Works only on strings\n- Use regex with `g` flag for global replacement\n- Returns a new string (original string unchanged)\n- Can use capture groups for advanced replacements",
    "type": "javascript"
  },
  {
    "question": "What are some Express.js request properties and methods?",
    "answer": "**Simple Definition:**\nExpress provides properties and methods on the `req` (request) object to get information about incoming HTTP requests.\n\n**Request Properties (Data about the request):**\n```javascript\n// HTTP Method & URL\nreq.method      // \"GET\", \"POST\", \"PUT\", \"DELETE\" \nreq.protocol    // \"http\" or \"https\"\nreq.originalUrl // \"/api/posts/5\" (full URL path)\nreq.path        // \"/api/posts/5\" (same as originalUrl)\nreq.params      // { id: \"5\" } (URL parameters)\nreq.query       // { limit: \"10\" } (query string ?limit=10)\n\n// Headers & Host\nreq.headers     // All request headers\nreq.get('host') // \"localhost:3000\" or \"mysite.com\"\nreq.hostname    // \"localhost\" or \"mysite.com\"\n\n// Security & IP\nreq.secure      // true if HTTPS, false if HTTP\nreq.ip          // \"192.168.1.1\" (client IP address)\n\n// Body data (with body-parser middleware)\nreq.body        // Parsed request body data\n```\n\n**Request Methods (Functions you can call):**\n```javascript\n// Header methods\nreq.get('Content-Type')        // Get specific header value\nreq.is('application/json')     // Check content type\n\n// Parameter methods  \nreq.param('id')               // Get parameter from URL or query\n\n// Other methods\nreq.accepts('json')           // Check what formats client accepts\nreq.accepts(['json', 'html']) // Check multiple formats\n```\n\n**Middleware Example Using Properties/Methods:**\n```javascript\nconst logger = (req, res, next) => {\n    console.log(`${req.method} ${req.protocol}://${req.get('host')}${req.originalUrl}`);\n    // Example output: \"GET http://localhost:3000/api/posts/5\"\n    next();\n};\n\n// Route handler example\napp.get('/api/users/:id', (req, res) => {\n    console.log(req.params.id);    // URL parameter\n    console.log(req.query.page);   // Query string\n    console.log(req.method);       // HTTP method\n    res.json({ success: true });\n});\n```\n\n**Key Points:**\n- **Properties** = Data stored on the request object (use directly)\n- **Methods** = Functions you call on the request object (use with parentheses)\n- All properties/methods are available in every middleware and route handler\n\n**Key Idea:** Express request properties and methods give you detailed information about incoming HTTP requests, making it easy to build dynamic, responsive web applications.",
    "type": "nodejs"
  },

  {
    "question": "What are some HTTP status codes and response formats?",
    "answer": "**Simple Definition:**\nHTTP status codes are standardized numbers that tell the client whether a request succeeded or failed, and why.\n\n**Status Code Categories:**\n- **200s (Success)**: Request was successful\n- **400s (Client Errors)**: User's fault - invalid request\n- **500s (Server Errors)**: Server's fault - internal problems\n\n**Success Responses (200s):**\n- **200 OK** - Successful GET request\n- **201 Created** - New resource created (POST)\n- **204 No Content** - Successful delete, no data to return\n\n**Client Errors (400s):**\n- **400 Bad Request** - Invalid form data, wrong format\n- **401 Unauthorized** - Not logged in\n- **403 Forbidden** - Logged in but no permission\n- **404 Not Found** - Requested resource doesn't exist\n\n**Server Errors (500s):**\n- **500 Internal Server Error** - Database issues, code bugs\n\n**Code Examples:**\n\n**200 OK - Getting Data:**\n```javascript\napp.get(\"/api/posts\", async (req, res) => {\n  try {\n    const posts = await db.getPosts();\n    res.status(200).json(posts);\n  } catch (error) {\n    res.status(500).json({ error: \"Server error\" });\n  }\n});\n```\n\n**201 Created - New Resource:**\n```javascript\napp.post(\"/api/posts\", async (req, res) => {\n  try {\n    const { title, content } = req.body;\n    const newPost = await db.createPost({ title, content });\n    res.status(201).json(newPost);\n  } catch (error) {\n    res.status(500).json({ error: \"Failed to create post\" });\n  }\n});\n```\n\n**204 No Content - Successful Delete:**\n```javascript\napp.delete(\"/api/posts/:id\", async (req, res) => {\n  try {\n    await db.deletePost(req.params.id);\n    res.status(204).send(); // No content to return\n  } catch (error) {\n    res.status(500).json({ error: \"Failed to delete\" });\n  }\n});\n```\n\n**400 Bad Request - Invalid Data:**\n```javascript\napp.post(\"/api/users\", async (req, res) => {\n  try {\n    const { email, password } = req.body;\n    \n    if (!email || !password) {\n      return res.status(400).json({ error: \"Email and password required\" });\n    }\n    \n    if (password.length < 6) {\n      return res.status(400).json({ error: \"Password too short\" });\n    }\n    \n    const user = await db.createUser({ email, password });\n    res.status(201).json(user);\n  } catch (error) {\n    res.status(500).json({ error: \"Registration failed\" });\n  }\n});\n```\n\n**404 Not Found - Resource Doesn't Exist:**\n```javascript\napp.get(\"/api/posts/:id\", async (req, res) => {\n  try {\n    const post = await db.getPost(req.params.id);\n    \n    if (!post) {\n      return res.status(404).json({ error: \"Post not found\" });\n    }\n    \n    res.status(200).json(post);\n  } catch (error) {\n    res.status(500).json({ error: \"Server error\" });\n  }\n});\n```\n\n**Complete Example with Multiple Status Codes:**\n```javascript\napp.put(\"/api/posts/:id\", async (req, res) => {\n  try {\n    const { title, content } = req.body;\n    \n    // 400 - Validation\n    if (!title || !content) {\n      return res.status(400).json({ error: \"Title and content required\" });\n    }\n    \n    // 404 - Check if exists\n    const existingPost = await db.getPost(req.params.id);\n    if (!existingPost) {\n      return res.status(404).json({ error: \"Post not found\" });\n    }\n    \n    // 403 - Authorization\n    if (existingPost.authorId !== req.user.id) {\n      return res.status(403).json({ error: \"Can only edit your own posts\" });\n    }\n    \n    // 200 - Success\n    const updatedPost = await db.updatePost(req.params.id, { title, content });\n    res.status(200).json(updatedPost);\n    \n  } catch (error) {\n    // 500 - Unexpected server error\n    res.status(500).json({ error: \"Failed to update post\" });\n  }\n});\n```\n\n**Key Idea:** Use appropriate HTTP status codes to clearly communicate the result of API requests and handle errors gracefully.",
    "type": "nodejs"
  },
  {
    "question": "What is npm package management?",
    "answer": "**Essential npm Commands:**\n\n**Installing Packages:**\n```bash\nnpm install express          # Install locally\nnpm i express               # Short version\nnpm install -g nodemon      # Install globally\nnpm i express mongoose      # Install multiple packages\n```\n\n**Different Installation Types:**\n```bash\n# Dependencies (needed for production)\nnpm install express mongoose\n\n# Dev Dependencies (only for development)\nnpm install --save-dev nodemon prettier\nnpm i -D nodemon           # Short version\n\n# Global tools (available everywhere)\nnpm install -g live-server\n```\n\n**Your package.json shows the difference:**\n```json\n{\n  \"dependencies\": {\n    \"express\": \"^4.18.0\",\n    \"mongoose\": \"^7.0.0\"\n  },\n  \"devDependencies\": {\n    \"nodemon\": \"^2.0.0\",\n    \"prettier\": \"^3.0.0\"\n  }\n}\n```\n\n**Managing Packages:**\n```bash\nnpm list                    # See installed packages\nnpm list --depth=0         # Shows: express@4.18.0\nnpm list --depth=1         # Shows: express + its direct dependencies\nnpm list --depth=2         # Shows: express + dependencies + THEIR dependencies\n\nnpm outdated               # Check for updates\nnpm update                 # Update all packages\nnpm update express         # Update specific package\n\nnpm uninstall express      # Remove package\nnpm un express            # Short version\n```\n\n**node_modules & .gitignore:**\n```bash\n# Your project structure:\nmy-app/\n├── node_modules/    # 🚨 100MB+ of dependencies\n├── package.json     # 📄 List of what to install\n├── package-lock.json # 🔒 Exact versions\n└── src/\n    └── app.js\n```\n\n**Critical .gitignore rule:**\n```gitignore\n# NEVER commit node_modules to GitHub\nnode_modules/\n.env\n.DS_Store\n```\n\n**Scripts Management:**\n```json\n{\n  \"scripts\": {\n    \"start\": \"node server.js\",\n    \"dev\": \"nodemon server.js\",\n    \"test\": \"jest\",\n    \"build\": \"webpack --mode=production\",\n    \"lint\": \"eslint src/\",\n    \"format\": \"prettier --write src/\"\n  }\n}\n```\n\n**Running scripts:**\n```bash\nnpm start          # Common: starts your app\nnpm run dev        # Development with auto-restart\nnpm run test       # Run tests\nnpm run build      # Build for production\n```\n\n**package-lock.json - Why It Matters:**\n- Locks exact versions of every dependency\n- Ensures everyone gets same versions\n- Never delete it! Commit to Git\n- Prevents \"it works on my machine\" problems\n\n**Real Workflow Example:**\n```bash\n# Starting a new project\nnpm init -y\nnpm install express mongoose\nnpm install -D nodemon prettier\n\n# Add scripts to package.json, then:\nnpm run dev          # Start development\n# ^ This uses nodemon to auto-restart on changes\n\n# When deploying:\nnpm start           # Uses regular node (no auto-restart)\n```\n\n**Key npm Concepts:**\n- **dependencies** - Packages your app NEEDS to run\n- **devDependencies** - Tools for development only\n- **scripts** - Custom commands for your project\n- **package-lock.json** - Version locking (keep it!)\n- **node_modules** - Actual installed code (ignore in Git)\n\n**Key Idea:** npm manages your project's dependencies and provides tools to automate common development tasks through scripts.",
    "type": "nodejs"
  },
  {
    "question": "What is OOP backend structure (Separation of Concerns)?",
    "answer": "**Simple Definition:**\nOrganizing backend code into specialized classes that each handle one specific responsibility, following the MVC (Model-View-Controller) pattern.\n\n**🏗️ Project Structure:**\n```\nsrc/\n├── controllers/     # 🎯 Handles HTTP requests/responses\n├── services/        # 🔧 Contains business logic\n├── models/          # 📊 Defines data structure\n└── routes/          # 🛣️ Maps URLs to controllers\n```\n\n**🔧 Each Layer's Responsibility:**\n\n**1. Models (Data Structure)**\n```javascript\n// models/Post.js\nclass Post {\n  constructor(title, content, author) {\n    this.title = title;\n    this.content = content;\n    this.author = author;\n    this.createdAt = new Date();\n  }\n  \n  isValid() {\n    return this.title && this.content;\n  }\n}\n\nmodule.exports = Post;\n```\n*Job:* Define data structure and validation\n\n**2. Services (Business Logic)**\n```javascript\n// services/PostService.js\nclass PostService {\n  constructor() {\n    this.posts = [];\n  }\n  \n  getAllPosts() {\n    return this.posts;\n  }\n  \n  createPost(postData) {\n    const newPost = new Post(postData.title, postData.content, postData.author);\n    if (!newPost.isValid()) {\n      throw new Error(\"Invalid post data\");\n    }\n    this.posts.push(newPost);\n    return newPost;\n  }\n}\n```\n*Job:* Handle business rules and data processing\n\n**3. Controllers (HTTP Handling)**\n```javascript\n// controllers/PostController.js\nclass PostController {\n  constructor() {\n    this.postService = new PostService();\n  }\n  \n  async getPosts(req, res) {\n    try {\n      const posts = this.postService.getAllPosts();\n      res.status(200).json(posts);\n    } catch (error) {\n      res.status(500).json({ error: error.message });\n    }\n  }\n}\n```\n*Job:* Manage HTTP requests and responses\n\n**4. Routes (URL Mapping)**\n```javascript\n// routes/postRoutes.js\nconst postController = new PostController();\n\nrouter.get('/posts', postController.getPosts.bind(postController));\nrouter.post('/posts', postController.createPost.bind(postController));\n```\n*Job:* Direct URLs to appropriate controllers\n\n**🎯 Workflow Example:**\n\n**Request Flow:**\n```\nPOST /api/posts \n→ Routes \n→ PostController.createPost() \n→ PostService.createPost() \n→ Post model\n→ Return HTTP response\n```\n\n**Code Flow:**\n```javascript\n// 1. Route receives request\nrouter.post('/posts', postController.createPost);\n\n// 2. Controller handles HTTP\ncreatePost(req, res) {\n  const result = this.postService.createPost(req.body);\n  res.status(201).json(result);\n}\n\n// 3. Service handles business logic\ncreatePost(postData) {\n  // Validation, processing, etc.\n  return new Post(postData);\n}\n\n// 4. Model defines data structure\nclass Post { ... }\n```\n\n**✅ Benefits:**\n- **Organization**: Code is logically separated\n- **Maintainability**: Easy to find and fix issues\n- **Testability**: Services can be tested without HTTP\n- **Reusability**: Services work with any interface (API, CLI, etc.)\n- **Teamwork**: Different developers can work on different layers\n\n**🔧 Restaurant Analogy:**\n- **Routes** = Host (directs customers to tables)\n- **Controllers** = Waiters (take orders, serve food)\n- **Services** = Chefs (cook food, business logic)\n- **Models** = Recipes (define how food should be made)\n\n**Key Idea:** Separation of concerns makes your code more organized, maintainable, and scalable by dividing responsibilities into distinct layers.",
    "type": "nodejs"
  },

  {
    "question": "How does the filter() method work for deleting items?",
    "answer": "**How filter() works:**\n- Goes through each item in the array\n- Keeps the item if your condition returns `true`\n- Removes the item if your condition returns `false`\n- Always returns a new array (doesn't modify the original)\n\n**Delete logic with filter():**\n```javascript\n// To delete an item, keep everything that is NOT the item\nposts = posts.filter(post => post.id !== idToDelete);\n```\n\n**Key comparison:**\n- `!==` → keep all other items → removes the match (correct for deletion)\n- `===` → keeps only the match → wrong for deletion\n\n**Example:**\n```javascript\nconst posts = [\n  { id: 1, title: \"First Post\" },\n  { id: 2, title: \"Second Post\" },\n  { id: 3, title: \"Third Post\" }\n];\n\n// Delete post with id 2\nconst idToDelete = 2;\nconst updatedPosts = posts.filter(post => post.id !== idToDelete);\n\nconsole.log(updatedPosts);\n// Output: [{ id: 1, title: \"First Post\" }, { id: 3, title: \"Third Post\" }]\n```\n\n**Remember:**\n- `===` keeps the item you're trying to delete\n- `!==` removes the item you're trying to delete\n- Filter creates a new array, so assign it back to your variable",
    "type": "javascript"
  },
  {
    "question": "What is the white-space: pre-line CSS property?",
    "answer": "**Simple Definition:**\nThe `white-space: pre-line` CSS property controls how whitespace and line breaks are handled in HTML.\n\n**Normal HTML Behavior:**\nHTML collapses spaces and ignores line breaks in text.\n\n**Example without pre-line:**\n```html\n<div>\nHello\nWorld\n</div>\n```\nWould display as: `Hello World` — no line break, even though there's one in the HTML source.\n\n**Using white-space: pre-line:**\n```css\ndiv {\n  white-space: pre-line;\n}\n```\nNow the same HTML displays as:\n```\nHello\nWorld\n```\n\n**Other white-space values:**\n- `normal`: Collapses whitespace (default)\n- `nowrap`: Collapses whitespace, no line breaks\n- `pre`: Preserves all whitespace and line breaks\n- `pre-line`: Collapses spaces but preserves line breaks\n- `pre-wrap`: Preserves both spaces and line breaks\n\n**Key Idea:** `white-space: pre-line` makes HTML respect the line breaks (`\\n`) in your text content while still collapsing multiple spaces into one.",
    "type": "css"
  },
  {
    "question": "What is the .replace() method in JavaScript?",
    "answer": "**Simple Definition:**\nThe `.replace()` method is a string method that lets you replace part of a string with something else.\n\n**Syntax:**\n```javascript\nstring.replace(searchValue, newValue)\n```\n- `searchValue` → what you want to find (can be a string or regular expression)\n- `newValue` → what you want to replace it with\n\n**Example with string replacement:**\n```javascript\nlet text = \"I have a dog. My neighbor has a dog too.\";\nlet newText = text.replace(/dog/g, \"cat\");\nconsole.log(newText);\n// Output: \"I have a cat. My neighbor has a cat too.\"\n```\n- `/dog/g` → find all \"dog\" words (g = global)\n- `\"cat\"` → replace with \"cat\"\n\n**Example with line breaks for HTML:**\n```javascript\nlet message = \"Hello\\nWorld\\nHow are you?\";\nlet htmlText = message.replace(/\\n/g, \"<br>\");\nconsole.log(htmlText);\n// Output: \"Hello<br>World<br>How are you?\"\n```\n\n**Working with numbers:**\n`.replace()` only works on strings. If you have a number:\n```javascript\nlet num = 12345;\nnum.replace(\"2\", \"9\"); // ❌ ERROR - numbers aren't strings\n```\n\n**Solution:** Convert to string first:\n```javascript\nlet num = 12345;\nlet newNum = num.toString().replace(\"2\", \"9\"); \nconsole.log(newNum); // \"19345\"\n\n// Using regex with numbers\nlet zeroedNum = num.toString().replace(/\\d/g, \"0\"); \nconsole.log(zeroedNum); // \"00000\"\n```\n\n**Key Points:**\n- Works only on strings\n- Use regex with `g` flag for global replacement\n- Returns a new string (original string unchanged)\n- Can use capture groups for advanced replacements",
    "type": "javascript"
  },
  {
    "question": "What are some Express.js request properties and methods?",
    "answer": "**Simple Definition:**\nExpress provides properties and methods on the `req` (request) object to get information about incoming HTTP requests.\n\n**Request Properties (Data about the request):**\n```javascript\n// HTTP Method & URL\nreq.method      // \"GET\", \"POST\", \"PUT\", \"DELETE\" \nreq.protocol    // \"http\" or \"https\"\nreq.originalUrl // \"/api/posts/5\" (full URL path)\nreq.path        // \"/api/posts/5\" (same as originalUrl)\nreq.params      // { id: \"5\" } (URL parameters)\nreq.query       // { limit: \"10\" } (query string ?limit=10)\n\n// Headers & Host\nreq.headers     // All request headers\nreq.get('host') // \"localhost:3000\" or \"mysite.com\"\nreq.hostname    // \"localhost\" or \"mysite.com\"\n\n// Security & IP\nreq.secure      // true if HTTPS, false if HTTP\nreq.ip          // \"192.168.1.1\" (client IP address)\n\n// Body data (with body-parser middleware)\nreq.body        // Parsed request body data\n```\n\n**Request Methods (Functions you can call):**\n```javascript\n// Header methods\nreq.get('Content-Type')        // Get specific header value\nreq.is('application/json')     // Check content type\n\n// Parameter methods  \nreq.param('id')               // Get parameter from URL or query\n\n// Other methods\nreq.accepts('json')           // Check what formats client accepts\nreq.accepts(['json', 'html']) // Check multiple formats\n```\n\n**Middleware Example Using Properties/Methods:**\n```javascript\nconst logger = (req, res, next) => {\n    console.log(`${req.method} ${req.protocol}://${req.get('host')}${req.originalUrl}`);\n    // Example output: \"GET http://localhost:3000/api/posts/5\"\n    next();\n};\n\n// Route handler example\napp.get('/api/users/:id', (req, res) => {\n    console.log(req.params.id);    // URL parameter\n    console.log(req.query.page);   // Query string\n    console.log(req.method);       // HTTP method\n    res.json({ success: true });\n});\n```\n\n**Key Points:**\n- **Properties** = Data stored on the request object (use directly)\n- **Methods** = Functions you call on the request object (use with parentheses)\n- All properties/methods are available in every middleware and route handler\n\n**Key Idea:** Express request properties and methods give you detailed information about incoming HTTP requests, making it easy to build dynamic, responsive web applications.",
    "type": "nodejs"
  },

  {
    "question": "How does route prefixing work in Express.js?",
    "answer": "**Simple Definition:**\nRoute prefixing adds a common base path to multiple routes, so you don't have to repeat the same path segment for every route.\n\n**🆚 Before vs After Prefixing:**\n\n**BEFORE (Repetitive Routes):**\n```javascript\n// ❌ Repeating \"/api/users\" everywhere\napp.get(\"/api/users\", userController.getUsers);\napp.get(\"/api/users/:id\", userController.getUser);\napp.post(\"/api/users\", userController.createUser);\napp.put(\"/api/users/:id\", userController.updateUser);\napp.delete(\"/api/users/:id\", userController.deleteUser);\n\n// ❌ Repeating \"/api/posts\" everywhere  \napp.get(\"/api/posts\", postController.getPosts);\napp.get(\"/api/posts/:id\", postController.getPost);\napp.post(\"/api/posts\", postController.createPost);\n```\n\n**AFTER (With Prefixing):**\n```javascript\n// ✅ Define prefix once, use clean routes\napp.use(\"/api/users\", userRoutes);  // 🎯 Prefix: \"/api/users\"\napp.use(\"/api/posts\", postRoutes);  // 🎯 Prefix: \"/api/posts\"\napp.use(\"/api/auth\", authRoutes);   // 🎯 Prefix: \"/api/auth\"\n```\n\n**🔧 How Route Prefixing Works:**\n\n**1. Create Route Files with Clean Paths:**\n```javascript\n// routes/userRoutes.js\nconst express = require('express');\nconst router = express.Router();\n\nrouter.get(\"/\", userController.getUsers);        // Becomes: /api/users\nrouter.get(\"/:id\", userController.getUser);      // Becomes: /api/users/1\nrouter.post(\"/\", userController.createUser);     // Becomes: /api/users\nrouter.put(\"/:id\", userController.updateUser);   // Becomes: /api/users/1\nrouter.delete(\"/:id\", userController.deleteUser);// Becomes: /api/users/1\n\nmodule.exports = router;\n```\n\n**2. Apply Prefixes in Main File:**\n```javascript\n// server.js\nconst userRoutes = require('./routes/userRoutes');\nconst postRoutes = require('./routes/postRoutes');\n\napp.use(\"/api/users\", userRoutes);  // 🎯 Adds \"/api/users\" prefix\napp.use(\"/api/posts\", postRoutes);  // 🎯 Adds \"/api/posts\" prefix \napp.use(\"/api/auth\", authRoutes);   // 🎯 Adds \"/api/auth\" prefix\n```\n\n**3. Resulting URLs:**\n```\nuserRoutes.js paths + \"/api/users\" prefix = Final URLs:\n\n\"/\"          + \"/api/users\" = \"/api/users\"\n\"/:id\"       + \"/api/users\" = \"/api/users/1\" \n\"/\" (POST)   + \"/api/users\" = \"/api/users\"\n\"/:id\" (PUT) + \"/api/users\" = \"/api/users/1\"\n```\n\n**Benefits of Route Prefixing:**\n- **Cleaner code**: No repeated path segments\n- **Better organization**: Group related routes together\n- **Easier maintenance**: Change prefix in one place\n- **Scalable**: Easy to add new route groups\n\n**Key Idea:** Route prefixing is like giving a common address to a group of related routes, making your code more organized and maintainable.",
    "type": "nodejs"
  },
  {
    "question": "When should you use parentheses () in classes?",
    "answer": "**Always use parentheses () when you want to execute/call a function in a class and get its result.**\n\n**🎯 With vs Without Parentheses:**\n\n**Without () - Gets the FUNCTION ITSELF:**\n```javascript\nclass UserService {\n  getUsers() {\n    return [\"John\", \"Jane\"];\n  }\n}\n\nconst service = new UserService();\n\n// ❌ Gets the function object (not what we want)\nconsole.log(service.getUsers); \n// Output: [Function: getUsers]\n```\n\n**With () - Gets the RESULT:**\n```javascript\n// ✅ Calls the function and gets the return value\nconsole.log(service.getUsers()); \n// Output: [\"John\", \"Jane\"]\n```\n\n**When to Use ():**\nALWAYS use () when you want to:\n- Execute a function\n- Get a return value\n- Call a method\n- Trigger an action\n\n**Real-world Examples:**\n```javascript\nclass BookController {\n  getAllBooks() {\n    return [\"Book 1\", \"Book 2\"];\n  }\n  \n  createBook(bookData) {\n    // Save book logic\n    return { id: 1, ...bookData };\n  }\n}\n\nconst controller = new BookController();\n\n// ✅ CORRECT - using parentheses to call methods\nconst books = controller.getAllBooks();\nconst newBook = controller.createBook({ title: \"New Book\" });\n\n// ❌ INCORRECT - missing parentheses\nconst functionReference = controller.getAllBooks; // This is the function itself, not the result\n```\n\n**Key Idea:** Parentheses `()` are like pressing the \"go\" button - they actually execute the function and give you the result.",
    "type": "javascript"
  },
  {
    "question": "What are the two ways to write methods in classes?",
    "answer": "**Method 1: Regular Function (Standard Way)**\n```javascript\nclass BookController {\n  getAllBooks(req, res) {\n    // This is correct and standard!\n    res.json([\"Book 1\", \"Book 2\"]);\n  }\n  \n  createBook(req, res) {\n    const { title, author } = req.body;\n    // Handle book creation\n    res.status(201).json({ id: 1, title, author });\n  }\n}\n```\n\n**Method 2: Arrow Function (Alternative Way)**\n```javascript\nclass BookController {\n  getAllBooks = (req, res) => {\n    // This also works!\n    res.json([\"Book 1\", \"Book 2\"]);\n  }\n  \n  createBook = (req, res) => {\n    const { title, author } = req.body;\n    // Handle book creation\n    res.status(201).json({ id: 1, title, author });\n  }\n}\n```\n\n**The Difference:**\n- **Regular functions** are the standard way in classes\n- **Arrow functions** behave differently with `this` keyword\n\n**When to Use Each:**\n- **Use Regular Functions** for most cases (standard, cleaner syntax)\n- **Use Arrow Functions** when you need specific `this` binding behavior\n\n**Example Showing the Difference:**\n```javascript\nclass Example {\n  constructor() {\n    this.name = \"Example\";\n  }\n  \n  // Regular function - 'this' refers to class instance\n  regularMethod() {\n    console.log(this.name); // \"Example\"\n  }\n  \n  // Arrow function - 'this' is bound differently\n  arrowMethod = () => {\n    console.log(this.name); // Also \"Example\" but bound at creation\n  }\n}\n```\n\n**For Your Controllers (Current Use Case):**\n```javascript\n// ✅ Recommended - Regular functions (simpler, standard)\nclass BookController {\n  getAllBooks(req, res) {\n    // We don't use 'this' in our controllers yet\n    // So regular functions work perfectly\n    res.json([\"Book 1\", \"Book 2\"]);\n  }\n}\n```\n\n**Key Idea:** Both ways work, but regular functions are the standard approach for class methods unless you need specific `this` binding behavior.",
    "type": "javascript"
  },
  {
    "question": "When do you and do you not need a constructor in a class?",
    "answer": "**You DON'T always need a constructor!**\n\n**When you NEED a constructor:**\n- When you need to initialize properties\n- When you need to set up things when the class is created\n- When you need to accept parameters during object creation\n\n**Example NEEDING Constructor:**\n```javascript\nclass Person {\n  constructor(name, age) {\n    this.name = name;  // Setting up properties\n    this.age = age;\n    this.createdAt = new Date();  // Initial setup\n  }\n  \n  greet() {\n    return `Hello, my name is ${this.name}`;\n  }\n}\n\n// Usage:\nconst person = new Person(\"John\", 25);\nconsole.log(person.greet()); // \"Hello, my name is John\"\n```\n\n**When you DON'T need a constructor:**\n- When your class is just a collection of methods\n- When you don't have initial data to set up\n- When you're creating utility/helper classes\n\n**Example NOT NEEDING Constructor:**\n```javascript\nclass BookController {\n  // No constructor needed - just methods!\n  getAllBooks(req, res) {\n    res.json([\"Book 1\", \"Book 2\"]);\n  }\n  \n  createBook(req, res) {\n    const { title, author } = req.body;\n    res.status(201).json({ id: 1, title, author });\n  }\n  \n  // Static utility method - also doesn't need constructor\n  static validateBook(bookData) {\n    return bookData.title && bookData.author;\n  }\n}\n\n// Usage:\nconst controller = new BookController();\ncontroller.getAllBooks(req, res);\n```\n\n**Another Example - Service Class:**\n```javascript\nclass CalculatorService {\n  // No constructor - just methods that perform operations\n  add(a, b) {\n    return a + b;\n  }\n  \n  multiply(a, b) {\n    return a * b;\n  }\n  \n  // No initial setup needed, so no constructor\n}\n```\n\n**Key Idea:** Only use a constructor when you need to set up initial state or accept parameters when creating an object. For classes that are just collections of methods (like controllers), you often don't need one.",
    "type": "javascript"
  },
  {
    "question": "What are some ways of exporting classes?",
    "answer": "**Option 1: Export the Class (Create Instance Yourself)**\n```javascript\n// BookController.js\nexport default class BookController {\n  getAllBooks(req, res) {\n    res.json([\"Book 1\", \"Book 2\"]);\n  }\n}\n\n// In routes file, you'd have to:\nimport BookController from \"./BookController.js\";\nconst bookController = new BookController(); // Create instance yourself\n\nrouter.get(\"/books\", bookController.getAllBooks);\n```\n\n**Option 2: Export an Instance (Instance Ready to Use)**\n```javascript\n// BookController.js\nclass BookController {\n  getAllBooks(req, res) {\n    res.json([\"Book 1\", \"Book 2\"]);\n  }\n}\n\nexport default new BookController(); // Export instance\n\n// In routes file, you just:\nimport bookController from \"./BookController.js\"; // Instance is ready to use\n\nrouter.get(\"/books\", bookController.getAllBooks);\n```\n\n**Option 3: Export Multiple Classes/Utilities**\n```javascript\n// utils/validation.js\nexport class BookValidator {\n  static validateCreate(bookData) {\n    return bookData.title && bookData.author;\n  }\n}\n\nexport class UserValidator {\n  static validateCreate(userData) {\n    return userData.email && userData.password;\n  }\n}\n\n// Import multiple:\nimport { BookValidator, UserValidator } from \"./utils/validation.js\";\n```\n\n**Comparison:**\n\n**Export Class (Option 1):**\n- ✅ Can create multiple instances\n- ✅ More flexible\n- ❌ Need to instantiate manually\n\n**Export Instance (Option 2):**\n- ✅ Convenient - ready to use\n- ✅ Good for singleton pattern\n- ❌ Only one instance possible\n\n**Which to Use:**\n- **Use Export Class** when you might need multiple instances\n- **Use Export Instance** for controllers/services (usually one is enough)\n- **Use Multiple Exports** for utility classes\n\n**Real-world Example for Controllers:**\n```javascript\n// ✅ Good for controllers - we usually need just one instance\n// BookController.js\nclass BookController {\n  getAllBooks(req, res) { /* ... */ }\n  createBook(req, res) { /* ... */ }\n}\n\nexport default new BookController(); // Export instance\n\n// routes/books.js\nimport bookController from \"../controllers/BookController.js\";\n\nrouter.get(\"/\", bookController.getAllBooks); // Ready to use!\nrouter.post(\"/\", bookController.createBook);\n```\n\n**Key Idea:** Choose your export strategy based on whether you need the flexibility of multiple instances or the convenience of a ready-to-use singleton.",
    "type": "javascript"
  },

  {
    "question": "Why does 'this' become undefined in Express controllers and how to fix it?",
    "answer": "**The Problem:**\nIn Express, when you pass a class method directly to a route:\n```javascript\nrouter.get(\"/\", bookController.getAllBooks);\n```\n\nExpress calls it like a plain function:\n```javascript\ngetAllBooks(req, res); // Not bookController.getAllBooks(...)\n```\n\nBecause it's no longer called as `bookController.getAllBooks(...)`, the method loses its `this` context.\n\n**Result:**\n```javascript\nTypeError: Cannot read properties of undefined (reading 'library')\n```\n\n**✔ How to Fix It:**\n\n**1. Use Arrow Functions (Best + Easiest)**\nArrow functions automatically bind `this` to the instance.\n\n```javascript\nclass BookController {\n  constructor() {\n    this.library = [\n      { id: 1, title: \"Book 1\", author: \"Author 1\" },\n      { id: 2, title: \"Book 2\", author: \"Author 2\" }\n    ];\n  }\n\n  getAllBooks = (req, res) => {\n    res.status(200).json(this.library);\n  };\n\n  createBook = (req, res) => {\n    res.status(201).json({ msg: \"Soon…\" });\n  };\n}\n\nexport default new BookController();\n```\n\n**2. Alternative: Bind in Constructor**\n```javascript\nclass BookController {\n  constructor() {\n    this.library = [...];\n    \n    // Manually bind methods\n    this.getAllBooks = this.getAllBooks.bind(this);\n    this.createBook = this.createBook.bind(this);\n  }\n\n  getAllBooks(req, res) {\n    res.status(200).json(this.library);\n  }\n\n  createBook(req, res) {\n    res.status(201).json({ msg: \"Soon…\" });\n  }\n}\n```\n\n**3. Alternative: Bind in Routes**\n```javascript\n// In routes file\nrouter.get(\"/\", bookController.getAllBooks.bind(bookController));\n```\n\n**Why Arrow Functions Work Best:**\n- Arrow functions don't have their own `this` context\n- They inherit `this` from the surrounding scope (the class instance)\n- No manual binding needed\n- Cleaner syntax\n\n**Works with:**\n```javascript\nrouter.get(\"/\", bookController.getAllBooks); // No binding needed!\n```\n\n**Key Idea:** Use arrow functions for class methods in Express controllers to automatically maintain the correct `this` context.",
    "type": "nodejs"
  },
  {
    "question": "How do you create a simple REST API to make POST and GET requests?",
    "answer": "**Complete REST API Structure with POST and GET:**\n\n**1. 🚀 Server Setup (server.js):**\n```javascript\nimport express from 'express';\nimport bookRoutes from './routes/bookRoutes.js';\n\nconst app = express();\nconst PORT = 9000;\n\n// Middleware to parse JSON\napp.use(express.json());\n\n// Routes with prefix\napp.use('/api/books', bookRoutes.getRouter());\n\n// Start server\napp.listen(PORT, () => {\n  console.log(`📚 Book API running on http://localhost:${PORT}`);\n});\n```\n\n**2. 📝 Controller (Business Logic - controllers/BookController.js):**\n```javascript\nclass BookController {\n  constructor() {\n    this.books = []; // Start with empty array\n  }\n\n  // GET all books\n  getAllBooks = (req, res) => {\n    res.json({\n      success: true,\n      count: this.books.length,\n      data: this.books\n    });\n  }\n\n  // POST new book\n  createBook = (req, res) => {\n    const { title, author } = req.body;\n    \n    // Simple validation\n    if (!title || !author) {\n      return res.status(400).json({\n        success: false,\n        error: \"Title and author are required\"\n      });\n    }\n\n    // Create new book\n    const newBook = {\n      id: this.books.length + 1,\n      title,\n      author,\n      createdAt: new Date().toISOString()\n    };\n\n    // Add to array\n    this.books.push(newBook);\n\n    // Return response\n    res.status(201).json({\n      success: true,\n      data: newBook\n    });\n  }\n\n  // GET single book by ID\n  getBookById = (req, res) => {\n    const bookId = parseInt(req.params.id);\n    const book = this.books.find(b => b.id === bookId);\n    \n    if (!book) {\n      return res.status(404).json({\n        success: false,\n        error: \"Book not found\"\n      });\n    }\n    \n    res.json({\n      success: true,\n      data: book\n    });\n  }\n}\n\n// Export instance (ready-to-use)\nexport default new BookController();\n```\n\n**3. 🛣️ Routes (URL Mapping - routes/bookRoutes.js):**\n```javascript\nimport express from 'express';\nimport bookController from '../controllers/BookController.js';\n\nclass BookRoutes {\n  constructor() {\n    this.router = express.Router();\n    this.setupRoutes();\n  }\n\n  setupRoutes() {\n    // GET /api/books - Get all books\n    this.router.get('/', bookController.getAllBooks);\n    \n    // GET /api/books/:id - Get single book\n    this.router.get('/:id', bookController.getBookById);\n    \n    // POST /api/books - Create new book\n    this.router.post('/', bookController.createBook);\n  }\n\n  getRouter() {\n    return this.router;\n  }\n}\n\n// Export instance\nexport default new BookRoutes();\n```\n\n**4. 📁 Project Structure:**\n```\nbook-api/\n├── server.js\n├── controllers/\n│   └── BookController.js\n├── routes/\n│   └── bookRoutes.js\n└── package.json\n```\n\n**5. 🧪 Testing with Postman:**\n\n**GET All Books:**\n```\nGET http://localhost:9000/api/books\nResponse: { \"success\": true, \"count\": 0, \"data\": [] }\n```\n\n**POST New Book:**\n```\nPOST http://localhost:9000/api/books\nBody (JSON):\n{\n  \"title\": \"The Great Gatsby\",\n  \"author\": \"F. Scott Fitzgerald\"\n}\nResponse: {\n  \"success\": true,\n  \"data\": {\n    \"id\": 1,\n    \"title\": \"The Great Gatsby\",\n    \"author\": \"F. Scott Fitzgerald\",\n    \"createdAt\": \"2023-10-01T10:30:00.000Z\"\n  }\n}\n```\n\n**GET Single Book:**\n```\nGET http://localhost:9000/api/books/1\nResponse: { \"success\": true, \"data\": { ... } }\n```\n\n**🔑 Key Concepts Summary:**\n- **Server Setup**: Express app + JSON middleware\n- **Controller**: Contains business logic (GET, POST)\n- **Routes**: Maps URLs to controller methods\n- **Export Pattern**: Export instances for easy use\n- **Prefixing**: `/api/books` + `/` = `/api/books`\n\n**🔄 Data Flow:**\n```\nClient → Server → Routes → Controller → Response\n   ↓       ↓         ↓         ↓         ↓\nPostman → Express → Router → Methods → JSON Back\n```\n\n**Key Idea:** This structure provides a clean, scalable foundation for building REST APIs with proper separation of concerns and OOP principles.",
    "type": "nodejs"
  },
  {
    "question": "What middleware function must you use when destructuring req.body?",
    "answer": "**You MUST use `app.use(express.json())` middleware when destructuring `req.body`.**\n\n**Why This Middleware is Essential:**\n\n**Express does NOT parse JSON by default**\n\nWhen you send a POST request with a JSON body:\n```json\n{\n  \"author\": \"James Carter\"\n}\n```\n\nExpress does not automatically convert that JSON into `req.body`.\n\nWithout the JSON parser, Express literally receives the raw bytes:\n```\n7B 20 22 61 75 74 68 6F 72 22 ...\n```\nWorthless to your controller.\n\n**❌ Without express.json():**\n```javascript\ncreateBook = (req, res) => {\n  console.log(req.body); // undefined\n  \n  // Your destructuring will break:\n  const { author } = req.body;\n  // TypeError: Cannot read properties of undefined\n};\n```\n\n**✅ With express.json():**\n```javascript\napp.use(express.json());\n```\n\nNow Express converts incoming JSON into a proper JavaScript object:\n```javascript\nreq.body = {\n  author: \"James Carter\"\n}\n```\n\nYour controller now works:\n```javascript\nconst { author } = req.body; // \"James Carter\"\n```\n\n**🧠 Where to Place express.json():**\n\n`server.js` (or `app.js`) is where you configure your Express app before any routes run. Middlewares must be set up first, otherwise your controllers will receive unparsed requests.\n\n**Flow is:**\n```\nMiddleware (parsers, loggers, CORS)\n↓\nRoutes\n↓\nControllers\n```\n\n**Correct Setup:**\n```javascript\n// server.js\nimport express from 'express';\n\nconst app = express();\n\n// ✅ MIDDLEWARE FIRST\napp.use(express.json());\napp.use(express.urlencoded({ extended: true }));\n\n// ✅ THEN ROUTES\napp.use('/api/books', bookRoutes);\napp.use('/api/users', userRoutes);\n\n// If you put app.use(express.json()) after your routes, \n// it's useless — the controllers won't get parsed bodies.\n```\n\n**Key Idea:** Always set up `express.json()` middleware BEFORE your routes to properly parse JSON request bodies and enable destructuring of `req.body`.",
    "type": "nodejs"
  },
  {
    "question": "How do you hash passwords with bcrypt?",
    "answer": "**Why Hash Passwords?**\n- **Security**: Never store plain text passwords\n- **Irreversible**: Can't get original password from hash\n- **Different hashes**: Same password → different hash every time\n\n**Installation:**\n```bash\nnpm install bcryptjs\n```\n\n**Basic Usage:**\n\n**1. Hashing Passwords (Registration):**\n```javascript\nimport bcrypt from 'bcryptjs';\n\n// Hash a password\nconst plainPassword = \"user123\";\nconst hashedPassword = bcrypt.hashSync(plainPassword, 10);\n// Result: \"$2a$10$N9qo8uLOickgx2ZMRZoMye...\"\n\n// Store hashedPassword in database instead of plain text\n```\n\n**2. Verifying Passwords (Login):**\n```javascript\n// When user tries to login\nconst isPasswordValid = bcrypt.compareSync(\"user123\", hashedPassword);\n// Returns: true (if correct) or false (if wrong)\n```\n\n**In UserService Class:**\n```javascript\nclass UserService {\n  constructor() {\n    this.users = [];\n  }\n  \n  registerUser(userData) {\n    const { email, password } = userData;\n    \n    // Hash before storing\n    const hashedPassword = bcrypt.hashSync(password, 10);\n    \n    const user = {\n      id: this.users.length + 1,\n      email: email,\n      password: hashedPassword, // Store the hash!\n      createdAt: new Date()\n    };\n    \n    this.users.push(user);\n    return user;\n  }\n\n  loginUser(email, password) {\n    const user = this.users.find(u => u.email === email);\n    if (!user) {\n      throw new Error(\"User not found\");\n    }\n    \n    // Verify password\n    const isValid = bcrypt.compareSync(password, user.password);\n    if (!isValid) {\n      throw new Error(\"Invalid password\");\n    }\n    \n    return user; // Login successful\n  }\n  \n  findUserByEmail(email) {\n    return this.users.find(u => u.email === email);\n  }\n}\n```\n\n**Async/Await Version (Recommended):**\n```javascript\nclass UserService {\n  async registerUser(userData) {\n    const { email, password } = userData;\n    \n    // Hash with async/await\n    const hashedPassword = await bcrypt.hash(password, 10);\n    \n    const user = {\n      email: email,\n      password: hashedPassword,\n    };\n    \n    return user;\n  }\n\n  async loginUser(email, password) {\n    const user = this.findUserByEmail(email);\n    if (!user) {\n      throw new Error(\"User not found\");\n    }\n    \n    // Verify with async/await\n    const isValid = await bcrypt.compare(password, user.password);\n    if (!isValid) {\n      throw new Error(\"Invalid password\");\n    }\n    \n    return user;\n  }\n}\n```\n\n**Key Points:**\n- **Salt Rounds (10)**: Higher = more secure but slower (10-12 is recommended)\n- **compareSync()**: Checks if password matches hash\n- **Never store plain passwords** in database\n- **Never return passwords** in API responses\n- Use **async methods** (`bcrypt.hash()` and `bcrypt.compare()`) in production for better performance\n\n**Security Benefits:**\n- ✅ Same password → different hashes (due to unique salt)\n- ✅ Slow hashing prevents brute force attacks\n- ✅ Can't reverse engineer passwords from hashes\n- ✅ Protects users even if database is compromised\n\n**Use this for ALL user authentication! 🛡️**",
    "type": "nodejs"
  },
  {
    "question": "What is Markdown syntax and how do you use it?",
    "answer": "# Markdown Syntax - Complete Guide\n\n## 📝 Basic Text Formatting\n\n**Headings:**\n```markdown\n# Heading 1\n## Heading 2\n### Heading 3\n#### Heading 4\n##### Heading 5\n###### Heading 6\n```\n\n**Text Styles:**\n```markdown\n_Italic Text_ or *Italic Text*\n**Bold Text** or __Bold Text__\n~~Strikethrough Text~~\n```\n\n## 🎯 Advanced Formatting\n\n**Horizontal Rule:**\n```markdown\n---\n***\n___\n```\n\n**Blockquote:**\n```markdown\n> This is a blockquote\n> It can span multiple lines\n```\n\n**Links:**\n```markdown\n[Display Text](https://example.com)\n[Google](https://www.google.com \"Google Homepage\")\n```\n\n## 📋 Lists\n\n**Unordered Lists:**\n```markdown\n- Item 1\n- Item 2\n  - Nested Item 1\n  - Nested Item 2\n- Item 3\n```\n\n**Ordered Lists:**\n```markdown\n1. First item\n2. Second item\n3. Third item\n```\n\n## 💻 Code & Technical\n\n**Inline Code:**\n```markdown\nUse `console.log()` for debugging.\n```\n\n**Code Blocks:**\n````markdown\n```javascript\nfunction add(a, b) {\n  return a + b;\n}\n```\n\n```bash\nnpm install\nnpm start\n```\n\n```python\ndef add(a, b):\n    return a + b\n```\n````\n\n## 🖼️ Media & Tables\n\n**Images:**\n```markdown\n![Alt Text](https://example.com/image.jpg)\n![Markdown Logo](https://markdown-here.com/img/icon256.png)\n```\n\n**Tables:**\n```markdown\n| Name      | Email          | Age |\n|-----------|----------------|-----|\n| John Doe  | john@email.com | 25  |\n| Jane Smith| jane@email.com | 30  |\n```\n\n## ✅ GitHub Extras\n\n**Task Lists:**\n```markdown\n- [x] Completed task\n- [ ] Incomplete task\n- [x] Another completed task\n```\n\n**Escaping Characters:**\n```markdown\n\\*This shows asterisks\\*\n\\# This shows a hash\n```\n\n## 🚀 Pro Tips\n- Use **consistent heading hierarchy**\n- **Tables** work best with clear column alignment\n- **Code blocks** with language specification get syntax highlighting\n- **Task lists** are great for README progress tracking\n- **Relative links** work for repository navigation\n\nMarkdown is perfect for documentation, README files, and note-taking!",
    "type": "markdown",
    "tags": [
      "markdown",
      "documentation",
      "github",
      "readme",
      "formatting",
      "syntax"
    ]
  }
]
